# Current Feature: Support Page

## Status

In Progress

## Goals

- Turn the static prototype `prototypes/homepage/support.html` into the real Support page at `/support`, replacing the placeholder `src/app/support/page.tsx` — rebuilt as React server components + one small client `ContactForm`, using Tailwind + the existing marketing primitives (5th marketing-page rebuild, mirroring About/Blog/Docs).
- Render four blocks (each wrapped in `Reveal`): a **page header** (pill + gradient `<h1>` "How can we *help?*" + subtitle), a **quick-help card row** (3 `HelpCard`s), an **FAQ list** (7 Q&As, verbatim), and a **contact form** (`#contact` anchor).
- Keep the route public: server component in `MarketingShell`, no `auth()` redirect, no `proxy.ts` matcher change; `metadata` title "Support" + prototype description.
- Wire the contact form to a **real `POST /api/support`** route: Zod `supportSchema`, Resend-backed `sendSupportEmail` (replyTo the sender, to `SUPPORT_EMAIL`), IP rate-limit (3/1h), `z.flattenError` 400 with `issues`, generic 500 on delivery failure. Not in the proxy matcher.
- Add `SUPPORT_HELP` + `SUPPORT_FAQS` data arrays to `constants.ts`; new `HelpCard`, `FaqItem`, `ContactForm` components under `src/components/marketing/`.
- Style with Tailwind arbitrary values + `--m-*` tokens only — **no new `globals.css` classes**; responsive (help cards 3→1, form row 2→1, no overflow at 390px).
- Add `src/lib/validations/support.test.ts` (Vitest) for `supportSchema`; existing suite must still pass. Verify test/tsc/lint/build + browser (`/support` static, `/api/support` `ƒ`, `ƒ Proxy` intact).

## Notes

- **Authoritative spec:** `context/features/support-page-spec.md`. Mirrors the completed About / Blog Listing/Detail / Documentation rebuilds — same `MarketingShell`, `--m-*` tokens, `Reveal`, pill + gradient-text, and `constants.ts` data-array pattern. Unlike those, the contact form hits a real Resend-backed API route.
- **Components:** `HelpCard` (server — centered brand-tinted icon tile + h3 + p in a `next/link`; props `{ icon, title, description, href }`; port `.help-card`); `FaqItem` (server — h3 with a monospace brand `Q.` marker `aria-hidden` + question, p answer that may hold inline `<code>`; props `{ question, answer: ReactNode }`); `ContactForm` (**client** — controlled Name/Email/Subject/Message → `fetch("/api/support")`, disable while pending, `toastManager` success/error, reset on success, inline field errors from `issues`; mirror `ForgotPasswordForm`/`SignInForm`).
- **Page composition:** page header, quick help (3 cards, grid 3→1), FAQ (section head + 7 items in `max-w-[760px]`), contact (section head `id="contact"` + `scroll-mt` + `<ContactForm />`). Reuse a small local `SectionHead` helper (centered, `max-w-[640px]`, h2 + dim sub), as About/Docs did.
- **`SUPPORT_HELP`** (`{ icon: LucideIcon; title; description; href }[]`): Read the docs (`BookOpen`/`FileText`) → `/docs`; Join the community (`Users`) → `#` (placeholder stub); Email the team (`Mail`) → `#contact`.
- **`SUPPORT_FAQS`** (`{ question; answer: ReactNode }[]`): 7 Q&As copied verbatim — What is DevStash / free plan / Pro cost ($8/mo, $72/yr) / Pro features / item types / How does search work (keep inline `<code>⌘K</code>` + `<code>Ctrl K</code>`) / GitHub sign-in. `answer` is `ReactNode` so the search entry embeds `<code>`.
- **Contact API:** `src/lib/validations/support.ts` → `supportSchema` (name `.trim().min(1).max(100)`, email `.trim().toLowerCase().email()`, subject `.trim().min(1).max(200)`, message `.trim().min(1).max(5000)`; export `SupportInput`). `sendSupportEmail({ name, email, subject, message })` in `src/lib/email.ts` (reuse `resend` + `FROM`; to `SUPPORT_EMAIL` env + fallback const; `replyTo: email`; subject `[Support] ${subject}`; inline HTML like `verificationEmailHtml`; throws on Resend error). `support` entry in `RATE_LIMITS` (`{ limit: 3, window: "1 h", prefix: "rl:support" }`, keyed by IP via `getClientIp`). Route `POST /api/support` modeled on `forgot-password/route.ts`: rate-limit → parse JSON → `safeParse` 400 `{ success:false, issues }` → `sendSupportEmail` try/catch → `{ success:true }` / generic 500.
- **Styling:** port `styles.css:963–1044` to Tailwind arbitrary values. Help cards `grid gap-5 md:grid-cols-3`, card surface + hover lift/brand glow, icon tile `size-12 rounded-xl grid place-items-center text-[var(--m-brand)] bg-[rgba(99,102,241,0.14)]`. FAQ `mx-auto max-w-[760px] flex flex-col gap-3.5`, item surface card, h3 with `flex-shrink-0 font-mono text-[var(--m-brand)]` `Q.` marker. Contact form `mx-auto max-w-[620px]` surface card `p-[30px]`, `.form-row` `grid gap-4 sm:grid-cols-2`, fields label + input/textarea (`bg-[var(--m-bg)] border-[var(--m-border-2)] rounded-[10px]`, brand focus ring, textarea `resize-y min-h-[130px]`), submit `marketingButton({ variant:"primary", size:"lg", block:true })`. **No ShadCN Input/Textarea** (use `--m-*` tokens, consistent with prior four pages).
- **Files:** New — `src/app/support/page.tsx` (replace placeholder), `src/app/api/support/route.ts`, `src/lib/validations/support.ts`, `src/lib/validations/support.test.ts`, `HelpCard.tsx`, `FaqItem.tsx`, `ContactForm.tsx`. Edit — `constants.ts`, `src/lib/email.ts`, `src/lib/rate-limit.ts`, `.env.example`. Reuse — `MarketingShell`, `Reveal`, `marketing-button.ts`, Hero pill/gradient, `toastManager`, `getClientIp`/`checkRateLimit`/`rateLimitResponse`, the `z.flattenError` 400 pattern.
- **Out of scope:** other interior pages, storing tickets in the DB, attachments/captcha beyond rate limiting, real content beyond the prototype copy.

## History

<!-- Keep this updated. Earliest to latest -->

- **Initial Next.js setup** — Bootstrapped the project with Create Next App, pinned the Node version (`.nvmrc` → v24.18.0), and cleaned up the Next.js boilerplate. Added the `CLAUDE.md` guide, project context files (`context/`), coding standards, and AI interaction guidelines.
- **Dashboard UI — Phase 1** — Initialized ShadCN UI (Tailwind v4) and added the `button` and `input` components. Set dark mode as the default on `<html>` and fixed the ShadCN font token so Geist loads correctly. Built the `/dashboard` route with the shell layout: a full-height sidebar (with the DevStash `Logo` brand mark on top) and a main column with a display-only top bar (search field + "New Collection" / "New Item" buttons). Sidebar and main content are `h2` placeholders for Phase 2.
- **Dashboard UI — Phase 2 (Sidebar)** — Built out the full sidebar from `mock-data.ts`. Added a `SidebarProvider` context (`sidebar-context.tsx`) plus a `useIsMobile` hook to drive one toggle that collapses the desktop rail to an icon-only strip and opens/closes a slide-in drawer on mobile (with overlay, `Escape`-to-close, and body-scroll lock). The `Sidebar` renders Home / All items, an **Item Types** section linking to `/items/<type>` (colored lucide icons via `ItemTypeIcon`, counts, `Pro` badges on file/image types), a **Collections** section (favorites starred + recent, colored type dots, `+` action), and a user area at the bottom (avatar, name, plan · email). Added a `SidebarToggle` (`PanelLeft`) to the top bar and made the `Logo` collapse to just its brand mark. Build and lint pass.
- **Dashboard UI — Phase 3 (Main Content)** — Built out the main content area from `mock-data.ts` as server components. Added a `Dashboard` page heading, a `StatsCards` row (Items, Collections, Favorite Items, Favorite Collections — derived counts), and three sections via a shared `SectionHeader` (icon + title + count): **Collections** (`CollectionCard` grid, background tinted by the collection's primary item type with a badge per type present), **Pinned Items**, and **Recent Items** (`ItemCard` with a left border colored by item type, preview box, tag chips, relative time, and a pin/favorite marker). Added a shared `ItemTypeBadge` (colored icon square) reused by both card types, and set a route-level `metadata` title of "Dashboard". Marked a few mock items as favorites so the favorite stats read non-zero. Build and lint pass.
- **Prisma + Neon PostgreSQL Setup** — Wired up **Prisma 7** against a **Neon** (serverless) Postgres dev branch. Followed Prisma 7's breaking changes: the `prisma-client` generator (not `prisma-client-js`) with a mandatory `output` at `src/generated/prisma` (gitignored; generated on `postinstall` + `build`), a required **driver adapter** (`@prisma/adapter-pg` over node-postgres) in the `src/lib/prisma.ts` client singleton, and the datasource url moved to `prisma.config.ts` (loaded via `dotenv`, CLI-only, prefers `DIRECT_URL` for migrate then falls back to `DATABASE_URL`). Authored `prisma/schema.prisma` from the `project-overview.md` data model plus **NextAuth models** (Account, Session, VerificationToken) with indexes and cascade deletes, and created/applied the initial migration with `migrate dev` (never `db push`). Added an idempotent `prisma/seed.ts` that seeds from `src/lib/mock-data.ts` (1 user, 7 system item types, 8 collections, 10 items, 26 tags, 27 item↔collection links) and a `scripts/test-db.ts` connectivity check. Added npm scripts: `db:migrate`, `db:migrate:deploy`, `db:seed`, `db:studio`, `db:test`. Build and lint pass; seed and DB test verified against the live Neon branch.
- **Seed Data** — Rewrote `prisma/seed.ts` to the demo dataset in `context/features/seed-spec.md`: one credentials user (**test-engineer@a5project.com** / "Demo User", `isPro: false`, `emailVerified` set, password `12345678` hashed with **bcryptjs** at 12 rounds), the 7 system item types (name/icon/color per spec, `isSystem: true`), and 5 collections with 18 items total — React Patterns (3 TS snippets), AI Workflows (3 prompts), DevOps (1 snippet + 1 command + 2 links), Terminal Commands (4 commands), Design Resources (4 links), with real URLs for all links and tags via `connectOrCreate`. Each collection's `defaultTypeId` is computed as the type it holds most. Added a **`password String?`** field to the `User` model (bcrypt hash; null for OAuth-only accounts) via the `add_user_password` migration (`migrate dev`, never `db push`), and added `bcryptjs` + `@types/bcryptjs`. Seed is idempotent (FK-safe wipe before insert); verified against the live Neon branch (1 user, 7 types, 5 collections, 18 items, 36 tags, 18 links). Build and lint pass.
- **Dashboard Collections — Real Data** — Replaced the dummy collection cards in the dashboard main area with live data from Neon via Prisma (items underneath deferred). Added `src/lib/db/collections.ts` with `getRecentCollections(limit = 6)` — fetches the most recently updated collections with their items' item types, then per collection derives `itemCount` and the item types it holds **ordered most-used first** (drives both the card's primary tint/border color and the badge row) — plus `getCollectionStats()` for total/favorite counts. Reworked `CollectionCard` to take the DB-shaped `DashboardCollection` (using the pre-resolved, pre-ordered `itemTypes`) instead of the `mock-data` lookup, narrowed `ItemTypeBadge`'s prop to `{ name, color, icon }` so it no longer depends on the mock `ItemType`, and switched the `StatsCards` Collections/Favorite Collections tiles to real counts via props (Items/Favorite Items stay on mock until items are wired). The dashboard page now fetches collections + stats via `Promise.all` and calls `await connection()` (Next 16's replacement for `force-dynamic`/`noStore`) so `/dashboard` renders request-time dynamic (`ƒ`) rather than statically baked at build. Pinned/Recent items still use mock data. Build and lint pass; verified against the live Neon branch (5 collections in recency order with correct counts and type badges, stats reading 5 Collections / 3 Favorite Collections).
- **Dashboard Items — Real Data** — Replaced the dummy Pinned/Recent item cards and the Items/Favorite Items stat tiles with live data from Neon via Prisma. Added `src/lib/db/items.ts` with a shared `DashboardItem` shape and mapper (`toDashboardItem`) that derives each card's `preview` from the content type (URL → `url`, FILE → `fileName`, else `content`), formats `updatedAt` into a short relative time (`relativeTime` → "2h ago"/"yesterday"/"3d ago"…), and flattens tags + a `{ id, name, color, icon }` item-type summary — exposing `getPinnedItems()` (isPinned, most-recent first; empty array when none), `getRecentItems(limit = 10)`, and `getItemStats()` (total + favorite counts). Reworked `ItemCard` to take the DB-shaped `DashboardItem` with its pre-resolved `itemType` (dropping the `mock-data` lookup; border/badge still driven by type color), and switched `StatsCards`' Items/Favorite Items tiles to real counts via new props. The dashboard page fetches items + stats alongside collections in one `Promise.all` and **hides the Pinned Items section entirely when nothing is pinned** (per spec). Also added an optional `isPinned` flag to `prisma/seed.ts`'s `SeedItem` (persisted on create) and pinned 3 items — useDebounce hook (snippet), Senior code reviewer (prompt), Prune Docker system (command) — then re-seeded so the Pinned section renders. `mock-data.ts` left untouched (still used by the sidebar). Build and lint pass; a fresh `tsc --noEmit` is clean; verified against the live Neon branch (18 items, 3 pinned, stats wired).
- **Stats & Sidebar — Real Data** — Moved the sidebar off `mock-data.ts` onto live Neon data via Prisma (the main-area stats were already DB-backed from the prior two features, so they were left as-is). Added `getSidebarItemTypes()` to `src/lib/db/items.ts` — the 7 system item types with a live per-type item count (`_count`), ordered to match the documented item-types table, deriving each type's display label + `/items/<slug>` route by pluralizing the singular stored name (e.g. `snippet` → "Snippets" / `snippets`) and its `isPro` flag from the `{file, image}` set (since `ItemType` has no `isPro` column). Added `getSidebarCollections(limit = 6)` to `src/lib/db/collections.ts` — favorites-first then most-recently-updated, each carrying the color of its most-used item type. Converted `Sidebar` from a self-contained mock reader into a props-driven component (`itemTypes`, `collections`) threaded through `SidebarContent` and both the desktop-rail and mobile-drawer render sites; the dashboard page now fetches both alongside the existing data in one `Promise.all`. Item Types link to `/items/<slug>` with colored icons, counts, and Pro badges; collections render a **star** for favorites and a **colored circle** (most-used type color) for recents, with a new **"View all collections"** row linking to `/collections`. The sidebar user area still uses the mock `currentUser` (no auth yet). `/items/<slug>` and `/collections` routes don't exist yet, so those links 404 until built (spec only asked to wire them). Build, lint, and `tsc --noEmit` clean; verified against the live Neon branch (7 item types with correct counts/slugs/Pro badges, 3 favorite + 2 recent collections rendering stars/circles).
- **Add Pro Badge to Sidebar** — Swapped the hand-rolled Pro pill on the File/Image sidebar item types for the **ShadCN `Badge`** component (installed `src/components/ui/badge.tsx` via `npx shadcn@latest add badge`). Rendered as a subtle **`secondary`** variant with a `text-muted-foreground` override, small `text-[10px]` uppercase **"PRO"** text. Extended `NavRow` with an optional `badge` prop that renders **inline right after the label** (label + badge wrapped in a `flex min-w-0 flex-1 items-center gap-2` container so the name truncates while the `shrink-0` badge stays intact), and reworked the Item Types rows so the **trailing slot now always shows the count** for every type (previously Pro rows showed the badge *instead of* a count — File/Image now read their real count, `0`). Net row layout: `📄 Files [PRO] ··· 0`. Build and lint pass; verified against the running dev server via the SSR'd `/dashboard` HTML (badges attach to the Files/Images rows across both the desktop-rail and mobile-drawer render sites, counts present in the trailing slot).
- **Code Scan Fixes — Type-Count Over-fetch & Sidebar Splitting** — Addressed the two actionable findings from a read-only code scan (1 Medium perf, 1 Low splitting), as pure refactors with no UI change. **Medium:** `getRecentCollections` and `getSidebarCollections` in `src/lib/db/collections.ts` were loading one `ItemCollection`→`itemType` join row per item just to tally types in JS. Replaced both with a shared `getTypeCountsByCollection()` helper backed by a single grouped **`$queryRaw`** that joins `ItemCollection → Item → ItemType` and returns per-(collection, type) counts `ORDER BY count DESC, name ASC` (raw SQL because `itemTypeId` lives on `Item`, not the join table, so Prisma `groupBy` can't span the join — the spec's `groupBy(['itemTypeId'])` suggestion isn't expressible). Both callers now `select` only their own scalar columns and derive `itemTypes`/`itemCount`/`color` from the aggregate; return shapes (`DashboardCollection`, `SidebarCollection`) unchanged, so `CollectionCard`/`Sidebar` needed no edits. **Low:** extracted `NavRow` → `src/components/dashboard/NavRow.tsx` (with `NavRowProps`) and `SectionLabel` → `src/components/dashboard/SectionLabel.tsx` from the 355-line `Sidebar.tsx`, imported them back, and dropped the now-dead `Link` import. No schema change (query-shape only), so the migrations policy didn't apply. `tsc --noEmit`, lint, and `npm run build` all clean (`/dashboard` still `ƒ`); verified against the live Neon branch that all 5 collections render identical counts, most-used-first type badges, and sidebar circle colors.
- **Auth Setup — NextAuth + GitHub Provider (Phase 1)** — Set up **NextAuth v5** (`next-auth@^5.0.0-beta.31`) with the **`@auth/prisma-adapter`** using the split-config pattern for Next.js 16 edge/proxy compatibility. Added `src/auth.config.ts` — the edge-safe slice (providers only: `GitHub`, reading `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` from env; no adapter so the proxy bundle never pulls in Prisma/node-postgres) — and `src/auth.ts` — the full Node-runtime instance combining those providers with `PrismaAdapter(prisma)`, `session: { strategy: 'jwt' }` (DB sessions aren't edge-compatible and the proxy reads the token), and `jwt`/`session` callbacks that thread `user.id` onto the session. Added the catch-all handler at `src/app/api/auth/[...nextauth]/route.ts` (`export const { GET, POST } = handlers`), and route protection at `src/proxy.ts` — a **named `export const proxy = auth(...)`** (Next.js 16 renamed Middleware → Proxy) built from the edge-only `authConfig` that redirects unauthenticated visitors on `matcher: ['/dashboard/:path*']` to NextAuth's **default** sign-in page (`/api/auth/signin`, no custom `pages.signIn`), preserving the original URL as `callbackUrl`. Extended `src/types/next-auth.d.ts` with `Session.user.id` + `JWT.id`. Env vars `AUTH_SECRET`/`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` present in `.env` and documented in `.env.example`. `npm run build` clean — proxy registers as `ƒ Proxy (Middleware)`, `/api/auth/[...nextauth]` and `/dashboard` both dynamic (`ƒ`). Browser OAuth round-trip not yet exercised (needs a real GitHub OAuth app with callback `http://localhost:3000/api/auth/callback/github`). Also committed a small housekeeping chore: gitignore + untrack `.mcp.json`.
- **Auth Credentials — Email/Password Provider (Phase 2)** — Added a **Credentials** provider for email/password auth alongside the existing GitHub OAuth, keeping the split-config pattern. `src/auth.config.ts` gains a Credentials **placeholder** (`authorize: () => null`) so the edge/proxy bundle stays free of Prisma/bcrypt; `src/auth.ts` **overrides** it with the real Node-runtime `authorize` — Zod-parses the input (`credentialsSchema`), `findUnique` by email, rejects OAuth-only accounts (no `password` hash), `bcrypt.compare`s, and returns the user (all failure modes return `null` generically per Auth.js guidance). Added the registration API at **`POST /api/auth/register`** — parses JSON, validates with `registerSchema` (name + email + min-8 password + `confirmPassword` match), 409s on an existing email (including catching a Prisma **P2002** unique-constraint race between the check and create), hashes with **bcryptjs at 12 rounds** (matching the seed), creates the user, and returns the `{ success, data | error }` pattern with proper status codes (201/400/409/500). Added `src/lib/validations/auth.ts` with the shared Zod schemas (email normalized via `.trim().toLowerCase()` so register and sign-in lookups always match) and installed **zod** (coding-standard input validation; wasn't a dependency yet). **No migration** — reused the existing `User.password` field. GitHub OAuth and `/dashboard` route protection unchanged (providers list reads `github, credentials`). `tsc --noEmit`, lint, and `npm run build` all clean (`/api/auth/register` is `ƒ`); verified end-to-end against the live Neon **development** branch via curl — registration (valid 201 / duplicate 409 / mismatch + short-password 400), Credentials sign-in (correct password sets a session with `user.id`; wrong password → `error=CredentialsSignin`, no session), the seeded user (`test-engineer@a5project.com` / `12345678`) signs in, `/dashboard` redirects unauthenticated (307 → signin with `callbackUrl`) and serves 200 when authenticated, and a concurrent double-register yields one 201 + one 409. Backend-only (no sign-in/register UI — users hit NextAuth's default `/api/auth/signin`); test rows cleaned up from the dev DB afterward.
- **Auth UI — Sign In, Register & Sign Out (Phase 3)** — Built the custom auth UI on top of the Phase 1/2 backend and connected the sidebar to the real session. Added **`/sign-in`** and **`/register`** under an **`(auth)` route group** with a shared centered `layout.tsx`, and pointed NextAuth at them via **`pages.signIn: "/sign-in"`** in `auth.config.ts` (replacing the default `/api/auth/signin`). **`SignInForm`** (client) does email/password via `signIn("credentials", { redirect: false })` with inline errors + a "Sign in with GitHub" button (`signIn("github")`); **`RegisterForm`** (client) validates name/email/password/confirm with the shared `registerSchema` (Zod), POSTs to `/api/auth/register`, surfaces server field errors (`issues`) and 409s, then redirects to `/sign-in`. **Open-redirect hardening:** `src/proxy.ts` now emits a **relative** `callbackUrl` (`pathname + search`, not the absolute `.href`), and `SignInForm.safeCallbackUrl()` only honors a single-leading-slash relative path (rejecting `//host` and absolute URLs) for both the credentials redirect and the GitHub `callbackUrl` — defaulting to `/dashboard`. Replaced the mock sidebar user with the live session: added **`getCurrentUser()`** (`src/lib/db/user.ts`, `auth()` + Prisma, carrying `isPro` which isn't on the JWT), threaded a `user` prop through `Sidebar`/`SidebarContent` to a new **`SidebarUser`** dropdown (shadcn/Base-UI `dropdown-menu`, opens upward) with **Profile** (→ `/profile`) and **Sign out** (`signOut({ callbackUrl: "/sign-in" })`). Added a reusable **`UserAvatar`** (shadcn/Base-UI `avatar`) that renders the GitHub `image` or an initials fallback via `getInitials` ("Brad Traversy" → "BT"), and a minimal **`/profile`** page (avatar, name, email, plan, sign-out) now also protected by the proxy `matcher` (`/dashboard/:path*`, `/profile/:path*`). Installed shadcn `avatar` + `dropdown-menu`; **inlined the GitHub mark as SVG** since lucide-react v1 dropped brand icons; fixed a Base-UI runtime error by not using `GroupLabel` outside a `Menu.Group`. Also flipped `next.config.ts` `devIndicators: false`. `npm run build` + lint clean (`/sign-in`, `/profile` are `ƒ`; `/register` static); verified end-to-end in a real browser against the live Neon **development** branch — custom sign-in renders, email/password sign-in lands on `/dashboard`, wrong password shows an error, the sidebar avatar shows initials + name + plan/email, the dropdown opens with Profile/Sign out, Profile navigates to `/profile`, Sign out clears the session (then `/dashboard` re-redirects), registration (with client mismatch validation) creates a user and redirects to sign-in, the new user signs in showing "BT", and a malicious `?callbackUrl=https://evil.example.com` is rejected back to `/dashboard`. Test user cleaned up from the dev DB afterward. GitHub OAuth round-trip still not exercised (needs a real GitHub OAuth app).
- **Registration Success Toast** — Added the app's first toast notification: on a successful registration the user now gets a confirmation telling them they can sign in. Built on **Base UI's native toast** (`@base-ui/react/toast`) rather than the shadcn `sonner` registry item, which would have pulled in `sonner` + `next-themes` (the app themes via a static `dark` class on `<html>`, so `next-themes` would be dead weight/conflict) — consistent with how `avatar`/`dropdown-menu` wrap Base UI. Added **`src/lib/toast.ts`** exporting a global `toastManager = Toast.createToastManager()` (lets any client component enqueue via `toastManager.add(...)` without being inside the provider tree), and **`src/components/ui/toast.tsx`** — a styled **`Toaster`** (`Toast.Provider` wired to that manager + `Portal`/`Viewport` bottom-right, and a `ToastList` mapping `useToastManager().toasts` to `Toast.Root`/`Title`/`Description`/`Close`, themed with `bg-popover`/`border-border`, a dismiss `X`, 6s auto-dismiss, and enter/exit animation via the verified `data-starting-style`/`data-ending-style` attributes). Mounted `<Toaster />` **once in the root layout** (`src/app/layout.tsx`, inside `<body>`) so it's globally available. Flow: **`RegisterForm`** now redirects to **`/sign-in?registered=1`** on a 201 (was `/sign-in`); the **sign-in page** reads `registered` from `searchParams` and passes `justRegistered` to **`SignInForm`**, whose mount effect (guarded by a `toastShown` ref so it fires exactly once, StrictMode-safe) calls `toastManager.add({ title: "Account created", description: "You can now sign in with your email and password.", timeout: 6000 })` and then strips the flag via `window.history.replaceState` so a refresh doesn't replay it. Scope kept minimal — success-only; existing inline field/form errors untouched. `npm run build` + lint clean (route table unchanged; `/register` still static, `/sign-in` `ƒ`); verified in a real browser (register → `/sign-in` shows the toast, confirmed by the user). Cleaned up the `toast-test@example.com` verification row (and, on request, the user's `jane@`/`bob@` test rows) from the dev DB afterward.
- **Email Verification on Register** — Required email/password users to verify their address before they can sign in, sending the verification email via **Resend**. Installed the **`resend`** SDK and added **`src/lib/email.ts`** (a `Resend` client + `sendVerificationEmail({ to, verifyUrl })` with an inline HTML template) sending from Resend's shared **`onboarding@resend.dev`** (no domain verification needed), and **`src/lib/base-url.ts`** (`getBaseUrl(request)` — prefers `AUTH_URL`/`NEXTAUTH_URL`, else the request origin) for building absolute links. Reused NextAuth's existing **`VerificationToken`** model (no migration): **`src/lib/db/verification.ts`** exposes `createEmailVerificationToken(email)` (deletes any prior tokens for the email, then stores a `randomBytes(32)` hex token with a **24h** expiry) and `verifyEmailToken(token)` (single-use — deletes the token on consume, checks expiry, sets `User.emailVerified`, and reports `verified` / `already-verified` / `expired` / `invalid`). **`POST /api/auth/register`** now issues a token + sends the email after creating the user (a delivery failure is logged but **non-fatal** — still 201). Added **`GET /api/auth/verify-email`** (consumes the token, redirects to `/sign-in?verified=1` on success/already-verified or `?error=expired|invalid` otherwise) and **`POST /api/auth/resend-verification`** (re-issues + re-sends for a real, password-based, still-unverified account; always returns a **generic** response to avoid account enumeration). **Sign-in gating:** `authorize` in `src/auth.ts` now throws a custom **`EmailNotVerifiedError extends CredentialsSignin`** (`code = "email_not_verified"`) when the password is correct but `emailVerified` is null, so the client's `signIn(...,{redirect:false})` result carries a `code` that distinguishes "verify your email" from "wrong password" (GitHub OAuth path untouched). **`SignInForm`** reads `verified`/`error` (via the sign-in page's `searchParams`) to fire **"Email verified"** / **"Verification failed"** toasts, shows a **"Resend verification email"** button on an unverified sign-in attempt (POSTing the entered email), and updated the registration toast to "check your email". Added an `emailSchema` (email-only) to `src/lib/validations/auth.ts`. **Toast cold-load fix:** the `<Toaster>` provider subscribes to the shared `toastManager` in a **passive** `useEffect`, and Base UI's manager has **no queue** — so on a cold full-page load (arriving from the verify-email redirect) the deep-child `SignInForm` effect fired `toastManager.add()` *before* the root-level provider subscribed and the toast was silently dropped (registration escaped this only as a warm client navigation). Fixed by deferring `add()` to `setTimeout(0)` so it lands after the commit's passive effects flush (no `clearTimeout` cleanup — the `toastShown` ref persists across the StrictMode remount and already guarantees a single schedule). `tsc --noEmit`, lint, and `npm run build` all clean (`/api/auth/verify-email` and `/api/auth/resend-verification` are `ƒ`); verified end-to-end against the live Neon **development** branch — real verification email delivered to `dasig.rg@a5project.com`, the link set `emailVerified` and landed on sign-in, the **"Email verified"** toast rendered (confirmed by the user after the cold-load fix), unverified sign-in blocked with `code=email_not_verified` (wrong password → `code=credentials`), and resend returned identical generic 200s for nonexistent/verified/bad-body inputs. Test rows cleaned up from the dev DB afterward.
- **Toggle Email Verification** — Added an env flag to switch the entire email-verification requirement on or off, so registrants aren't stuck unverified while **Resend has no verified domain** (`onboarding@resend.dev` only delivers to the account owner). Added **`src/lib/features.ts`** → `isEmailVerificationEnabled()` — a plain `process.env.EMAIL_VERIFICATION_ENABLED` read (`=== "true"`, trimmed/lowercased; **defaults off** when unset), kept import-free so it stays edge-safe (though it's only imported from Node-runtime files). Gated four touch points: **`authorize`** in `src/auth.ts` now only throws `EmailNotVerifiedError` when the flag is **on** (`isEmailVerificationEnabled() && !user.emailVerified`); **`POST /api/auth/register`** skips issuing the token + `sendVerificationEmail` when off (computed once into a `verificationEnabled` const) and **leaves `emailVerified` null** — sign-in works purely because the gate is bypassed — while returning a new **`verificationRequired`** field in its 201 body; **`POST /api/auth/resend-verification`** short-circuits to the same generic response when off (indistinguishable, no enumeration signal). **UX correctness:** since the post-register toast previously always said "check your email", `RegisterForm` now reads `verificationRequired` and redirects to **`/sign-in?registered=1`** (verify) or **`?registered=ready`** (no verification); the sign-in page maps those to a new `awaitingVerification` prop on `SignInForm`, which branches the toast copy to **"You can now sign in with your email and password"** when off vs **"Check your email…"** when on (GitHub OAuth path untouched; the "Resend verification" affordance never appears while off since `email_not_verified` is never thrown). **No schema/migration** — reuses `User.emailVerified`. Also **un-ignored `.env.example`** (added `!.env.example` to `.gitignore`) so the flag is documented in version control (real secrets stay in the still-ignored `.env`); default set to `EMAIL_VERIFICATION_ENABLED="false"`. `tsc --noEmit`, lint, and `npm run build` all clean (route table unchanged); verified end-to-end against the live Neon **development** branch with the flag off — register returned `201 verificationRequired:false` with **0** verification tokens and `emailVerified` null, and that unverified account then signed in successfully (302 + valid session with `user.id`). Left the flag off as the safe default until a Resend domain is linked.
- **Forgot Password** — Added self-service password reset for credentials accounts, reusing NextAuth's **`VerificationToken`** model (no schema/migration). Added **`src/lib/db/password-reset.ts`**: `createPasswordResetToken(email)` stores a `randomBytes(32)` hex token under a **namespaced identifier** `password-reset:<email>` (deleting any prior reset token first) with a **1h** expiry — shorter than the 24h verification TTL — and `resetPasswordWithToken(token, hashedPassword)` which looks the token up, **rejects any row whose identifier lacks the `password-reset:` prefix** (so an email-verification token can never reset a password), deletes it single-use (whether valid or expired), checks expiry, then updates `User.password`, reporting `success` / `expired` / `invalid`. Added **`sendPasswordResetEmail({ to, resetUrl })`** to `src/lib/email.ts` (inline HTML template, Resend `onboarding@resend.dev`, "expires in 1 hour" copy) and `forgotPasswordSchema` (email alias) + `resetPasswordSchema` (token + min-8 password + confirm match) to `src/lib/validations/auth.ts`. **`POST /api/auth/forgot-password`** always returns a **generic** response (no account enumeration), issuing a token + emailing `${base}/reset-password?token=…` only for a real, **password-based** account (OAuth-only accounts are a no-op — never silently given a password). **`POST /api/auth/reset-password`** Zod-validates, hashes with **bcryptjs at 12 rounds** (matching register/seed), consumes the token, and returns `{ success }` / a 400 with a `code` (`expired` | `invalid`) the UI turns into a friendly message. UI (both under the `(auth)` group): **`/forgot-password`** (`ForgotPasswordForm` — email → generic "check your email" confirmation) and **`/reset-password`** (`ResetPasswordForm` — reads `token` from `searchParams`, new-password + confirm with shared-schema client validation, and a **missing-token fallback** screen linking to request a fresh link); on success it redirects to **`/sign-in?reset=1`**. **`SignInForm`** gained a **"Forgot password?"** link beside the Password label and a new `justReset` branch firing a **"Password reset"** toast (via the sign-in page's `reset` searchParam), reusing the existing `setTimeout(0)` cold-load-safe toast pattern. **No migration** — reuses `VerificationToken` + `User.password`; the `purge-users` script's `identifier != KEEP_EMAIL` sweep already catches the namespaced reset rows, so no orphans. `tsc --noEmit`, lint, and `npm run build` all clean (`/api/auth/forgot-password` + `/api/auth/reset-password` are `ƒ`, `/reset-password` `ƒ`, `/forgot-password` static); verified end-to-end in a real browser against the live Neon **development** branch — the sign-in "Forgot password?" link opens `/forgot-password`, submitting delivered a real reset email to `dasig.rg@a5project.com`, and the reset flow set a new password and signed in (confirmed by the user). Test user purged from the dev DB afterward via `npm run db:purge-users`.
- **Profile Page** — Built the minimal `/profile` stub (from Auth UI Phase 3) out to the full spec: account info, usage stats, change-password, and delete-account, all as server-component + Prisma data fetching. Added **`src/lib/db/profile.ts`** — `getProfile()` (`auth()` + Prisma; selects `password` only to derive a **`hasPassword`** boolean, never exposing the hash — plus `createdAt`) and `getProfileStats(userId)` (**user-scoped** `item.count` + `collection.count`, and a per-type breakdown via one `item.groupBy(['itemTypeId'])` merged against all 7 system types so each reads a count, 0 when none, ordered per the overview table and pluralized/capitalized for the label). The page (`await connection()`, defensive `redirect("/sign-in")`) renders: **account info** (`UserAvatar` — GitHub image or `getInitials` fallback — name, email, Plan, a new **Member since** via `toLocaleDateString`, and the existing `SignOutButton`); **Usage** (Items/Collections tiles + a **By type** grid using `ItemTypeIcon` with per-type colors/counts); a **Change password** section rendered **only when `hasPassword`** (email/password accounts, never OAuth-only); and a **Delete account** danger zone. Added two API routes following the `{ success, error }` pattern: **`POST /api/auth/change-password`** (session-guarded; Zod `changePasswordSchema`; loads the user, rejects OAuth-only with `code: "no_password"`, `bcrypt.compare`s the current password → `code: "invalid_password"` on mismatch, then rehashes at **bcryptjs 12 rounds** and updates) and **`DELETE /api/auth/account`** (session-guarded; `prisma.user.delete` cascades items/collections/accounts/sessions per the schema's `onDelete: Cascade`). Client components: **`ChangePasswordForm`** (shared-schema client validation, POSTs, inline errors, success toast + field reset) and **`DeleteAccountDialog`** (a new Base UI **`AlertDialog`** wrapper — `src/components/ui/alert-dialog.tsx`, consistent with the avatar/dropdown/toast wrappers — composed via the `render` prop with `Button`; confirm → DELETE → `signOut({ callbackUrl: "/sign-in" })`, staying open with a spinner while pending, toasting on failure). Added `changePasswordSchema` to `src/lib/validations/auth.ts`. **No migration** — reuses `User.password`/`createdAt`. `tsc --noEmit`, lint, and `npm run build` all clean (`/profile`, `/api/auth/change-password`, `/api/auth/account` are `ƒ`); verified end-to-end against the live Neon **development** branch — the seeded user's `/profile` SSR'd all sections with correct counts (18 items = 4/3/0/5/6/0/0 across types, 5 collections), and against a **throwaway** registered user (seed left intact) change-password rejected a wrong current password (400 `invalid_password`) / accepted the correct one (200) with the new password signing in, and delete-account returned 200 authenticated (account then gone — sign-in yields no session) / 401 unauthenticated. The delete **dialog's** open→confirm click wasn't exercised in a real browser (portal-rendered on click; standard Base UI `render`-prop composition, typechecks + builds clean). OAuth-only gating (`hasPassword` hides the section; API returns `no_password`) is covered in code but not runtime-exercised (no real GitHub OAuth app wired up).
- **Rate Limiting for Auth** — Added sliding-window rate limiting to the auth endpoints (brute-force / credential-stuffing / email-flood protection) backed by **Upstash Redis** via **`@upstash/ratelimit`** + **`@upstash/redis`**. Added **`src/lib/rate-limit.ts`** — a reusable utility with a `RATE_LIMITS` registry (per-endpoint limit/window/prefix, typed against Upstash's `Duration` format via `satisfies`), a **lazily-built** Redis client and per-endpoint `Ratelimit` instances (memoized in a `Map`; the `null` "not configured" result is cached too), `checkRateLimit(key, identifier)` → `{ success, remaining, reset, limit }`, IP extraction (`ipFromHeaders`/`getClientIp` — first `x-forwarded-for` entry, then `x-real-ip`, else `"unknown"`), and `rateLimitResponse()` building the **429** with a friendly minutes message + **`Retry-After`** (seconds) header. **Fails open** — a missing Upstash config or a Redis error returns `success: true` (logged), so an outage never locks users out of auth. Wired five endpoints: **register** (3/1h, IP), **forgot-password** (3/1h, IP), **reset-password** (5/15min, IP) checked at the top of each route handler; **resend-verification** (3/15min, IP+email) checked after the email parse (so only active when `EMAIL_VERIFICATION_ENABLED` is on — when off the route short-circuits to its generic response before the check, since there's no email to flood); and **login** (5/15min, IP+email) gated **inside `authorize()` in `src/auth.ts`** (the `/api/auth/callback/credentials` route is owned by NextAuth, so limiting lives there) via `ipFromHeaders(await headers())`, throwing a new **`RateLimitError extends CredentialsSignin`** (`code = "rate_limited"`) that mirrors the existing `EmailNotVerifiedError` so the client's `signIn(...,{redirect:false})` result carries the code. The rate check runs **before** the DB/bcrypt work so every attempt — right or wrong password — counts against the budget. **Frontend toasts:** `SignInForm` handles the `rate_limited` code (login) and a resend **429**; `RegisterForm`/`ForgotPasswordForm`/`ResetPasswordForm` each toast on a **429** (forgot-password no longer shows a false "sent" confirmation when throttled). No enumeration regression — the 429s are keyed on the requester (IP / IP+email), reflecting request volume not account existence. **Import graph stays edge-clean** — `rate-limit.ts` is imported only by `src/auth.ts` + the 4 Node route handlers, never by the edge `auth.config.ts`, so the proxy bundle stays Prisma/Redis-free (build still `ƒ Proxy (Middleware)`). Env vars `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` were already provisioned in `.env` and documented in `.env.example`. `tsc --noEmit`, lint, and `npm run build` all clean (route table unchanged); verified live against Upstash — register **400×3 → 429** (`Retry-After: 964`, "try again in 17 minutes"), login credentials **×5 → `code=rate_limited`** on the 6th/7th via the full CSRF callback flow, and reset-password **400×5 → 429** (independent bucket). Fail-open (env unset) and resend limiting (feature off) are code-covered but not runtime-exercised.
- **Items List View** — Added the dynamic **`/items/[type]`** listing page that resolves the plural slugs the sidebar already links to (`/items/snippets`, `/items/commands`, …) and lists that system type's items; unknown slugs render a **404** via `notFound()`. Added **`getItemsByType(slug)`** to `src/lib/db/items.ts` — maps the plural slug back to the singular stored `ItemType.name` (strips a trailing `s`), `findFirst`s the system type, fetches its items **most-recently-updated first**, and returns an `ItemTypeListing` (`{ type, items }`) reusing the shared `itemSelect` + `toDashboardItem` mapper (so cards get the same preview/relative-time/tags shape as the dashboard); returns `null` for an unmatched slug. **Wrapped in React `cache()`** so a single request shares one DB lookup across `generateMetadata` (page `<title>` = the type label) and the page body instead of querying twice. Extracted **`typeLabel`/`typeSlug`** helpers (singular→"Snippets"/"snippets") and reused them in `getSidebarItemTypes` to avoid drift. The page (`src/app/items/[type]/page.tsx`) is a server component that **mirrors the dashboard shell** (`SidebarProvider` + `Sidebar` + `TopBar`, fetching sidebar item-types/collections/user in the same `Promise.all`), calls `await connection()` for request-time dynamic render, and shows a **responsive `ItemCard` grid — `grid-cols-1` → `md:grid-cols-2` → `lg:grid-cols-3` → `xl:grid-cols-4`** (each card's left border colored by item type, unchanged) with a dashed **empty-state** panel ("No <type> yet.") when the type has none. **Route protection:** added **`/items/:path*`** to the `src/proxy.ts` matcher (alongside `/dashboard`, `/profile`) so unauthenticated visitors redirect to `/sign-in` with a relative `callbackUrl`. **No schema/migration** (query + page only), no new deps. `tsc --noEmit`, lint, and `npm run build` all clean (`/items/[type]` is `ƒ`, `ƒ Proxy (Middleware)` intact); verified against the running dev server on the live Neon **development** branch — authenticated, `/items/snippets` SSR'd the 4 seeded snippet cards with title "Snippets" and the `md:2/lg:3/xl:4` grid classes, `/items/notes` rendered the empty state (count 0), valid types (`snippets`/`commands`/`links`/`prompts`) returned 200 and `/items/bogus` 404; unauthenticated, `/items/snippets` and `/items/bogus` both **307 → `/sign-in?callbackUrl=%2Fitems%2F…`**.
- **Vitest Unit Testing Setup** — Set up **Vitest** as the unit test runner, deliberately scoped to **server actions (`src/actions/*`) and utilities (`src/lib/*`) only** — components, pages, and layouts are out of scope (cover with E2E later). Installed **`vitest`** as the sole new dev dep — **no** `jsdom`/`@testing-library`/`@vitejs/plugin-react` since we don't render components. Added **`vitest.config.mts`** with a **`node`** environment, `include: ["src/**/*.test.ts"]`, and **`resolve.tsconfigPaths: true`** to resolve the `@/*` alias natively from `tsconfig.json` (initially used the `vite-tsconfig-paths` plugin, but Vitest 4 / Vite emitted a deprecation notice that native resolution supersedes it, so the plugin was uninstalled). Tests import helpers explicitly from `vitest` (`describe`/`it`/`expect`) rather than enabling globals, so `tsconfig` needs no `types` edit. Added npm scripts **`test`** (`vitest run` — single pass for the workflow/CI) and **`test:watch`** (`vitest`). Seeded **15 example tests across 3 files** for existing pure, exported utilities: `src/lib/features.test.ts` (`isEmailVerificationEnabled` env parsing — default-off, `"true"`, case/whitespace, non-true values, restoring `process.env` in `afterEach`), `src/lib/base-url.test.ts` (`getBaseUrl` — AUTH_URL precedence + trailing-slash strip, NEXTAUTH_URL fallback, dev request-origin fallback, prod Host-header throw, via `vi.stubEnv`/`vi.unstubAllEnvs`), and `src/lib/validations/auth.test.ts` (the Zod `credentialsSchema`/`registerSchema`/`resetPasswordSchema` — email normalization, min-length, confirm-match `path`). Writing the base-url test surfaced a real behavioral nuance (documented by the test): the `NEXTAUTH_URL` fallback only fires when `AUTH_URL` is truly **unset**, not empty-string, because the code uses `??` (so the fallback test passes `undefined` to `vi.stubEnv`, not `""`). **Docs updated:** `CLAUDE.md` Commands section (replaced "No test runner is configured yet" with the `test`/`test:watch` commands + scope note) and `context/ai-interaction.md` (Workflow **step 4** now requires adding/updating Vitest tests for new/changed actions & utilities and running `npm run test`; step 6 gated on tests+build; added a new **Testing** section covering scope, colocation, no-DB/network rule, explicit imports, and commands). No schema/migration, no `src/actions/` yet (so example tests cover utilities only — ready for actions when added). `npm run test` (15/15), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged).
- **Item Drawer** — Added a right-side slide-in **drawer** as the item detail view (no separate item page); clicking any item card opens it on both the **dashboard** and the **`/items/[type]`** list. **Data layer:** `getItemDetail(id, userId)` in `src/lib/db/items.ts` — an **owner-scoped** (`findFirst where { id, userId }`, guarding IDOR) full-detail query returning `ItemDetail` (content, language, url/file fields, tags flattened, the collections it belongs to each with an accent `color` = the collection's `defaultType.color` or a gray `#6b7280` fallback, formatted `createdAt` via `toLocaleDateString`, relative `updatedAt`). **API:** `GET /api/items/[id]` (`src/app/api/items/[id]/route.ts`) — session-guarded, **401** unauth, **404** for a missing/foreign id (no existence leak), `{ success, data }` otherwise; deliberately **not** added to the proxy matcher so it returns JSON rather than an HTML redirect. **UI:** `src/components/ui/sheet.tsx` — a shadcn-style **Sheet** built on **Base UI `Dialog`** (slides from the right, `showClose` toggle, `side` prop) rather than the real shadcn/Radix sheet, to preserve the codebase's deliberate Base-UI-wrapper pattern (avatar/dropdown/alert-dialog/toast) and avoid pulling in `@radix-ui/react-dialog`. `ItemDrawerProvider` (`ItemDrawer.tsx`, client) holds open/loading/error state, exposes `openItem(id)` via `item-drawer-context.tsx`, and **fetches detail on click** (no navigation) with a **race-safe `requestId` ref** (a slower earlier fetch can't clobber a newer one) and a `sr-only` `SheetTitle` in every state for a11y; `ItemCardButton` wraps the presentational `ItemCard` in a focusable button that calls `openItem`. `ItemDrawerDetail` renders the type header + **action bar** (Pin, Favorite [star, yellow when active], Edit, Delete, Close), a **working Copy** (client clipboard), meta pills (updated/created), the content box (code/text `pre`, or an external link for URL items), tags, and an **In collections** list with colored dots; plus an `ItemDrawerSkeleton` loading placeholder. **Scope:** display-only — the Pin/Favorite/Edit/Delete **mutations are not wired yet** (deferred), and the code editor / type-specific rendering come later. Both pages wrap their shell in `ItemDrawerProvider` and swap `ItemCard` → `ItemCardButton`; drawer horizontal padding is `px-8` per feedback. **Testing:** added `src/lib/db/items.test.ts` — 5 Vitest cases for `getItemDetail` (Prisma mocked via `vi.hoisted`/`vi.mock`, no DB): null-on-miss, owner-scoping (`where { id, userId }`), shape mapping (tag flatten, passthrough, formatted date), collection color from `defaultType`, and the gray fallback. **No schema/migration**, no new deps. `npm run test` (20/20), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/api/items/[id]` is `ƒ`, both pages `ƒ`, `ƒ Proxy (Middleware)` intact); verified against the running dev server on the live Neon **development** branch — API **401** unauth → credentials login → **200** with full `useDebounce hook` detail (content, `typescript`, tags, React Patterns collection `#3b82f6`, pinned) → **404** for a bogus id; `/dashboard` and `/items/snippets` both SSR **200** with clickable `aria-label="Open …"` card buttons. The actual click→drawer-open **visual was not verified in a real browser** (no headless browser available in the environment; the requested `dasig.rg@a5project.com` account is passwordless/OAuth-only so the seeded `test-engineer@a5project.com` credentials were used for the API checks).
- **Item Drawer — Edit Mode** — Wired the drawer's **Edit** (pencil) button to flip the same drawer from view mode into an **inline edit mode**: the action bar is replaced with **Save / Cancel**, fields become controlled inputs. Cancel discards back to view mode; Save persists via a server action, returns to view mode, refreshes the underlying card list (`router.refresh()`), and re-renders the drawer from the returned detail (**no second fetch**). **Validation:** `src/lib/validations/items.ts` — `updateItemSchema` (`title` required/trimmed; `description`/`language` optional-nullable via an `emptyToNull` preprocess + `min(1)`; `content` optional-nullable but **not trimmed** so code whitespace survives; `url` a valid-URL-or-null; `tags` an array transformed to trimmed, non-empty, **de-duplicated** names) — the server-side source of truth. **Query:** owner-scoped `updateItem(id, userId, data)` in `src/lib/db/items.ts` — a `findFirst { id, userId }` ownership check (guards IDOR; `item.update` keys off the unique id alone so it can't scope by userId), writes **only defined fields** (an undefined field is left as-is, null clears it), **replaces tags** via `set: []` + `connectOrCreate`, and returns the refreshed `ItemDetail` by reusing `getItemDetail`. **Server action** (first `src/actions/*` in the repo): `updateItem(itemId, input)` in `src/actions/items.ts` — `"use server"`, `auth()` guard, Zod `safeParse` (returns `issues` via `z.flattenError`), delegates to the query, maps null→"Item not found.", try/catch→generic error, `{ success, data | error }` (`ActionResult<T>`). **UI:** new `ItemDrawerEdit.tsx` (client) with **type-gated fields** — Content for snippet/prompt/command/note, Language for snippet/command, URL for link; all types get Title/Description/Tags — plus **Save disabled while the title is empty** (client UX guard), a success/error **toast**, and `useRouter().refresh()`. Added a shadcn-style **`Textarea`** (`src/components/ui/textarea.tsx`, matching the `Input` classes). The Edit button in `ItemDrawerDetail` now takes an `onEdit` prop; `ItemDrawer` (provider) holds an `editing` state, resets it on open/close, and swaps `ItemDrawerDetail` ↔ `ItemDrawerEdit` (keyed by `item.id`), updating `item` from the saved detail. **Testing:** **+19 Vitest cases** — `src/lib/validations/items.test.ts` (title/tags/url/blank-coercion/content-whitespace), extended `src/lib/db/items.test.ts` (updateItem: ownership null-return + no write, `where` scoping, `set:[]`+`connectOrCreate` shape, null-clear, refreshed return), and `src/actions/items.test.ts` (unauth, validation issues, parsed-data passthrough, not-found, success, thrown→generic). **No schema/migration**, no new deps. `npm run test` (**39/39**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/api/items/[id]` and both pages still `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — snippet edit rendered Title/Description/Content/Language/Tags, Save persisted a new title + added `#clsx` tag and re-sorted the card list to "just now"; a link's edit form showed **URL** (no Content/Language); empty title disabled Save; Cancel discarded changes with the original title intact. Re-seeded the dev DB afterward to restore the clean demo dataset. Also reverted a live Context7 key the harness had written into the tracked `.mcp.json` (kept the redacted `${CONTEXT7_API_KEY}` placeholder).
- **Item Delete** — Wired the drawer's **Delete** (trash) button — previously display-only — to actually delete an item, behind a confirmation dialog with a success toast. **Query:** owner-scoped `deleteItem(id, userId)` in `src/lib/db/items.ts` — a single atomic **`prisma.item.deleteMany({ where: { id, userId } })`** (returns `boolean` from `count > 0`). Chose `deleteMany` over the spec-noted `findFirst`-then-`item.delete`: it's atomic (no TOCTOU window between the ownership check and the delete), one round-trip, and can't delete by unique id alone — same IDOR guard, strictly better. Cascade deletes remove the item's `ItemCollection` links (`onDelete: Cascade`) and its implicit tag-join rows; global `Tag` rows are intentionally left (reusable, matching `updateItem`'s connect-without-prune behavior). **Server action:** `deleteItem(itemId)` in `src/actions/items.ts` — `"use server"`, `auth()` guard, delegates to the query, maps false→"Item not found.", try/catch→generic error, `{ success, data: { id } | error }` (`ActionResult<T>`). **UI:** new **`DeleteItemButton.tsx`** (client) wraps the trash icon in the existing Base UI **`AlertDialog`** (the same wrapper the profile Delete-account danger zone uses) with the item title interpolated into the confirmation copy; on confirm → server action → **success toast** ("Item deleted") → **`router.refresh()`** → **`onDeleted()`** closes the drawer; error/exception paths close the dialog and toast a failure. The dialog stays open with a spinner while pending (`Loader2`). Wired into `ItemDrawerDetail` (replaced the display-only `Trash2` button, added an `onDeleted` prop, dropped the now-unused `Trash2` import) and `ItemDrawer` (passes `onDeleted={() => setOpen(false)}`). **Testing:** **+8 Vitest cases** — `src/lib/db/items.test.ts` (`deleteItem`: id+owner `where` scoping, `count>0`→true, `count:0`→false) and `src/actions/items.test.ts` (unauth without DB touch, id+owner passthrough, false→not-found, success→`{ id }`, thrown→generic). **No schema/migration**, no new deps (AlertDialog + toast already installed). `npm run test` (**47/47**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — opened a command's drawer, clicked Delete, the **"Delete this item?"** confirmation rendered with the title interpolated, confirming closed the drawer + fired the **"Item deleted"** toast + removed the card via `router.refresh()` (All Commands 5→4, sidebar Commands 5→4 and All items 18→17). Re-seeded the dev DB afterward to restore the clean 18-item demo dataset.
- **Item Create** — Added item creation via a modal **Dialog** opened from the top bar's **New Item** button, supporting the 5 text/url system types (file/image are Pro/out of scope). **Validation:** `createItemSchema` in `src/lib/validations/items.ts` — a `type` enum (`CREATABLE_ITEM_TYPES` = snippet/prompt/command/note/link, exported for the UI), required trimmed `title`, the same `emptyToNull`-preprocessed optional-nullable `description`/`language`/`url` (valid-URL) and **untrimmed** `content` as `updateItemSchema`, tags trimmed/de-duplicated, plus a **`.refine`** enforcing **URL required when `type === "link"`** (`path: ["url"]`). **Query:** `createItem(userId, data)` in `src/lib/db/items.ts` — resolves the **system** `ItemType` by name (`findFirst { isSystem: true, name }`, returns null if unresolved), derives `contentType` from a `CONTENT_TYPE_BY_TYPE` map (link→URL, else TEXT), creates the item with tags via `connectOrCreate`, and returns the created row's `ItemDetail` by reusing `getItemDetail` (so the caller gets the full shape without a second query). **Server action:** `createItem(input)` in `src/actions/items.ts` — `"use server"`, `auth()` guard, Zod `safeParse` (returns `issues` via `z.flattenError`), delegates to the query, maps a null (unresolved type) to a generic error, try/catch→generic error, `{ success, data | error }` (`ActionResult<ItemDetail>`). **UI:** new **`CreateItemDialog.tsx`** (client) with a 5-type selector and **type-gated fields** (Content for snippet/prompt/command/note, Language for snippet/command, URL for link; all types get Title/Description/Tags), **Save disabled while the title is empty** (client UX guard), success/error **toast** (`toastManager`), and **`router.refresh()`** + close on success; built on a new shadcn-style **`src/components/ui/dialog.tsx`** wrapping **Base UI `Dialog`** (centered modal) — consistent with the deliberate Base-UI-wrapper convention (`sheet.tsx`/`alert-dialog.tsx`/avatar/dropdown/toast) rather than pulling in Radix. Wired into `TopBar.tsx` (the New Item button now opens the dialog). **Testing:** **+17 Vitest cases** — `src/lib/validations/items.test.ts` (createItemSchema: link URL-required refine, type enum, tag/blank coercion, content whitespace), `src/lib/db/items.test.ts` (createItem: unresolved-type null-return, contentType-by-type mapping, `connectOrCreate` tag shape, getItemDetail re-read), and `src/actions/items.test.ts` (unauth, validation issues, parsed-data passthrough, null→generic, success, thrown→generic). **No schema/migration**, no new deps. `npm run test` (**64/64**), and `npm run build` clean (route table unchanged, `ƒ Proxy (Middleware)` intact).
- **Code Editor (Monaco)** — Added a **Monaco**-backed code editor for **snippet** and **command** content, replacing the plain `Textarea`/`pre` for those two types only (note/prompt/link keep the textarea). Installed **`@monaco-editor/react`** + **`monaco-editor`** (loaded from the **jsDelivr CDN pinned to `0.55.1`** via `loader.config`, so nothing heavy is bundled and no Turbopack/worker config is needed). New **`src/components/dashboard/CodeEditor.tsx`** (client): a header with **macOS window dots** (red/yellow/green, `aria-hidden`) on the left and the **language label + Copy button** on the right, a custom **`devstash-dark`** Monaco theme (`beforeMount` — app-matched `#1c1c1c` background, translucent-white **themed scrollbar** via `scrollbarSlider.*`), **fluid height** driven by `onDidContentSizeChange` and **capped at 400px** (`min(400, max(64, contentHeight))`, `scrollBeyondLastLine:false` so it hugs the content), and both **readonly** (display) and **editable** (`onChange`) modes. Editor options keep it snug for a snippet store: minimap off, `alwaysConsumeMouseWheel:false` (page still scrolls past a non-scrolling editor), folding/overview-ruler/sticky-scroll off, and **TS/JS diagnostics disabled** (`setDiagnosticsOptions` noSemantic/noSyntax) so fragment snippets don't get false "cannot find module" squiggles. Extracted the type/language logic into a pure, dependency-free **`src/lib/code-types.ts`** — `isCodeType` (snippet/command), `monacoLanguage` (alias map: bash→shell, ts→typescript, …; unknown pass through lowercased; blank→plaintext), and `editorLanguageLabel` (language, else capitalized type name) — reused across all three call sites so the gating never drifts. Wired into **`ItemDrawerDetail.tsx`** (readonly, replaces the boxed `pre` for code types; non-code/URL branch unchanged), **`ItemDrawerEdit.tsx`**, and **`CreateItemDialog.tsx`** (editable; the Language field updates highlighting live). **Testing:** **+9 Vitest cases** — `src/lib/code-types.test.ts` (`isCodeType` case/whitespace, `monacoLanguage` aliases + passthrough + plaintext fallback, `editorLanguageLabel` language-vs-typename). **No schema/migration.** `npm run test` (**73/73**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — readonly `useDebounce` snippet rendered dots + `typescript` label + Copy + 12 numbered lines + syntax highlighting; the 19-line Dockerfile rendered `dockerfile` highlighting with no squiggles; edit mode swapped Content to the editable editor (Language field driving highlighting); the create dialog showed the editor for Snippet and a plain Textarea for Prompt. **Caveats:** Monaco loads from CDN (won't render offline / under a strict CSP — the app sets neither), and the **400px cap + scroll** is deterministic in code but wasn't visually exercised (no seeded snippet exceeds ~19 lines).
- **Markdown Editor** — Added a tabbed **Write/Preview** Markdown editor for **note** and **prompt** content, replacing the plain `Textarea`/`pre` for those two types only (snippet/command keep the Monaco `CodeEditor`; link unchanged). Installed **`react-markdown@10`** + **`remark-gfm@4`** (Context7 was unauthenticated, so wired against the stable v9/v10 API — React 19 compatible; verified in-browser). New **`src/components/dashboard/MarkdownEditor.tsx`** (client): mirrors `CodeEditor`'s framing — a dark container (**`bg-[#1e1e1e]`**) with a header (**`bg-[#2d2d2d]`**) carrying an internal `TabButton` pill row (Write/Preview) on the left and a **Copy button** (same `ghost`/`size="xs"` style as `CodeEditor`) on the right. **Write** is an auto-growing `<textarea>` (a `useLayoutEffect` sets height to `scrollHeight` clamped to **`min 120 / max 400`px**, then it scrolls); **Preview** renders `Markdown` with `remarkPlugins={[remarkGfm]}` inside a `.markdown-preview` wrapper (also `max-h-[400px]` scroll), showing a "Nothing to preview." fallback when blank. **Readonly** hides the Write tab entirely (`activeTab` forced to `"preview"`); **edit** defaults to Write with Preview available. Extracted the type gate into the pure **`src/lib/code-types.ts`** — `MARKDOWN_ITEM_TYPES` (note/prompt) + **`isMarkdownType`**, mirroring `isCodeType` and reused across all three call sites so gating can't drift (the two sets are proven disjoint by a test). **Styling:** ~146 lines of explicit `.markdown-preview` rules in **`src/app/globals.css`** (plain CSS, not utilities, for reliable rendering on the dark surface) — h1–h6 with distinct sizes (h1/h2 get a bottom border), inline code (`rgba(255,255,255,.1)` bg), fenced code blocks (`#141414`), ordered/unordered/nested lists, blockquotes (left-border accent), blue links (`#60a5fa` + hover underline), and bordered tables with a header background. Wired the three-way Content branch (**code → `CodeEditor`**, **note/prompt → `MarkdownEditor`**, **else → textarea/`pre`/link**) into **`ItemDrawerDetail.tsx`** (readonly), **`ItemDrawerEdit.tsx`**, and **`CreateItemDialog.tsx`** (editable via `onChange={setContent}`); content stays a plain string end-to-end, so validation / the server action / the DB layer were untouched. **Security:** react-markdown renders no raw HTML by default (no `rehype-raw`) and sanitizes URLs via its built-in `defaultUrlTransform`, so user Markdown can't inject scripts or `javascript:` links. **Testing:** **+4 Vitest cases** — `src/lib/code-types.test.ts` (`isMarkdownType` note/prompt true, snippet/command/link false, case/whitespace-insensitive, mutually exclusive with `isCodeType`). **No schema/migration.** `npm run test` (**77/77**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — a readonly prompt drawer showed the **Preview-only** tab + Copy; edit mode rendered rich GFM (H1/H2 with borders, bold/italic/inline code, nested bullets, ordered list, blockquote, fenced code block, table, task list) with the 400px scroll cap; the New Item dialog swapped Content to the `MarkdownEditor` for **Note** (no Language field) while **Snippet** kept the Monaco editor. No data persisted (edit cancelled, create title empty). **Caveat:** the `.markdown-preview` link/code colors are hardcoded for the always-dark editor surface (consistent with `CodeEditor`'s hardcoded dark bg); if light mode is ever built, both editors need revisiting together.
- **File & Image Upload (Cloudflare R2)** — Added upload/download for the two Pro system types (**File**, **Image**) backed by **Cloudflare R2** via its S3-compatible API. **No migration** — `fileUrl`/`fileName`/`fileSize` already exist on `Item` (storage shape `ContentType.FILE`). **R2 client:** new **`src/lib/r2.ts`** (installed **`@aws-sdk/client-s3`**) — a **lazily-built** `S3Client` pointed at `https://<accountId>.r2.cloudflarestorage.com` so importing the module is cheap, plus `uploadToR2`/`deleteFromR2`/`getFromR2` (the getter returns `Body.transformToWebStream()` for the proxy), an `isR2Configured()` guard, and pure helpers `buildObjectKey(userId, name)` (namespaced `items/<userId>/<uuid>-<sanitized-name>.<ext>`), `r2PublicUrl(key)` (via `R2_PUBLIC_URL`), and `r2KeyFromUrl(url)` (pathname → key, for delete/download). **Validation:** new pure **`src/lib/validations/upload.ts`** — per-kind constraints (images **5 MB**: png/jpg/jpeg/gif/webp/svg; files **10 MB**: pdf/txt/md/json/yaml/yml/xml/csv/toml/ini) with `validateUpload` (extension + size **hard** gates; non-empty MIME checked against the allow-list, empty tolerated since browsers omit it for text files), `fileExtension`, `acceptAttribute`, `formatBytes`, and `isUploadKind` — shared by the client (UX pre-check) and the upload route (source of truth). **Upload route:** **`POST /api/items/upload`** (session-guarded; **503** when R2 unconfigured) parses `formData`, re-validates, buffers the file (small, ≤10 MB), `PutObject`s to R2, and returns `{ fileUrl, fileName, fileSize }`. **Download proxy:** **`GET /api/items/[id]/download`** — owner-scoped (via a new lean `getItemFile(id, userId)` in `lib/db/items.ts`), streams the R2 object **same-origin** with `Content-Type` passthrough + `Content-Disposition: attachment` (RFC 5987 filename), so downloads work without CORS and access stays scoped. Neither route is in the proxy matcher, so they return JSON **401** (not an HTML redirect) when unauthenticated. **DB (kept in `lib/db/items.ts` per spec):** `createItem` widened to persist `fileUrl/fileName/fileSize` and map file/image → `FILE` (`CONTENT_TYPE_BY_TYPE`); `deleteItem` now reads the item's `fileUrl` first, deletes the row (`deleteMany` id+userId), then **best-effort** deletes the R2 object (a storage error is logged, never fails the delete); `getItemDetail`/`ItemDetail` gained `fileUrl`. **Validation schema:** `createItemSchema` gained `file`/`image` to its `type` enum (new `NEW_ITEM_TYPES = CREATABLE + FILE_ITEM_TYPES`), optional `fileUrl`/`fileName`/`fileSize`, and a **`.superRefine`** requiring all three for file/image items (flagged on their own paths). **UI:** new **`FileUpload.tsx`** (client) — drag-and-drop dropzone + hidden input with client-side `validateUpload`, uploads via **`XMLHttpRequest`** for real **upload-progress** (`xhr.upload.onprogress` → a progress bar), then shows an **image thumbnail** (images, via the public `fileUrl`) or a **file-info row** (icon + name + `formatBytes`) with a remove control; exposes `onUploadingChange` so the dialog can disable submit mid-upload. **`CreateItemDialog`** gained File/Image type chips, renders `FileUpload` for those types (hiding Content/Language/URL), **auto-fills the title from the filename** (sans extension) when blank, and gates Create on title + file present + not uploading; switching type clears the file. **`ItemDrawerDetail`** renders a FILE branch — **image preview** (`<img>` for image types) or a **file-info card**, plus a file meta line and a new **`DownloadFileButton`** (anchor to the proxy, styled via `buttonVariants`). Edit mode degrades gracefully for file items (Title/Description/Tags only; the file is kept). Env vars `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL` provisioned in `.env` and documented in `.env.example`. **Testing:** **+30 Vitest cases** (77 → **104**) — `upload.test.ts` (validate/extension/size/MIME, formatBytes, isUploadKind, acceptAttribute), `r2.test.ts` (r2KeyFromUrl, r2PublicUrl env, buildObjectKey uniqueness/sanitization), extended `items.test.ts` (createItem file-metadata + FILE mapping, deleteItem new lookup+R2-cleanup+best-effort, getItemFile owner-scoping/defaults) and `items` validation (file/image field-required refine). **`npm run test` (104/104)**, `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/api/items/upload` + `/api/items/[id]/download` are `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch + live R2 — image upload rendered a thumbnail (from the R2 public URL) + auto-filled title, created an item on `/items/images`; its drawer showed the **preview + Download**, and the proxy returned **`200 image/png`, `Content-Disposition: attachment; filename="sample.png"`, 70 bytes** streamed from R2; a File upload showed the **file-info card** (no preview) + Download; delete removed both items **and** their R2 objects (no cleanup errors logged); unauthenticated `POST /api/items/upload` and `GET …/download` both returned **401**. Test items + uploads cleaned up afterward (DB back to the clean 18-item seed). **Caveat:** the upload buffers the whole file in memory (fine at ≤10 MB) and there's no orphan-cleanup for a file uploaded then abandoned before Create (the object lingers in R2 until a matching item is deleted).
- **Image Gallery View** — Replaced the standard item-card grid on the **`/items/images`** listing with a **thumbnail gallery**; every other item type keeps the existing `ItemCard` grid. Added **`src/components/dashboard/ImageCard.tsx`** (presentational) — a card whose thumbnail is a **16:9 `aspect-video`** frame with **`object-cover`** fill (edges may crop), wrapped in `overflow-hidden` so a **subtle hover zoom** (`group-hover:scale-105` + `transition-transform duration-300`) stays clipped; a truncated title caption sits below, and a placeholder `ImageIcon` renders when `fileUrl` is missing. Added **`src/components/dashboard/ImageCardButton.tsx`** (client) mirroring `ItemCardButton` — a focusable button that is the hover `group` and opens the **existing item drawer** via `openItem(item.id)` (no navigation, drawer/detail/download unchanged). Wired the split into **`src/app/items/[type]/page.tsx`**: computed `isImageGallery = listing.type.slug === "images"` and branch the grid — images use **`grid-cols-2 md:grid-cols-3 lg:grid-cols-4`** with `ImageCardButton`, all other types keep the original **`grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`** `ItemCardButton` grid verbatim (empty-state panel unchanged). To source the thumbnail, added **`fileUrl`** to the shared `DashboardItem` shape, the `itemSelect` projection, the `RawItem` type, and the `toDashboardItem` mapper in **`src/lib/db/items.ts`** (null for text/url items; the R2 public URL for file/image) — a passthrough field with no branching logic, so no new Vitest cases (components/pages are out of test scope). **No schema/migration**, no new deps. `npm run test` (**104/104**), `npm run lint`, and `npm run build` all clean (route table unchanged, `/items/[type]` still `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch + live R2 — uploaded three test PNGs of varied aspect ratios (square/wide/tall) and confirmed the gallery cropped all to 16:9 via `object-cover`, rendered **4 columns at the large breakpoint** and **3 columns at medium (900px)**, and that clicking a card opened the existing drawer with the full image + metadata + Download; the three test images (and their R2 objects) were deleted afterward via the UI, restoring the dev DB to its prior state.
- **File List View** — Replaced the standard item-card grid on the **`/items/files`** listing with a **single-column list** (Google Drive/Dropbox style); every other item type keeps its `ItemCard` grid and **`/items/images`** keeps its gallery. Added **`src/components/dashboard/FileListRow.tsx`** (client) — a `role="button"` row (rather than a real `<button>`, so the nested download `<a>` stays valid HTML; keyboard activation wired via `onKeyDown` for Enter/Space) that opens the **existing item drawer** via `openItem(item.id)` (no navigation, drawer/detail unchanged). Each row shows a **file icon (by extension)**, the file name (`fileName ?? title`, truncated), the file size (`formatBytes`), the upload date (the item's relative `updatedAt`), and a **download control** — an anchor to the owner-scoped **`/api/items/[id]/download`** proxy that **`stopPropagation`s** so it doesn't also open the drawer. **Hover highlight** (`hover:bg-muted/50`); the info **stacks vertically on mobile** and collapses to a single row from `sm` up. Wired the split into **`src/app/items/[type]/page.tsx`**: added `isFileList = listing.type.slug === "files"` mirroring the existing `isImageGallery` branch — files render a `flex flex-col` list of `FileListRow`, all other types keep their grids verbatim (empty-state panel unchanged). Added a pure, testable **`src/lib/file-icons.ts`** → `fileIconCategory(fileName)` (maps an extension to `pdf`/`image`/`code`/`spreadsheet`/`text`/`generic` via the shared `fileExtension` helper; unknown → `generic`), and `FileListRow` maps that category to a lucide icon (`FileText`/`FileImage`/`FileCode`/`FileSpreadsheet`/`File`). Extended the shared `DashboardItem` shape with **`fileName`/`fileSize`** (only `fileUrl` was added for the gallery), threading them through `itemSelect`, `RawItem`, and `toDashboardItem` in **`src/lib/db/items.ts`** (null for text/url items). **Testing:** **+7 Vitest cases** — `src/lib/file-icons.test.ts` (`fileIconCategory`: pdf/image/code/spreadsheet/text mappings, case-insensitivity, generic fallback for unknown/missing extensions). **No schema/migration**, no new deps. `npm run test` (**111/111**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `/items/[type]` still `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch + live R2 — `/items/files` SSR'd the list with a seeded PDF row (`FileText` icon, `Solar PV System Quotation.pdf`, `125 KB`, `32m ago`, download link to the correct proxy URL); clicking the row opened the item drawer (file info card + Download); at 390px the row stacked the name/size/date vertically. The download button's `stopPropagation` is covered in code (not separately click-exercised to avoid triggering a real download). One throwaway File item remains in the dev DB from prior upload testing (All items reads 21 rather than the clean 18-item seed).
- **Owner-Scoped Items & Collections (fix)** — Closed a live multi-tenant data leak: the read queries behind the **dashboard**, the **sidebar**, and **`/items/[type]`** carried **no `userId` filter**, so every signed-in user saw **every other user's** items and collections (stats tiles, collection grid, pinned/recent grids, sidebar type counts + collection list, and the per-type listing). The detail/write paths (`getItemDetail`, `updateItem`, `deleteItem`, `getItemFile`) were already owner-scoped via `findFirst { id, userId }`, so the leak was confined to the list/stat reads. Fixed **8 functions** by giving each a `userId` parameter filtered in its `where` — matching the existing `getItemDetail(id, userId)` / `getProfileStats(userId)` convention rather than reaching for the session inside the data layer: **`src/lib/db/items.ts`** (`getPinnedItems`, `getRecentItems`, `getItemStats`, `getSidebarItemTypes`, `getItemsByType`) and **`src/lib/db/collections.ts`** (`getRecentCollections`, `getCollectionStats`, `getSidebarCollections`). **System `ItemType` rows are global** (`isSystem: true`, no owner), so type *resolution* stays unscoped and only the sidebar's per-type tally is filtered — via a **filtered relation count** (`_count: { select: { items: { where: { userId } } } }`), which filters the count without dropping the type rows (scoping the types themselves would blank the sidebar). Split out a cached **`getSystemItemType(slug)`** so `generateMetadata` can resolve the page title from the label alone instead of threading a `userId` into metadata generation (and so the page no longer runs the item query twice per request); `getItemsByType(slug, userId)` now delegates to it, and a **valid slug the user owns nothing for renders the empty state, not a 404** (a 404 there would leak whether the type exists). Both pages now resolve the session user **before** fetching and **`redirect("/sign-in")`** when absent, **removing the `sidebarUser ?? { guest }` fallbacks** — with scoped queries a null user has no renderable page, and a silent guest fallback is the exact shape of defect that let the leak survive review. `getTypeCountsByCollection` needs no filter of its own (callers only pass already-scoped collection ids); that invariant is now documented at the call boundary. **No schema/migration**, no new deps. **Testing:** **+13 Vitest cases** (111 → **124**) — a new `src/lib/db/collections.test.ts` (owner-scoped `where`, limit/`orderBy` favorites-first, tally-derived counts + most-used-first types, null color when empty, no raw query when the user has no collections) and an extended `src/lib/db/items.test.ts` (each read passes `userId`; `getSidebarItemTypes` filters the **relation count** and not the `isSystem` type rows; `getItemsByType` null-on-unknown-slug without querying items, and **empty listing ≠ null** when the user owns none). `npm run test` (**124/124**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP) against the live Neon **development** branch — the seeded `test-engineer@a5project.com` still saw its own 21 items / 5 collections with correct sidebar counts (4/3/1/5/6/1/1), while a **freshly registered second user** saw **0 everywhere** (stats, collections, recent items, and all seven sidebar type counts), `/items/snippets` rendered "No snippets yet." while the owner had 4, `GET /api/items/<owner's item id>` returned **404** for the second user, and `/items/bogus` still 404'd. **Caveats:** `getItemDetail`'s "In collections" list joins `ItemCollection` without filtering the *collection* side by owner — not exploitable today (the item itself is owner-scoped and nothing creates cross-user links), but the **add-to-collection feature must scope both sides of the link on write**. Cleanup note: `npm run db:purge-users` keeps only `test-engineer@a5project.com`, so removing the throwaway test account also deleted the empty `dasig.rg@a5project.com` account (0 items / 0 collections; re-creatable via register or GitHub OAuth).
- **Quick Copy on Item Cards** — Added a one-click **copy icon** to every `ItemCard`, so an item's content can be grabbed straight from the dashboard's Pinned/Recent grids and the `/items/[type]` listing without opening the drawer. New **`src/components/dashboard/CopyButton.tsx`** (client) — an icon-only `ghost`/`icon-xs` `Button` (the Base UI wrapper, matching `ItemDrawerDetail`) that writes to `navigator.clipboard`, swaps `Copy` → a green `Check` for **2s** (with a `useEffect` cleanup clearing the pending `setTimeout` on unmount), flips its `aria-label` to "Copied", and **swallows the click/keydown** (`event.stopPropagation()`) so activating it never opens the item drawer. Rendered in `ItemCard`'s top-right cluster **beside** the existing pin/favorite marker (both wrapped in a `flex shrink-0 items-center gap-1.5` container), and **omitted entirely when `item.preview` is empty** — mirroring the drawer's `{value && …}` guard. It copies **`DashboardItem.preview`**, which `itemPreview()` in `src/lib/db/items.ts` already derives per content type (URL → `url`, FILE → `fileName`, else `content`), so the card and the drawer's Copy button always put the **same** value on the clipboard — **no query, shape, or schema change**. **`ItemCardButton`** had to change: it wrapped the card in a real `<button>`, and a nested copy `<button>` is **invalid HTML**, so it now uses the **`role="button"` + `tabIndex={0}` + `onKeyDown` (Enter/Space)** pattern `FileListRow` already uses for exactly this reason (it nests a download `<a>`); `ImageCardButton`/`FileListRow` were untouched (the images gallery and files list don't render `ItemCard`). Kept the icon **always visible** rather than hover-revealed — hover doesn't exist on touch, so a hover-reveal would hide the feature on mobile (confirmed by the user). **No new unit tests** — this is component-only, which the Vitest scope deliberately excludes; no new deps. `npm run test` (**124/124**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — 13 cards / 13 copy buttons on `/dashboard` with **no `button`-inside-`button`** in the DOM; a copy click overwrote a clipboard sentinel with the item's real content while the drawer **stayed closed**; keyboard **Enter** on the copy button copied without opening the drawer; keyboard **Enter** on the card still opened it (no regression from the `<button>` → `div` switch); and the copied `Check` state cleared after ~2s. **Process note:** an initial "the drawer opens on every copy click" finding was a **false positive** — the drawer detector matched the always-present mobile sidebar `<aside role="dialog">` rather than the drawer's `[data-slot="sheet-content"]`. That bogus signal prompted a rewrite of `CopyButton` off the Base UI `Button` onto a plain `<button>` + `buttonVariants`, justified by a confidently-wrong comment claiming Base UI hands `onClick` a native event. Once the correct selector was used, the Base UI `Button` proved fine and the rewrite + comment were **reverted**; the `stopPropagation` guard was then confirmed **load-bearing** by removing it and watching the drawer open.
- **Collection Create** — Wired the top bar's previously **display-only "New Collection" button** to a modal that creates a user-scoped collection (name + optional description), mirroring the item-create stack layer for layer. **Validation:** new `src/lib/validations/collections.ts` → `createCollectionSchema` (trimmed required `name`; `description` optional-nullable via the same `emptyToNull` preprocess + `min(1)` as `items.ts`) — the server-side source of truth. **Query:** `createCollection(userId, data)` appended to `src/lib/db/collections.ts` alongside the existing owner-scoped reads; the owner comes from the caller's session (`user: { connect: { id: userId } }`), never the payload, and since a brand-new collection holds no items it returns a `DashboardCollection` with `itemCount: 0` / `itemTypes: []` **without running the `getTypeCountsByCollection` tally query**. **Server action** (second `src/actions/*` in the repo): `createCollection(input)` in `src/actions/collections.ts` — `"use server"`, `auth()` guard, Zod `safeParse` (returns `issues` via `z.flattenError`), try/catch→generic error, reusing the `ActionResult<T>` type imported from `src/actions/items.ts` (a type-only import, erased at build, so the `"use server"` export constraint is satisfied). **Deliberate deviation from the ask:** the spec said "api routes for any client-side calls", but item *create* is a server action called from the client — the items API route (`GET /api/items/[id]`) exists for the drawer's client-side *fetch*. Collection create has no client fetch, so mirroring items meant **a server action and no new route**; noted in the spec before implementing. **UI:** new **`CreateCollectionDialog.tsx`** (client) built on the existing Base UI **`Dialog`** wrapper (`src/components/ui/dialog.tsx`) — Name + Description fields, **submit disabled while the name is blank or a save is in flight**, success/error **toast** via `toastManager`, `router.refresh()` + close on success, fields reset on close; wired into `TopBar.tsx` in place of the dead `Button` (dropping the now-unused `Button`/`FolderPlus` imports). Because all three collection surfaces are server-component reads, one `router.refresh()` updates the **dashboard grid, the sidebar list, and the Collections stat tile** together — no client cache to invalidate. **Testing:** **+17 Vitest cases** (124 → **141**) — `src/lib/validations/collections.test.ts` (name required/trimmed/blank-rejected, description blank→null + trimmed, issue `path`), extended `src/lib/db/collections.test.ts` (`createCollection`: owner `connect`, null description, dashboard-shaped zero-item return, **no tally query**), and a new `src/actions/collections.test.ts` (unauth without DB touch, validation issues, parsed-data passthrough, **a client-supplied `userId` is ignored**, success, thrown→generic). **No schema/migration** (`Collection` already had `name`/`description`/`userId`), no new deps. `npm run test` (**141/141**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — the dialog opened with Create disabled, creating "Testing Playbook" fired the **"Collection created"** toast, closed the modal, and refreshed all three surfaces at once (stat tile **5 → 6**, the new card first in the grid with its description and "0 items", the name in the sidebar list); a read-only SQL check confirmed the row's owner and persisted description; reopening showed the fields reset and a **whitespace-only name kept Create disabled** (the server-side rejection of that same input is covered by a unit test). **Process note:** the first verification pass reported the dialog as "open" when it was closed — `[role=dialog]` matches the always-present **mobile sidebar `<aside>`**, the exact false positive recorded in the Quick Copy entry above. Switching to **`[data-slot=dialog-content]`** gave the truth; that's the selector to use for any drawer/modal assertion in this app. **Cleanup:** the throwaway `Testing Playbook` collection was left in the dev DB — there's no collection-delete UI yet (out of scope), so removing it needs a `DELETE` on the `development` branch. Out of scope and deferred: editing, deleting, favoriting, `defaultTypeId` selection, and adding items to a collection.
- **Add Items to Collections** — Wired item↔collection membership into the item forms: a **collection picker** in the **New Item** dialog and in the drawer's **edit mode**, letting an item be filed under zero, one, or many collections. **Validation:** both schemas in `src/lib/validations/items.ts` gain `collectionIds`, sharing a new `cleanList` helper (trim → drop empties → de-duplicate) that `tags` now reuses. The two differ deliberately: `createItemSchema` has `.default([])` (always a `string[]`), while `updateItemSchema` uses `.transform(cleanList).optional()` — so update has **three distinct states**: omitted leaves membership untouched, `[]` clears it, a list replaces it. (First attempt used `.optional().transform(...)`, which in Zod v4 yields a *required* key typed `string[] | undefined` and broke every existing `updateItem` test caller; `.transform().optional()` wraps in `ZodOptional` and makes the key genuinely optional.) **Query:** `src/lib/db/items.ts` gains a private **`ownedCollectionIds(userId, ids)`** — a `findMany({ where: { id: { in: ids }, userId } })` that narrows client-supplied ids to the caller's own collections, **silently dropping** foreign/unknown ids (no existence leak) and short-circuiting to `[]` without a query when the list is empty — plus a `collectionLinks()` helper building the nested `ItemCollection` create payload. This closes the caveat flagged in the **Owner-Scoped Items & Collections (fix)** entry, which explicitly warned that the add-to-collection feature "must scope both sides of the link on write". `createItem` nests `collections: { create: … }`; `updateItem` **replaces** membership with `{ deleteMany: {}, create: … }` (mirroring how it already replaces `tags` via `set: []` + `connectOrCreate`) and only touches the join rows when `collectionIds` is defined. Server actions in `src/actions/items.ts` needed **no change** — they already pass `parsed.data` through wholesale. **API:** new **`GET /api/collections`** (`src/app/api/collections/route.ts`) — session-guarded, backed by a new lean `getCollectionOptions(userId)` in `src/lib/db/collections.ts` (owner-scoped, `{ id, name }`, alphabetical); like `/api/items/[id]` it's deliberately **not** in the proxy matcher, so an unauthenticated call returns JSON **401** rather than an HTML redirect. **UI:** new **`CollectionPicker.tsx`** (client) — a scrollable checkbox list (native `<input type="checkbox">` + `accent-primary`, no new primitive needed, so Radix stayed out) with loading / error / **empty** ("No collections yet…") states, an `idPrefix` prop so the two pickers' checkbox ids don't collide, and a `cancelled` flag guarding the fetch against unmount. It **fetches its own options** rather than taking a prop: the create dialog lives in `TopBar` and the edit form deep inside the client `ItemDrawer`, so neither has a server-fetched `collections` prop to hand down — the spec's option (a). Wired into `CreateItemDialog` (state resets on close) and `ItemDrawerEdit` (pre-checked from `item.collections`, available for **all** types including file/image). The drawer's existing **"In collections"** list refreshes from the `ItemDetail` the action already returns — **no extra fetch**. **Testing:** **+13 Vitest cases** (141 → **154**) — `items.test.ts` validation (create default `[]`, update `undefined` vs `[]`, trim/dedupe, non-array rejected), `db/items.test.ts` (createItem: owner-scoped lookup `where`, foreign ids dropped, no lookup when empty; updateItem: membership untouched when omitted, `deleteMany`+recreate shape, foreign ids dropped, empty clears without a lookup), `db/collections.test.ts` (`getCollectionOptions` owner-scoped + alphabetical), and `actions/items.test.ts` (`collectionIds` passthrough on create + update). The Prisma mock gained a `collection.findMany` spy. **No schema/migration** (`ItemCollection` already exists with cascade deletes on both sides), no new deps. `npm run test` (**154/154**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/api/collections` is `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — the New Item dialog rendered all 6 collections alphabetically; creating a snippet with *AI Workflows* + *React Patterns* checked toasted, closed, and its drawer listed both under **In Collections**; edit mode **pre-checked exactly those two**; unchecking *AI Workflows* and checking *DevOps* then saving left *DevOps* + *React Patterns* (**replace, not merge**); a read-only SQL check confirmed exactly 2 join rows, both with `collection.userId = item.userId`; `GET /api/collections` with `credentials: "omit"` returned **401 JSON**. The test item was deleted through the UI afterward (DB back to 18 join rows / 21 items). **Caveat:** the write-side **foreign-id drop is unit-tested but not runtime-exercised** — the picker only ever offers the user's own collections, so smuggling a foreign id would have needed a second account plus a tampered payload; the 4 tests pin the exact `where: { id: { in: ids }, userId }` clause that makes it impossible. **Process note:** `/feature start` was run out of order (branch + implementation before flipping Status to *In Progress*); the user flagged it twice and it's now saved to memory — follow the action file's numbered steps in sequence. **Out of scope and deferred:** collection detail pages, removing an item from a collection outside the edit form, collection edit/delete/favorite, `defaultTypeId` selection.
- **Collections Pages** — Built the **`/collections`** list and **`/collections/[id]`** detail routes. **The links already existed** — `CollectionCard` already wrapped itself in `<Link href={`/collections/${id}`}>` and the sidebar's "View all collections" `NavRow` already pointed at `/collections`; both merely 404'd because the routes didn't exist, so this feature satisfied that goal by building the pages and *verifying* the links rather than rewriting them (the sidebar's individual collection rows turned out to link to the detail pages too, and now work as a bonus). **Data layer:** added **`getAllCollections(userId)`** (favorites first, then most-recently-updated, **no `take`**) and **`getCollectionDetail(id, userId)`** to `src/lib/db/collections.ts`. Rather than duplicate the grouped `getTypeCountsByCollection` tally, `getRecentCollections` and `getAllCollections` now both delegate to a private **`listCollections(userId, { orderBy, take })`** — this resolved the spec's open "large limit vs. sibling function" question in favor of the sibling. `getCollectionDetail` is **owner-scoped** via `findFirst({ where: { id, userId } })`: an unknown id and another user's id both return `null`, so the page `notFound()`s either way and never reveals which — the same IDOR guard as `getItemDetail`, and the collection's items need no filter of their own since a collection's items belong to its owner. Its items are selected through **`itemSelect`** and mapped by **`toDashboardItem`** — both previously private to `src/lib/db/items.ts` and now **exported** — so collection items render through the identical preview / relative-time / tags pipeline as the dashboard cards. **Perf:** `generateMetadata` and the page body both resolve the collection *and* the user, so **`getCollectionDetail`** and **`getCurrentUser`** (`src/lib/db/user.ts`) are now wrapped in React **`cache()`**, sharing one query per request instead of each running it twice — following the existing `getSystemItemType` precedent. Note `getCurrentUser` is shared by every page, so that dedupe quietly benefits all of them. **Pages** (server components mirroring the `/items/[type]` shell — `SidebarProvider` + `Sidebar` + `TopBar`, sidebar data in the same `Promise.all`, `await connection()`, `getCurrentUser()` resolved first with a defensive `redirect("/sign-in")`, no guest fallback): `/collections` renders the existing **`CollectionCard`** in the dashboard's `sm:2 / lg:3 / xl:4` grid with a **"No collections yet."** empty state; `/collections/[id]` wraps in **`ItemDrawerProvider`** and renders one **`ItemCardButton`** grid (`md:2 / lg:3 / xl:4`) — a collection holds **mixed item types**, so unlike `/items/[type]` there is deliberately **no images-gallery / files-list branch** — plus a heading tinted by the collection's primary type color, a favorite star, the description, and a **"No items in this collection yet."** empty state. Cards open the **existing drawer** in place via `openItem(id)` (no navigation). **Route protection:** added **`/collections/:path*`** to the `matcher` in `src/proxy.ts` (`:path*` matches zero segments, so it covers `/collections` itself). **Testing:** **+7 Vitest cases** (154 → **161**) in `src/lib/db/collections.test.ts` — `getAllCollections` (owner-scoped `where`, favorites-first `orderBy`, **`take` is `undefined`** so the page can't silently truncate, tally-derived counts + most-used-first types) and `getCollectionDetail` (id **and** owner in the `where`, null on missing/foreign, item mapping through the shared mapper, `orderBy: { item: { updatedAt: "desc" } }`, empty item list) — the Prisma mock gained a `collection.findFirst` spy. **No schema/migration**, no new deps. `npm run test` (**161/161**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/collections` and `/collections/[id]` are `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP) against the live Neon **development** branch — unauthenticated, both routes **307 → `/sign-in?callbackUrl=…`**; as the seeded `test-engineer@a5project.com`, the sidebar's "View all collections" navigated to `/collections` showing all **6** collections favorites-first with correct counts and DevOps's 3 type badges, clicking the DevOps card landed on `/collections/col_devops` (document title "DevOps") with its **4 mixed-type items** (2 links emerald, 1 command orange, 1 snippet blue), and clicking one opened the drawer in place (URL unchanged, Monaco rendered) with **no nested `button`-in-`button`**. **The leak guard was exercised for real:** a freshly registered second account got a **404 on `col_devops`** — byte-identical to the 404 for a nonexistent id, with the document title reading the generic 404 rather than "DevOps" (so `generateMetadata` doesn't leak the name either) — and saw **"No collections yet."** with 0 cards. The empty-*collection* state was confirmed on the owner's 0-item "Testing Playbook". Drawer assertions used **`[data-slot=sheet-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter — the false positive recorded twice in the entries above). The throwaway account was removed with `npm run db:purge-users -- --yes`; a read-only SQL check confirmed only the seeded user remains (21 items / 6 collections — the extra File item and the "Testing Playbook" collection both predate this feature). **Out of scope and deferred:** collection edit / delete / favorite toggle, `defaultTypeId` selection, removing an item from a collection outside the item edit form, and pagination.
- **Collection Edit & Delete** — Added **Edit**, **Delete**, and a placeholder **Favorite** to `/collections/[id]` (an action row beside the heading) and to the collection cards on `/collections` and `/dashboard` (a **three-dots dropdown**), with a click anywhere else on a card still navigating to the collection. **Validation:** `updateCollectionSchema` in `src/lib/validations/collections.ts` — deliberately an **alias of `createCollectionSchema`** (same name + optional-nullable description via the shared `emptyToNull` preprocess), since the edit dialog always submits both fields; no new validation tests, the create schema's are already exhaustive. **Query:** `src/lib/db/collections.ts` gains `updateCollection(id, userId, data)` — an owner-scoped **`updateMany({ where: { id, userId } })`** returning null on `count === 0` (chosen over `findFirst`-then-`update` because `collection.update` keys off the unique id alone and so can't be scoped; `updateMany` is atomic and closes the TOCTOU window, matching the `deleteItem` precedent) — and `deleteCollection(id, userId)`, a single **`deleteMany({ where: { id, userId } })`** → `count > 0`. Unknown and foreign ids are indistinguishable at every layer, so nothing leaks whether a collection exists. To return the card shape after an edit without duplicating the tally mapping, extracted a private **`toDashboardCollection(row, tallyRows)`** now shared by `listCollections` and `updateCollection`. **Items are never deleted:** `ItemCollection` already declares `onDelete: Cascade` on the collection side, so `collection.deleteMany` drops only the membership rows — **no schema/migration**, and no hand-deleting of links. **Server actions:** `updateCollection(collectionId, input)` / `deleteCollection(collectionId)` in `src/actions/collections.ts`, following the house pattern (`"use server"`, `auth()` guard, Zod `safeParse` + `z.flattenError`, null/false → "Collection not found.", try/catch → generic, `ActionResult<T>`). **UI:** two **fully controlled** dialogs — `EditCollectionDialog` (Base UI `Dialog`) and `DeleteCollectionDialog` (Base UI `AlertDialog`, confirmation copy stating the items are *not* deleted) — plus `CollectionCardMenu` (three-dots, lucide `MoreHorizontal`) and `CollectionDetailActions` (icon row). The dialogs are controlled rather than trigger-owning because a Base UI menu **unmounts its content on close**, which would take a nested dialog with it; they render as **siblings** of the menu instead. `EditCollectionDialog` mounts its form only while `open` so the fields always initialize from the saved values — an initial `useEffect` reset tripped the `react-hooks/set-state-in-effect` lint rule, and the mount-fresh version is simply better. **Card restructure** (the one non-mechanical change): `CollectionCard` **was** a single `<Link>` wrapping everything, and a dropdown trigger inside an anchor is invalid HTML. It's now a `relative` container with the link **overlaying it** (`absolute inset-0 z-0`, `aria-label="Open <name>"`) as the click surface and the menu above it (`relative z-10`) — so the menu button is a sibling of the link, not a descendant, and needs **no `stopPropagation`** (the spec's suggestion) since the click never reaches the link at all. Chose the overlay link over the `role="button"` + `onKeyDown` pattern used by `ItemCardButton`/`FileListRow` because a collection card *navigates*, and a real anchor keeps middle-click / open-in-new-tab working. `CollectionCard` stays a **server component**; only the menu is client. **Bug found in browser testing (not by the unit tests):** deleting from the detail page left the user **stranded on the deleted collection's page** rendering its now-orphaned items — the delete had succeeded, only the navigation failed. `DeleteCollectionDialog` called `router.push("/collections")` and then `router.refresh()` unconditionally, and refreshing the route being *left* (whose id no longer exists) **aborts the pending navigation**. Fixed by branching: the detail page pushes (and `/collections` is dynamic, so the push refetches it), the card menus — which stay put — refresh. Everything else is one `router.refresh()`, which updates the dashboard grid, `/collections`, and the sidebar together since all three are server reads. **Testing:** **+18 Vitest cases** (161 → **179**) — `src/lib/db/collections.test.ts` (7: `updateCollection` owner-scoped `where`, null-without-re-read on no match, description cleared when omitted, refreshed card shape with tally; `deleteCollection` owner-scoped `where` and `count > 0` → boolean both ways) and `src/actions/collections.test.ts` (11: each action's unauth guard without a DB touch, validation issues, id + session-userId + parsed-data passthrough with trim/blank→null, not-found mapping, success shape, thrown → generic). The Prisma mock gained `findUniqueOrThrow`/`updateMany`/`deleteMany` spies. **No schema/migration**, no new deps (Base UI `Dialog`/`AlertDialog`/`dropdown-menu` wrappers all existed; Radix stayed out). `npm run test` (**179/179**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — `a button, a [role=button]` returned **0** on both `/collections` and `/dashboard` (no invalid nesting), `elementFromPoint` confirmed the card centre hits the overlay link while the three-dots hits the button, the menu opened **without changing the URL** with Favorite `data-disabled` and Delete `data-variant="destructive"`, Edit-from-the-menu pre-filled DevOps's saved values and saving renamed it (toast + grid + sidebar all updated; a read-only SQL check confirmed the row persisted under the right `userId`), the detail page rendered all three buttons with Favorite disabled, the delete confirmation interpolated the name, **Cancel was a genuine no-op** (row still present), and confirming deleted it. **The headline requirement was exercised for real:** deleting DevOps (4 items) took collections 5 → 4 and `ItemCollection` links 18 → 14 while **items stayed at 18** — all 4 ex-DevOps items alive, now owned by no collection, and still listed in the dashboard's Recent Items. The dev DB was **re-seeded** afterward to the clean 18-item dataset (note: re-seeding recreates the user, invalidating the browser session). Modal assertions used **`[data-slot=dialog-content]`** / **`[data-slot=alert-dialog-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches it — the false positive recorded twice in the entries above). **Known nit:** delete-from-detail uses `router.push`, so the deleted collection stays in browser history and **Back lands on a 404**; `router.replace` would avoid it. **Out of scope and deferred:** the favorite **toggle** (button/menu item render disabled — no mutation, no `isFavorite` write), `defaultTypeId` selection, and removing a single item from a collection outside the item edit form.
- **Global Search / Command Palette** — Added a **Cmd+K / Ctrl+K** command palette with client-side fuzzy search over the signed-in user's items and collections, grouped **Items** then **Collections**, keyboard-navigable, opening the existing item drawer or navigating to a collection page on select. The TopBar's previously **display-only search `Input`** became the trigger (a `<button>` styled as an input — a real `<input>` that opens a modal on focus is awkward) carrying a **`⌘K` hint** that corrects to `Ctrl K` off-Mac. **Data:** new `src/lib/db/search.ts` → `getSearchData(userId)` returning `{ items, collections }`, both queries **owner-scoped**; items carry `id`/`title`/preview/tags/type summary, collections `id`/`name`/`itemCount` (via `_count`). Previews are collapsed to one line and **capped at 160 chars** (`truncatePreview`) since the whole dataset ships to the client. Rather than duplicate the content-type branching, **`itemPreview` was exported** from `src/lib/db/items.ts` with its parameter widened to a structural `PreviewSource` (tags added to the payload beyond the spec's field list, because the search placeholder advertises tag search). **API:** `GET /api/search` — session-guarded, mirroring `/api/collections`, deliberately **outside the proxy matcher** so an unauthenticated call returns **JSON 401**, not an HTML redirect. The palette **pre-fetches on mount** and **re-fetches in the background on each open**, so a freshly created/deleted item never lingers in a stale client cache (race-guarded by a `requestId` ref, same as `ItemDrawer`). **UI:** new `src/components/ui/command.tsx` wrapping **cmdk primitives** — importing `Command`/`Input`/`List`/`Group`/`Item`/`Empty` but **never cmdk's own `CommandDialog`** — with the shell being the app's existing **Base UI `Dialog`** (`data-slot="command-dialog"`), preserving the deliberate Base-UI-wrapper convention; plus `SearchPalette.tsx` (client) holding the open state, the global keydown listener, and the fetch. The `⌘K` hint is read via **`useSyncExternalStore`** (server snapshot `true`, client snapshot from `navigator.userAgent`) rather than a mount effect — an effect tripped the `react-hooks/set-state-in-effect` lint rule and would have caused a cascading render. **Ranking (`src/lib/search.ts`, pure + injected `Scorer` so tests never import cmdk):** each result scores as the **max** of its weighted fields (max, not sum, so many tags can't out-rank a title hit; each field scored separately so a fuzzy match can't span a title→content boundary), weights title `1` > tag `.7` > preview `.5` > type `.3`; `buildSearchIndex` maps **id → fields** because each `CommandItem`'s cmdk `value` is its id (stable/unique, unlike a title — and verified cmdk passes `value` to `filter` verbatim, no lowercasing). **Bug caught only in the browser, never by the unit tests:** typing `devops` selected a *prompt* named "Refactoring assistant" over the collection literally named **DevOps**. Cause: fuzzy-matching a 160-char content blob finds a scattered subsequence for nearly any short query, and **cmdk sorts only *within* a group** (verified: it leaves group DOM order alone), so with Items rendered first per spec a weak item hit always beats a perfect collection hit. Fixed by giving `WeightedField` a **`fuzzy` flag** — short identifiers (title, tags, name, type) fuzzy-match; the long content preview requires a **verbatim case-insensitive substring**. `devops` now selects the DevOps collection; `useEffect` still finds the snippet by its body. **Structural fix:** `/collections/page.tsx` rendered `TopBar` but sat **outside `ItemDrawerProvider`**, so the palette's `useItemDrawer()` would have thrown on render — the page is now wrapped (the other three shells already were). **Dependency reality check:** the spec said "use the shadcn `cmdk` component", but shadcn's `Command` ships with `@radix-ui/react-dialog` and this codebase has deliberately kept Radix out. Probing `cmdk@1.1.1` showed it **hard-depends** on Radix (16 transitive packages) and imports `@radix-ui/react-dialog` at module scope. The initial claim that it would tree-shake out was **wrong**: despite every package declaring `sideEffects: false`, a real build ships it (`data-radix`/`DialogOverlay` present in the chunks). Measured by stubbing cmdk and rebuilding: **cmdk + its Radix subtree cost ~158 KB raw** client JS (2336.3 → 2178.3 KB), the Radix dialog being dead code. Accepted knowingly (authenticated dashboard, and cmdk's roving focus / `aria-activedescendant` / scroll-into-view are the easy things to get wrong by hand); the false comment in `command.tsx` was corrected to say so. **Testing:** **+33 Vitest cases** (179 → **212**) — `src/lib/search.test.ts` (19: field composition + fuzzy flags, max-not-sum, per-field scoring, empty-field skip, verbatim substring/case-insensitivity, "`udh` is a subsequence but not a substring", and a regression pinning the exact `devops` collection-vs-content ranking) and `src/lib/db/search.test.ts` (14: both queries owner-scoped, preview derived per content type, description fallback, truncation, `itemCount` from `_count`, recency `orderBy`, and that the lean select omits `fileUrl`/`fileSize`). **No schema/migration**; one new dep (`cmdk`). `npm run test` (**212/212**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/api/search` is `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — `Cmd+K` and clicking the search field both open it with the input auto-focused and **no hydration warning**; 18 Items + 5 Collections grouped with type-colored icons and pluralized counts; `udh` fuzzily ranks `useDebounce hook` first; ArrowDown/Up move the selection and **Enter opens the existing drawer** (Monaco rendered, URL unchanged); selecting a collection navigates to `/collections/col_devops`; `useEffect` matches by content substring; an unmatched query shows "No results found." and cmdk **hides the emptied group** entirely. **The leak guard was exercised for real:** a freshly registered second account got **0 items / 0 collections** from `/api/search` (the palette pre-fetches an entire dataset, so this is its highest-risk surface), and an unauthenticated `GET` returned **JSON 401**. The probe account was removed with `npm run db:purge-users -- --yes`; the dev DB is back to the clean 18-item seed. Drawer/modal assertions used **`[data-slot=command-dialog]`** / **`[data-slot=sheet-content]`**, never `[role=dialog]` (the mobile sidebar `<aside>` matches it — the false positive recorded three times above). **Known cost / open door:** the ~158 KB cmdk+Radix payload; the hand-rolled alternative remains cheap to swap in, since `src/lib/db/search.ts`, `/api/search`, `src/lib/search.ts` and all 33 tests are cmdk-independent (only `command.tsx`, the palette's list rendering, and the injected `defaultFilter` would change). **Out of scope and deferred:** searching by collection membership, recent-searches history, result pagination, and any server-side search.
- **Pagination** — Added `?page=`-based pagination with numbered links and prev/next to the three listings, and capped the dashboard's two grids. **Spec resolution:** the requirements named `/items/[type]` and `/collections/[id]`, but also defined `COLLECTIONS_PER_PAGE` — a constant with nowhere to live on either (a collection detail page lists *items*). The only listing of collections is `/collections`, which called `getAllCollections` with **no `take` at all**, so it was read as the third paginated listing; the two named pages use `ITEMS_PER_PAGE`, `/collections` uses `COLLECTIONS_PER_PAGE`. **Page math** lives in a new pure, import-free **`src/lib/pagination.ts`** — the four constants (`ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE` = 20, `DASHBOARD_COLLECTIONS_LIMIT`/`DASHBOARD_RECENT_ITEMS_LIMIT` = 8) plus `parsePageParam` (a positive whole number, else page 1 — covering absent/empty/`"0"`/`"-3"`/`"2.5"`/`"abc"`/`"Infinity"`/a repeated `?page=1&page=2` array), `buildPageInfo` (→ `{ page, perPage, totalCount, totalPages, hasPrev, hasNext, skip, take }`), `pageLinks` (first + last + current ±1 with `"ellipsis"` markers; every page when ≤ 7), and `pageHref` (page 1 drops the param, so a list's bare path is canonical). Keeping it dependency-free is what lets it carry **30 unit tests** under the repo's actions-and-utilities-only Vitest scope — the component and pages are thin over it. **Data layer — "only fetch what a page requires":** `getItemsByType(slug, userId, page)`, `getAllCollections(userId, page)` (→ `CollectionPage { collections, pagination }`), and `getCollectionDetail(id, userId, page)` each run an indexed `count`, then a `skip`/`take` `findMany`. The count runs **first, sequentially**, because `buildPageInfo` **clamps** an out-of-range `?page=` to the last page and the clamped page decides `skip` — so `?page=999` renders the last page rather than an empty grid. `listCollections` gained `skip`; `getRecentItems`/`getRecentCollections` now default to the two dashboard limits. **The one structural change:** `getCollectionDetail` used to pull the collection *and every one of its items* through a nested `findFirst` select — exactly what the spec forbids. Its items are now a separate paged query off **`ItemCollection`** (`orderBy: { item: { updatedAt: "desc" } }`), and the collection's own scalars were split into a new cached **`getCollectionSummary(id, userId)`** so `generateMetadata` (which only needs the name) and the page body still share **one** query per request — preserving the `cache()` dedupe that the old single-call shape gave for free. Owner scoping is unchanged and unweakened: `getCollectionSummary` keeps the `findFirst({ where: { id, userId } })` IDOR guard (unknown id and another user's id both → `null` → an identical 404), and the item query needs no filter of its own since the collection above is already owner-verified and a collection's items belong to its owner (documented at the call site). Each `count` is scoped **exactly** like its rows — a test pins that, since a mismatched count makes the controls lie. **UI:** new **`src/components/dashboard/Pagination.tsx`**, a **server component of plain `next/link` anchors** (no `"use client"`) so pages stay shareable, middle-click/open-in-new-tab work, and the back button behaves; it renders nothing when `totalPages < 2`. An unavailable prev/next is a **`<span>`, not a disabled `<a>`** — an anchor has no disabled state, and dropping the `href` is what actually stops it being followed or focused (`aria-disabled` + `opacity-50` carry the greyed-out look). Wired into all three pages via `parsePageParam((await searchParams).page)`; each `SectionHeader` now shows `pagination.totalCount` (the true total) rather than the 20 cards on screen. `/items/images` (gallery) and `/items/files` (list) inherit the controls for free — `Pagination` renders outside the gallery/list/grid branch. **Testing:** **+28 Vitest cases** (212 → **240**) — `src/lib/pagination.test.ts` (30 across the constants, `parsePageParam` junk-input fallbacks, `buildPageInfo` skip/flags/clamp-both-ends/empty-list-is-"1 of 1"/partial-last-page, `pageLinks` gap placement + no-duplicate-first-or-last + out-of-range clamp, `pageHref`), plus `db/items.test.ts` and `db/collections.test.ts` gaining count-scoping, `skip`/`take`, clamp, dashboard-default-limit, and "no item query when the collection is missing" cases. `getAllCollections`'s old **"with no limit … a `take` here would silently truncate it"** assertion was inverted by design and rewritten. The collections Prisma mock gained `itemCollection.{count,findMany}` spies. **No schema/migration**, no new deps. `npm run test` (**240/240**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — with 25 throwaway snippets + 25 throwaway collections inserted (**with the user's explicit approval**, per the CLAUDE.md no-unprompted-INSERT rule): 29 snippets paged **20 + 9** with page 2 starting exactly at probe 21 (**no overlap or gap across the boundary**), prev a disabled `SPAN` on page 1 and next a disabled `SPAN` on page 2, the header reading the true **29**; `?page=999` **clamped to page 2 with 9 cards** (not an empty grid) while `page=0`/`-2`/`abc`/`?page=1&page=2` all fell back to page 1; `/collections` paged **30 → 20 + 10**; `/collections/col_react_patterns` paged **28 → 20 + 8** with its document title still resolving through the new summary query; the **dashboard capped at 8 collection cards** (with 30 in the DB) and **8 recent items**, rendering **no** pagination nav; single-page lists (`/items/notes` empty state) hid the controls entirely; `/collections/col_bogus` and `/items/bogus` still **404**; and the item drawer still opened from **page 2** with `?page=2` intact and Monaco rendered. All 50 probe rows were then deleted by id prefix and a read-only check confirmed the dev DB back to its exact baseline (18 items / 4 snippets / 5 collections / 18 links / 0 leftovers). **Caveats:** `/items/images` and `/items/files` inherit pagination through the same code path proven on snippets, but neither seed type has >20 rows, so their controls weren't separately exercised. On `/collections/[id]` the heading tint reads `items[0]` — now the first item **of the current page** — so it can shift between pages (the collection *card*'s tint comes from a most-used-type tally and stays stable); left as-is with a comment rather than silently changed. **Out of scope and deferred:** pagination on the dashboard (capped, not paged) and on search results, and any page-size selector.
- **Settings Page** — Added a protected **`/settings`** route and moved the two account actions off `/profile` onto it. **Scope was a move, not a rewrite:** `ChangePasswordForm` and `DeleteAccountDialog` already existed and worked, and the API routes behind them (`POST /api/auth/change-password`, `DELETE /api/auth/account`) were **untouched** — so this feature added a route, added a dropdown link, and relocated two `<section>`s. **Terminology:** the ask said "forgot password", but `/profile` had no such section — it had **Change password** (current → new). That form is what moved; the separate `/forgot-password` email flow (reached from the sign-in page) is unrelated and stayed put. Confirmed with the user before implementing, along with the decision that `/profile` keeps its account-info card and Usage stats. **Page:** `src/app/settings/page.tsx` — a server component mirroring `/profile`'s centered shell (`await connection()`, `getProfile()` with a defensive `redirect("/sign-in")`, "Back to dashboard" link), rendering **Change password** only when `hasPassword` (so OAuth-only accounts never see it — the same gate `/profile` used) and the **Delete account** danger zone. `getProfile()` (`src/lib/db/profile.ts`) already returned `hasPassword` and was reused as-is; it's **not** `cache()`-wrapped, which is fine because `/settings` calls it once (no `generateMetadata` needing the same row). **Route protection:** added **`/settings/:path*`** to the `matcher` in `src/proxy.ts` (`:path*` matches zero segments, so it covers `/settings` itself — same trick as `/collections`). **Nav:** `SidebarUser.tsx` gained a lucide `Settings` `DropdownMenuItem` between Profile and Sign out (doc comment updated, since it claimed only two entries). **Components:** `ChangePasswordForm`/`DeleteAccountDialog` moved `src/components/profile/` → `src/components/settings/` via **`git mv`** (recorded as renames `R`, 0-line diffs, history preserved) per the `src/components/[feature]/` convention; the emptied `profile/` directory was removed. `src/app/profile/page.tsx` lost both sections and their imports (−31 lines). **Layout bug found while widening (the one real discovery):** asked to widen `/profile` to "the same width as Settings", both pages were **already `max-w-2xl`** — a literal no-op. Swapping to `max-w-4xl` *still* changed nothing: the container's computed `max-width` read `896px` but its box measured **493px**. Cause: **`<body>` is `flex flex-col`** (`src/app/layout.tsx`), so each page root is a flex item, and **`mx-auto` sets auto cross-axis margins, which suppress flexbox's default `align-self: stretch`** — collapsing the box to `fit-content`. `max-width` only *caps* a width; it can't make a shrink-to-fit box grow into it. So `max-w-2xl` had **never bound** on either page, and the two looked like different widths only because they hold different amounts of content. Fixed by adding **`w-full`** alongside `max-w-4xl` on both. Also bumped `/profile`'s "By type" grid to `lg:grid-cols-3` (flagged as the one change beyond container width). **Vestigial `flex flex-col`:** investigated on request — there is **no `<footer>` anywhere in `src/`**, no child uses `flex-1`/`grow`/`mt-auto`, and the `Toaster` portals out of flow, so the body's flex column is a sticky-footer scaffold for a footer that was never built. Empirically confirmed removal is safe (dashboard fills 1905px; the `(auth)` layout still fills the viewport and centers the card on **both** axes; `/profile` reaches 896px **without** `w-full`, directly proving the diagnosis; a toast fired via `/sign-in?registered=ready` stayed `position: fixed` bottom-right) — then **reverted it**, since changing the root layout touches every route and the user declined. `w-full` belongs **nowhere else**: `profile`/`settings` were the only two `mx-auto` containers, every other body child stretches by default, and the `(auth)` card already uses `w-full max-w-sm`. **The trap stays armed** for any future standalone `mx-auto max-w-*` page under `<body>`. **Testing:** **no new unit tests** — the feature adds no server actions or utilities, and the Vitest scope (`src/actions/*`, `src/lib/*`) deliberately excludes components and pages; the existing **240** must (and do) still pass. `npm run test` (**240/240**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/settings` and `/profile` both `ƒ`, `ƒ Proxy (Middleware)` intact). **No schema/migration**, no new deps. **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — unauthenticated `/settings` **307 → `/sign-in?callbackUrl=%2Fsettings`**, and signing in from that URL landed back on `/settings` (callbackUrl round-trip); the sidebar dropdown rendered exactly `Profile, Settings, Sign out` and navigated; `/settings` showed both sections and `/profile` showed only account-info + Usage (Sign out kept, zero trace of either moved section); a **wrong** current password produced the inline "Your current password is incorrect." and a **correct** one changed the password with the success toast + cleared fields; the delete dialog opened with the right copy and **cancelled** (never confirmed). The seeded password was changed and **changed back**, with the restore *proven* from a fresh cookie jar (`12345678` → valid session; `tempPass98765` → `null`) — an earlier in-page check was junk, returning an opaque `status: 0` and reading back the **pre-existing** session, which would have "passed" regardless. Widths measured at 1920px (both containers exactly **896px**, `left: 512`, By-type 3 columns) and at 390px (both fill, By-type 1 column, **no horizontal overflow** either way). Dialog assertions used **`[data-slot=alert-dialog-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches it — the false positive recorded three times in the entries above). **Process note:** nearly started a **second dev server** on `:3000` before the user stopped me — check `lsof -ti:3000` first. **Out of scope and deferred:** theme/appearance settings, notification preferences, billing/Stripe management, editing name or email, and dropping the vestigial `flex flex-col` from `<body>`.
- **Editor Preferences Settings** — Added an **Editor Preferences** section to `/settings` that persists five Monaco settings to a new `editorPreferences` JSON column on `User`, **auto-saves on every change** (no save button; success toast), and feeds the saved values into every `CodeEditor` via a new context. **Confirmed decisions before building:** follow the spec's theme list **literally** (`vs-dark` default / `monokai` / `github-dark`) and **drop** the app's custom `devstash-dark`; **hand-define** monokai + github-dark inline (no new dep, same approach the old `devstash-dark` used); **no live preview** on settings (controls + toast only). **Schema:** `editorPreferences Json?` on `User` via `migrate dev` (additive `ADD COLUMN`, never `db push`) — the `20260711045930_add_user_editor_preferences` migration on the Neon **development** branch; `prisma generate` re-run so the client types picked it up (tsc failed until then). **Validation** (`src/lib/validations/editor-preferences.ts`, pure/import-free so it's unit-testable): `editorPreferencesSchema` (fontSize/tabSize as `z.number().refine`-in-set, `wordWrap`/`minimap` booleans, `theme` a `z.enum`), exported `EditorPreferences` type, `DEFAULT_EDITOR_PREFERENCES` (`{ fontSize 12, tabSize 2, wordWrap true, minimap false, theme "vs-dark" }` — note **wordWrap default flips on**, opposite the editor's old hardcoded `off`), and the `FONT_SIZES`/`TAB_SIZES`/`EDITOR_THEMES` option arrays the form reuses. **DB** (`src/lib/db/editor-preferences.ts`): `getEditorPreferences(userId)` — `findUnique` the JSON, `safeParse({ ...DEFAULT, ...stored })` so a null column, a **partial** object, or a legacy/invalid value all degrade to defaults (per-field for missing keys, wholesale for invalid) — wrapped in React **`cache()`** so a page reading prefs alongside `generateMetadata` shares one query; `updateEditorPreferences(userId, prefs)` writes the JSON. **Server action** (`src/actions/editor-preferences.ts`, house pattern): `"use server"` → `auth()` guard → `safeParse` (`issues` via `z.flattenError`) → owner-scoped write (owner from the **session**, not the payload) → `ActionResult<EditorPreferences>` (type imported type-only from `@/actions/items`, matching `actions/collections.ts`). **Context** (`src/components/dashboard/editor-preferences-context.tsx`): `EditorPreferencesProvider` (takes a server-fetched `value`) + `useEditorPreferences()` that **returns the defaults when no provider is present** (defensive default, *not* a throw like `useItemDrawer`, so `CodeEditor` never crashes anywhere). **Form** (`src/components/settings/EditorPreferencesForm.tsx`, client): native `<select>`s + checkbox toggles styled with existing tokens (`accent-primary`, matching `CollectionPicker` — no new shadcn primitive), seeded from an `initial` prop; each change **optimistically** updates local state, calls the action, toasts on success (`toastManager`), and **reverts + error-toasts on failure**. **`CodeEditor` changes:** consume `useEditorPreferences()`; in `beforeMount` keep the TS/JS diagnostics suppression, **remove `devstash-dark`**, and `defineTheme("monokai", …)` + `defineTheme("github-dark", …)` (compact inline token/color rules — `vs-dark` is built-in); apply `fontSize`/`tabSize`/`wordWrap ? "on":"off"`/`minimap.enabled`/`theme` from prefs; dropped the hardcoded `#1c1c1c` wrapper bg (→ `bg-card`) so the selected theme's own background shows through. **Provider wiring:** all **4 dashboard-shell pages** (`dashboard`, `items/[type]`, `collections`, `collections/[id]`) add `getEditorPreferences(currentUser.id)` to their existing `Promise.all` and wrap the shell in `<EditorPreferencesProvider>` just inside `SidebarProvider`, around `ItemDrawerProvider` — so both the drawer's editor and `TopBar`'s `CreateItemDialog` editor read it. The settings page fetches prefs and renders the section for **all** accounts (not gated on `hasPassword`). **Testing:** **+20 Vitest cases** (240 → **260**) — `validations/editor-preferences.test.ts` (valid parse, out-of-set fontSize/tabSize/theme rejected, non-boolean toggles rejected, partial rejected, unknown keys stripped, defaults valid + exact, option-array contents), `db/editor-preferences.test.ts` (mock Prisma: defaults on null/missing-row/invalid, full object as-is, **partial merged over defaults**, read scoped to the user id, update `where`/`data` shape — distinct userIds per case to dodge `cache()` memo collisions), and `actions/editor-preferences.test.ts` (unauth without a DB touch, validation `issues`, parsed-data + session-id passthrough, thrown → generic). **No new deps.** `npm run test` (**260/260**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `/settings` still `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — `/settings` rendered the section with all five controls seeded from saved values; changing theme → github-dark, font → 18, minimap → on, word-wrap → off each **auto-saved** (the **"Preferences saved"** toast captured by polling the notifications region, since its 4s auto-dismiss outran a naive `wait_for`), and a read-only SQL check confirmed the **full merged JSON** persisted under the right `userId`; opening the `useDebounce` snippet drawer showed the Monaco editor at **18px**, **minimap visible**, and background **rgb(13,17,23) = #0d1117** (github-dark applied), proving the whole context→editor chain. Preferences were then **restored to defaults via the UI** (confirmed by SQL) so the demo user is back to baseline; the dev server (started after a `lsof -ti:3000` check) was stopped afterward. **Out of scope and deferred:** applying preferences to the `MarkdownEditor`, per-language/per-item overrides, a settings live-preview editor, themes beyond the three named, and font-family selection.
- **Favorites Page** — Added a protected **`/favorites`** route listing the signed-in user's favorited items and collections in a compact, terminal-style list (no cards), plus a **star link in the `TopBar`** pointing to it. **Scope decisions (confirmed with the user before starting):** do **not** build a favorite-*toggle* — the star buttons in the item drawer / collection actions stay display-only, so the page only *lists* whatever is already flagged `isFavorite`; and sort both sections by **`updatedAt` desc** (there's no dedicated "favorited-at" timestamp, so the per-row "date added" is the record's `updatedAt`). **Data layer:** `getFavoriteItems(userId)` in `src/lib/db/items.ts` (owner-scoped `where: { userId, isFavorite: true }`, `orderBy updatedAt desc`, mapped through the shared `itemSelect` + `toDashboardItem` so rows carry the same preview/relative-time/type shape as the dashboard cards) and `getFavoriteCollections(userId)` in `src/lib/db/collections.ts` (same owner-scoped filter; selects `{ id, name, updatedAt }` then reuses the private `getTypeCountsByCollection` tally to derive each collection's **most-used-type color** and **item count**, color `null` when empty). Exported the previously-private **`relativeTime`** from `items.ts` so the collection query can format its own dates. **Components:** a shared presentational **`FavoriteRow`** (dense `font-mono` row — leading type icon, truncated title, a small color-tinted type badge, trailing relative date; clean lines, `hover:bg-muted/50`, no cards) wrapped by **`FavoriteItemRow`** (`"use client"`, `role="button"` + `tabIndex`/`onKeyDown` for Enter/Space → `openItem(id)`, mirroring `ItemCardButton`'s non-`<button>` pattern so the drawer opens in place with no navigation) and **`FavoriteCollectionRow`** (a server-rendered `next/link` → `/collections/[id]`, icon/badge tinted by the collection's primary type color, neutral gray `#6b7280` when empty; badge reads "Collection"). Item badge label capitalizes the stored singular type name (`snippet` → "Snippet"). **Page** (`src/app/favorites/page.tsx`, server component mirroring the `/collections` shell — `SidebarProvider` + `EditorPreferencesProvider` + `ItemDrawerProvider` + `Sidebar` + `TopBar`, `await connection()`, `getCurrentUser()` resolved first with a defensive `redirect("/sign-in")`, sidebar data + both favorites lists in one `Promise.all`): a **Star** heading, then **Items** and **Collections** sections each via `SectionHeader` with a live count, a per-section muted empty line, and a single dashed **global empty state** ("No favorites yet. Star an item or collection to see it here.") when both are empty. Wrapped in `ItemDrawerProvider` + `EditorPreferencesProvider` so item rows open the drawer (with its Monaco editor) in place. **Route protection:** added **`/favorites/:path*`** to the `matcher` in `src/proxy.ts` (`:path*` covers `/favorites` itself). **Testing:** **+3 Vitest cases** (260 → **263**) — `getFavoriteItems` (owner + `isFavorite` `where`, `updatedAt desc`, shared-mapper shape) in `db/items.test.ts`, and `getFavoriteCollections` (owner + `isFavorite` `where`/`orderBy`; tally-derived color + count, `null`/`0` when empty) in `db/collections.test.ts`. **No schema/migration** (reuses the existing `Item.isFavorite` / `Collection.isFavorite` columns), no new deps. `npm run test` (**263/263**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (`/favorites` is `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — `/favorites` rendered the TopBar star link, an empty Items section (**DB read confirmed 0 favorited items**, so the empty state is correct, not a bug), and the **3 favorited collections** (Design Resources / AI Workflows / React Patterns, each a "Collection" badge + "2d ago"); clicking a collection row navigated to `/collections/col_design_resources`. To exercise the un-seeded item path, **with the user's explicit approval** (per the CLAUDE.md no-unprompted-`UPDATE` rule) one item (`useDebounce hook`) was temporarily flagged `isFavorite = true` — the Items section then showed the row (Snippet badge, "2d ago") and clicking it **opened the ItemDrawer in place** (URL stayed `/favorites`, Monaco editor rendered) — then reverted to `false` (a read-back confirmed the dev DB is back to baseline: **0 favorite items / 3 favorite collections**). Drawer assertions used **`[data-slot=sheet-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter — the false positive recorded repeatedly in the entries above). **Out of scope and deferred:** the favorite-toggle itself (no UI writes `isFavorite` yet), searching/filtering within favorites, and pagination (favorites render as a single unpaged list).
- **Favorite Toggle (Items & Collections)** — Wired the previously **display-only** favorite (star) controls to actually persist `isFavorite`, closing the toggle that the **Favorites Page** feature built the listing for but explicitly deferred. **Confirmed with the user before starting:** for collections the toggle lives on the **card** (three-dots menu) and the **detail page** action row only — collections open a page, not a drawer, so there's no collection drawer. **Data layer** (owner-scoped, matching the `updateCollection`/`deleteItem` precedent): `setItemFavorite(id, userId, isFavorite)` in `src/lib/db/items.ts` and `setCollectionFavorite(id, userId, isFavorite)` in `src/lib/db/collections.ts` — each a single atomic **`updateMany({ where: { id, userId }, data: { isFavorite } })`** returning `count > 0` (can't be scoped the way `update`, which keys off the unique id alone, could — same IDOR guard as delete). **Server actions** (`src/actions/items.ts`, `src/actions/collections.ts`): `setItemFavorite(itemId, isFavorite)` / `setCollectionFavorite(collectionId, isFavorite)` following the house pattern — `"use server"`, `auth()` guard, a **`typeof isFavorite !== "boolean"`** guard (server actions take untrusted input), delegate to the owner-scoped query, map false → "not found.", try/catch → generic, `ActionResult<{ id, isFavorite }>`. **Client:** a shared **`useFavoriteToggle`** hook (`src/components/dashboard/useFavoriteToggle.ts`) does **optimistic** UI — flips local state immediately, calls the injected action, **reverts + toasts on failure**, and **`router.refresh()`** on success so the dashboard stats tiles, sidebar, and `/favorites` (all server reads) update together. Two thin star buttons consume it: **`ItemFavoriteButton`** (used in the item card with `stopPropagation` so a click favorites without opening the drawer — the same guard `CopyButton` uses — and in the drawer action bar) and **`CollectionFavoriteButton`** (collection detail action row). **Surfaces wired:** `ItemCard` now shows an **always-visible** interactive star (pin stays a display-only indicator; the always-visible choice matches `CopyButton`, since hover doesn't exist on touch) — nested as a real `<button>` inside the `role="button"` card wrapper (valid HTML, like `CopyButton`); `ItemDrawerDetail`'s star became the toggle (keyed by `item.id`); `CollectionCardMenu`'s disabled "Favorite" item now toggles (label flips **Favorite/Unfavorite**, keeps its optimistic state across the menu content unmounting on close because the hook lives on the menu component, not its content) — the card keeps its amber indicator star; `CollectionDetailActions`' disabled star became the toggle, and the **now-redundant heading indicator star was removed** from `/collections/[id]` (the button is both toggle and indicator). **No schema/migration** — reuses the existing `Item.isFavorite` / `Collection.isFavorite` columns; no new deps. **Behavior note:** the toggle writes through `updateMany`, so `@updatedAt` bumps on favorite — which reorders Recent Items and lifts the record to the top of `/favorites`; kept as-is since `/favorites` uses `updatedAt` as its "favorited-at" proxy (per that feature's design), so this makes the ordering favorite-aware. **Testing:** **+18 Vitest cases** (263 → **281**) — `db/items.test.ts` + `db/collections.test.ts` (each query: owner-scoped `where`, `count>0`→true / `count:0`→false; added `item.updateMany` to the items Prisma mock) and `actions/items.test.ts` + `actions/collections.test.ts` (each action: unauth guard + non-boolean guard both **without touching the DB**, id/owner/flag passthrough, missing/foreign → not-found, success `{ id, isFavorite }`, thrown → generic). `npm run test` (**281/281**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — baseline **0 favorite items / 3 favorite collections** captured first: (1) favoriting from an **item card** left the drawer closed (`[data-slot=sheet-content]` absent), filled the star, and moved the **Favorite Items stat 0→1** with the DB row persisted; (2) the **item drawer** star toggled on (DB → 2) then off (DB → 1), exercising both directions; (3) the **collection card menu** opened **without navigating** (URL unchanged) and favorited DevOps (DB persisted, card indicator star appeared); (4) the **collection detail** button loaded reflecting the favorited state and toggled off; **cross-surface sync** confirmed on `/favorites` (Items 1 / Collections 3). **The DB was returned to its exact baseline** (0 fav items / 3 fav collections) by toggling everything back through the UI — no `isFavorite` cleanup needed. Modal/drawer assertions used **`[data-slot=sheet-content]`** / **`[data-slot=dropdown-menu-item]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter — the false positive recorded repeatedly in the entries above). **Residue:** favoriting bumps `@updatedAt`, so the three records touched during testing (Lucide + Radix items, DevOps collection) now read "just now" instead of "2d ago" — cosmetic recency drift only; `isFavorite` is exact, so no re-seed was run. **Out of scope and deferred:** the pin toggle (still display-only), a favorite toggle on the compact `/favorites` rows, and bulk favoriting.
- **Pinned Items** — Wired the item drawer's previously **inert** Pin button (rendered but no `onClick`) to persist `isPinned`, closing the pin toggle the **Favorite Toggle** feature explicitly deferred. **Items only** — collections are out of scope (spec). Mirrors the Favorite Toggle stack layer-for-layer, deliberately duplicated per-entity rather than generalized. **Data layer** (`src/lib/db/items.ts`): `setItemPin(id, userId, isPinned)` — a single atomic owner-scoped **`updateMany({ where: { id, userId }, data: { isPinned } })`** returning `count > 0` (same IDOR guard as `setItemFavorite`/`deleteItem` — can't write another user's row the way `item.update`, keyed off the unique id alone, could). Also changed **`getItemsByType`**'s ordering from `{ updatedAt: "desc" }` to **`[{ isPinned: "desc" }, { updatedAt: "desc" }]`** so pinned items float to the top of the `/items/[type]` listing (pagination's `skip`/`take` ride on top of the combined order unchanged). The dashboard's Pinned section already existed (`getPinnedItems`), so no change there. **Server action** (`src/actions/items.ts`): `setItemPin(itemId, isPinned)` — `"use server"`, `auth()` guard, a **`typeof isPinned !== "boolean"`** guard (untrusted input), delegates to the owner-scoped query, maps false → "Item not found.", try/catch → generic, `ActionResult<{ id, isPinned }>`. **Client:** a dedicated **`usePinToggle`** hook (`src/components/dashboard/usePinToggle.ts`) mirroring `useFavoriteToggle` — **optimistic** flip, calls the action, **reverts + toasts on failure**, `router.refresh()` on success (no success toast — the optimistic flip is the feedback; **confirmed with the user** that following the Favorite pattern's error-only toast is what's wanted, resolving the spec's contradictory "toast on success/error" vs "follow the Favorite pattern" bullets). A thin **`ItemPinButton`** (`src/components/dashboard/ItemPinButton.tsx`) consumes it — a `rotate-45` `Pin` that fills when pinned, `aria-pressed`, `aria-label` flipping Pin/Unpin. Wired into `ItemDrawerDetail` in place of the display-only `<Button>` (dropped the now-unused `Pin`/`cn` imports; updated the "display-only for now" doc comment). **The `ItemCard` pin stays a static indicator** (spec) — unlike the favorite star, which became interactive; only the drawer button toggles. **Bug caught in the browser, not by the unit tests:** I'd keyed **both** the pin and favorite drawer buttons with `key={item.id}` — duplicate sibling keys, a React console error. Each needs a per-item key (to force-remount so the optimistic `useState(initial)` resets when the drawer swaps items), so fixed to distinct **`pin-${item.id}`** / **`favorite-${item.id}`**. **No schema/migration** (reuses `Item.isPinned`), no new deps. **Testing:** **+9 Vitest cases** (281 → **290**) — `db/items.test.ts` (`setItemPin`: owner-scoped `where`+`data` shape, `count>0`→true / `count:0`→false; updated the `getItemsByType` orderBy assertion to the pinned-first array) and `actions/items.test.ts` (`setItemPin`: unauth guard + non-boolean guard both **without touching the DB**, id/owner/flag passthrough, missing/foreign → not-found, success `{ id, isPinned }`, thrown → generic). `npm run test` (**290/290**), `tsc --noEmit`, `npm run lint`, and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — on `/items/snippets`, pinning the **last** snippet ("Theme context provider") flipped the drawer button to "Unpin"/`aria-pressed=true` optimistically and, after `router.refresh()`, moved it to the **top** of the listing (proving both persistence via the re-query and pinned-first ordering); unpinning dropped it back below the still-pinned "useDebounce hook"; the seeded-pinned "useDebounce hook" opened already showing "Unpin"/pressed. The duplicate-key fix was confirmed by re-checking the console (clean) after the change. Drawer assertions used **`[data-slot=sheet-content]`**, never `[role=dialog]` (the mobile sidebar `<aside>` matches the latter — the false positive recorded repeatedly in the entries above). **Residue:** pinning bumps `@updatedAt`, so the one item toggled during testing ("Theme context provider") now reads "just now" and sorts first among the unpinned snippets — cosmetic recency drift only; `isPinned` is back at its seeded baseline (the seed's 3 pinned items untouched), so no re-seed was run. **Out of scope and deferred:** a pin toggle on the `ItemCard` / compact rows, pinning collections, and bulk pinning.
- **Homepage Marketing Mockup** — Built a **standalone marketing homepage prototype** at `prototypes/homepage/` (`index.html`, `styles.css`, `script.js`) — plain HTML/CSS/JS with **no** Next.js/React/Prisma/DB involvement, deliberately outside the app's `src/` structure, Vitest scope, and migrations policy (so no unit tests, `npm run build`, or DB work applied). Followed the spec's own dark-theme **item-type accent palette** (Snippet blue `#3b82f6`, Prompt amber `#f59e0b`, Command cyan `#06b6d4`, Note green `#22c55e`, File slate `#64748b`, Image pink `#ec4899`, URL indigo `#6366f1`) as CSS custom properties — noting these **differ** from the live app's type colors (app Prompt is purple, Command orange, Link emerald); the prototype follows the spec, not the app. **The centerpiece hero** is a three-part "chaos → order" visual: (left) a **chaos container** ("Your knowledge today…") with **8 floating icons** (inline SVGs for Notion, GitHub, Slack, VS Code, browser tabs, terminal, text file, bookmark) animated in `script.js` via a single **`requestAnimationFrame`** physics loop — each icon drifts, **bounces off the container walls**, has subtle **rotation + scale pulsing**, is speed-clamped with light damping + a minimum "keep-alive" jitter, and **repels from the mouse cursor** (inverse-distance push within a 90px radius, tracked relative to the field via `getBoundingClientRect`); (center) a **CSS-pulsing transform arrow** (`@keyframes arrow-pulse`); (right) a **mini DevStash dashboard preview** ("…with DevStash") — a mock sidebar nav + a grid of item cards with **type-colored top borders**. **Supporting sections:** a **fixed nav** (logo, Features/Pricing links, Sign In / Get Started) that gains a blurred opaque background on scroll via an `is-scrolled` class toggled by a passive scroll listener (plus a mobile hamburger that toggles an `is-open` dropdown panel); a **gradient-text hero headline** ("Stop Losing Your Developer Knowledge") with subheadline + CTAs; a **6-card feature grid** (Code Snippets, AI Prompts, Instant Search, Commands, Files & Docs, Collections) each tinted by its item-type accent (top-border + icon + hover glow via `color-mix`); an **AI/Pro section** — two columns with a "Pro Feature" badge + AI-capability checklist on the left and a **code-editor mockup** (window dots, faux-highlighted `useDebounce` TS, and a staggered-animation "AI Generated Tags" demo) on the right; a **Free vs Pro pricing** block (Free $0 / 50 items / 3 collections vs Pro highlighted with a "Most Popular" badge) with a **monthly/yearly billing toggle** in `script.js` that rewrites the Pro price ($8/mo ↔ $6/mo "$72 billed yearly"); a **CTA band** ("Ready to Organize Your Knowledge?"); and a **footer** with link columns + a JS-injected current year. **Animations & a11y:** scroll **fade-in reveals** via `IntersectionObserver` (with a no-`IO` fallback that just shows everything), the rAF chaos loop, the CSS arrow pulse, and a **`prefers-reduced-motion`** branch that statically places the icons (no loop), disables the arrow pulse + tag animation, and shows reveals immediately. **Responsive:** the hero's chaos/arrow/dashboard **stack vertically** on mobile with the **arrow rotated 90°** to point down (its keyframes overridden to preserve the rotation), feature/pricing/footer grids collapse to fewer columns, and the desktop nav links/actions swap for the hamburger dropdown. The chaos-field JS also handles **resize** (re-measures the field and clamps icon positions back inside). During implementation the user **edited `index.html` to add 6 more mini-cards** to the dashboard-preview grid (12 total) — kept as-is. **Verification note:** browser verification was **interrupted** (the local HTTP server the prototype needs — `file://` is blocked in the Playwright env — was declined) and the feature was completed **without a live browser check**; the markup/CSS/JS were reviewed by hand but the chaos animation, cursor-repel, scroll reveals, nav-opacity, and pricing toggle were **not visually confirmed running**. No app build/test/lint was run since the prototype is fully outside the Next.js app.
- **Homepage (Marketing Landing Page)** — Turned the static prototype (`prototypes/homepage/` — `index.html`/`styles.css`/`script.js`, built in the prior "Homepage Marketing Mockup" feature and never browser-verified) into the **real public landing page at the root route `/`** (replacing the placeholder `<h1>DevStash</h1>`), rebuilt as React server/client components with Tailwind + the app's ShadCN primitives — reproducing the prototype's look/behavior, not copying its raw HTML/CSS/JS. **`src/app/page.tsx`** is a **server component** rendered **outside** the dashboard shell (no `Sidebar`/`TopBar`), **public** (not added to the `proxy.ts` matcher), that calls **`auth()` and `redirect("/dashboard")`** when a session exists (so the marketing page is logged-out-only — this makes `/` render dynamic `ƒ`), and sets `metadata` (title "DevStash — Stop Losing Your Developer Knowledge" + description) from the prototype `<head>`. **Components under `src/components/marketing/`** (server-first; only four are `"use client"`): **`MarketingNav`** (client — fixed nav, passive scroll listener toggles a blurred/opaque background, mobile hamburger toggles a stacked dropdown with `aria-expanded`); **`Hero`** (server — gradient headline, CTAs, and the "chaos → order" showcase); **`ChaosField`** (client — the `requestAnimationFrame` physics: 8 inline-SVG brand icons drift, bounce off walls, rotate + scale-pulse, and repel from the cursor, with resize re-measure/clamp, a `prefers-reduced-motion` static-placement branch, and full rAF + listener cleanup on unmount); **`DashboardPreview`** (server — static mini sidebar + 12 accent-top-bordered item cards); **`FeatureGrid`** (server — 6 cards mapped from a `FEATURES` array, each tinted by its accent via a per-card `--c` inline var, `color-mix` hover glow); **`AiSection`** (server — Pro badge, green checklist, and a code-editor mockup with window dots, syntax-colored spans, and a staggered "AI Generated Tags" fade-in); **`Pricing`** (client — monthly/yearly toggle drives the Pro price `$8/mo ↔ $6/mo "$72 billed yearly"` from state, `aria-pressed` reflects the period, cards mapped from `PRICING_PLANS`); **`Reveal`** (client — `IntersectionObserver` fade-in with a no-IO immediate-show fallback and `motion-reduce:*` utilities that force the visible state); and **`Footer`** (server — link columns + `new Date().getFullYear()`). Kept **DRY**: a shared **`marketing-button`** cva (primary/outline/ghost × default/lg × block) that styles `next/link` CTAs, plus `FEATURES`/`PRICING_PLANS`/`PREVIEW_*`/`FOOTER_COLUMNS`/`AI_*` data arrays in `constants.ts` and inline-SVG chaos marks in `icons.tsx`. **Deliberate deviation:** the spec suggested reusing the ShadCN `Button` via `asChild`, but that `Button` is Base-UI-based (uses `render`, not `asChild`) and its primary color differs from the marketing indigo→violet gradient, so CTAs use the `marketing-button` cva on a `Link` instead (the spec's "utility classes" latitude) — the existing **`Logo`** *is* reused (nav + footer). **Styling:** the prototype's own **marketing accent palette + dark surfaces** are defined once as `--m-*` custom properties scoped to a **`.marketing`** wrapper in `globals.css` (distinct from the app's oklch theme; these accents differ from the live item-type colors), referenced via Tailwind arbitrary values; the ambient background glow is a fixed `-z-[1]` child inside the `isolate` wrapper; the two keyframe animations (`m-arrow-pulse` + mobile-rotated `m-arrow-pulse-vertical`, `m-tag-in`) and their `prefers-reduced-motion` off-switch live in `globals.css`. **Responsive:** the chaos/arrow/preview showcase stacks vertically under `900px` with the arrow rotated 90°, feature/pricing/footer grids collapse, and the nav swaps to the hamburger. **Links** all wired via `next/link`: brand→`/`, Sign In→`/sign-in`, all CTAs (Get Started/Start stashing/Go Pro)→`/register`, Features→`#features`, Pricing→`#pricing`, "Unlock AI features"→`#pricing`; footer placeholders→`#`. **No unit tests** — components/page only, which the Vitest scope (`src/actions/*`, `src/lib/*`) deliberately excludes; **no schema/migration**, no new deps. Fixed one lint error during the build: `Reveal`'s no-IO fallback called `setVisible(true)` synchronously in the effect (tripping `react-hooks/set-state-in-effect`, a rule this repo has hit before) — deferred it to a `requestAnimationFrame` with cleanup. `npm run lint` clean (only a pre-existing warning in the prototype's untouched `script.js`), `npm run build` clean (`/` is `ƒ`, `ƒ Proxy (Middleware)` intact), and `npm run test` **290/290**. **Verified end-to-end in a real browser** (Playwright MCP, live dev server): a signed-in session at `/` **307→`/dashboard`** while a logged-out visitor gets the marketing page with the correct `<title>` and no console errors; all link destinations correct via SSR (5×`/register`, `/sign-in`, 3×`#features`/`#pricing`); scroll-`Reveal` fades Features/AI/Pricing in; the **ChaosField rAF physics is animating** (8 icons' transforms — position/rotation/scale — changing over 350ms); the **pricing toggle** flips Pro to **$6** / "$72 billed yearly" with `aria-pressed`; the **mobile hamburger** (390px) opens/closes with `aria-expanded`, the showcase collapses to one column with the **arrow rotated 90°** (`matrix(0,1,-1,0,0,0)`), and there's **no horizontal overflow**; the **nav scroll-background** turns `rgba(10,10,15,0.76)` + `blur(10.7px)` when scrolled and transparent at top; and **`prefers-reduced-motion` (emulated)** makes the chaos icons static (no loop), reveals show immediately (`opacity:1`), and the arrow/tags animations read `animation-name: none`. To test the logged-out page I signed out the seeded session in the Playwright browser via the NextAuth CSRF signout (expected; the app session is unaffected). **Out of scope and deferred:** real billing/Stripe wiring, the placeholder footer pages (Changelog/About/Blog/Careers/Docs/Support/Community), and any changes to the dashboard or auth flows.
- **Stripe Integration — Core Infrastructure (Phase 1)** — Laid the reusable, testable foundation for Stripe Pro subscriptions ($8/mo or $72/yr) as **pure library code with zero user-facing surface** — no routes, webhooks, gating, or UI (Phase 2 builds those on top), so Vitest covers it without the Stripe CLI or a live DB. Installed the **`stripe`** server SDK (v22.3.1; Hosted Checkout needs no client `@stripe/stripe-js`) and pinned its expected **`apiVersion` `2026-06-24.dahlia`** (read from the installed SDK's `ApiVersion` constant — with `typescript: true` a wrong literal is a compile error). **`src/lib/stripe.ts`** mirrors the lazy-client shape of `src/lib/r2.ts` exactly (module-level `let client`, cheap import that never throws on a missing key): `isStripeConfigured()` → `Boolean(process.env.STRIPE_SECRET_KEY)` (the guard callers check before using `stripe()`, mirroring `isR2Configured()`); `stripe()` → lazy memoized singleton built with `new Stripe(process.env.STRIPE_SECRET_KEY ?? "", { apiVersion, typescript: true })`; `appBaseUrl()` → request-less `(AUTH_URL ?? "http://localhost:3000")` trailing-slash-stripped for Checkout/Portal return URLs (with a doc note that Phase 2 should prefer `getBaseUrl(request)` from `src/lib/base-url.ts` where a `Request` is in scope — it also honors `NEXTAUTH_URL` and hardens against Host spoofing); and `priceIdForPlan("monthly" | "yearly")` → the configured `STRIPE_PRICE_ID_MONTHLY`/`STRIPE_PRICE_ID_YEARLY` or `null`. **`src/lib/plan.ts`** holds the free-tier caps from `project-overview.md` §7 — `FREE_ITEM_LIMIT = 50`, `FREE_COLLECTION_LIMIT = 3`, `PRO_ITEM_TYPES = new Set(["file","image"])` (consumed in Phase 2), `interface QuotaCheck { allowed; used; limit }` — plus the pure decision helpers `canCreateItem(userId, isPro)` / `canCreateCollection(userId, isPro)`: Pro ⇒ `{ allowed: true, used: 0, limit: Infinity }` **without** a `count` call; free ⇒ `used = prisma.item/collection.count({ where: { userId } })` (the only impure part, mirroring `getItemStats`/`getCollectionStats`) with `allowed = used < limit`. **`src/lib/plan.test.ts`** (+10 Vitest cases, 290 → **300**) uses the repo's `vi.hoisted` + `vi.mock("@/lib/prisma")` convention to cover the boundary logic at **49/50/51** items and **2/3/4** collections (allowed vs blocked on `used < limit`), the **Pro-bypass** (unlimited return shape + `count` asserted **not called**), the `{ where: { userId } }` call shape, and the constant values. **No Prisma migration** — the billing columns (`isPro`, `stripeCustomerId`, `stripeSubscriptionId`) already exist on `User`; the Stripe env vars were already staged in `.env.example`. `npm run test` (**300/300**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — **route table unchanged** and `ƒ Proxy (Middleware)` intact, confirming this phase added zero surface. Manually verified via `tsx`: `isStripeConfigured()` returns `false` with the key unset and `true` when set, `stripe()` constructs without throwing, `priceIdForPlan("monthly")` returns `null` when unset, and `appBaseUrl()` falls back to `http://localhost:3000`. **Manual user step (not code):** the Stripe Dashboard Test-mode setup (create "DevStash Pro" product + the two recurring prices, grab the Test-mode secret/publishable keys, enable the Billing Customer Portal) and filling the ignored `.env` with those values remain the user's to do; `STRIPE_WEBHOOK_SECRET` waits for Phase 2. **Out of scope and deferred:** Checkout session route, the Stripe webhook + `isPro` sync, actual free-tier enforcement at the create paths, the Pro/upgrade UI, and the Billing Portal link — all Phase 2.
- **Stripe Integration — Integration & UI (Phase 2)** — Turned Phase 1's pure foundation into the working subscription flow: hosted Checkout, the Billing Portal, the signature-verified webhook that is the source of truth for `isPro`, real free-tier enforcement, and the `/settings` Billing UI. **No schema/migration** (billing columns already on `User`); `stripe` SDK + the five `STRIPE_*` vars already in place. **Three API routes**, all shaped like `src/app/api/items/upload/route.ts` and kept **out of the `src/proxy.ts` matcher** so unauthenticated calls return JSON (401), not an HTML redirect: **`POST /api/stripe/checkout`** (`auth()` → `isStripeConfigured()` 503-guard → Zod `{ plan: "monthly" | "yearly" }` → `priceIdForPlan` → load user, **400 if already Pro** → reuse or **create + persist** a Stripe Customer → `checkout.sessions.create({ mode: "subscription", customer, line_items:[{ price, quantity:1 }], client_reference_id, success_url ".../settings?checkout=success", cancel_url ".../settings?checkout=cancelled", allow_promotion_codes:true })` → `{ url }`, base URL via `getBaseUrl(request)`), **`POST /api/stripe/portal`** (require `stripeCustomerId` else 400 → `billingPortal.sessions.create({ customer, return_url })` → `{ url }`), and **`POST /api/stripe/webhook`** (read the **raw** body via `await request.text()`, verify `stripe().webhooks.constructEvent(raw, sig, STRIPE_WEBHOOK_SECRET)` — 400 on missing/bad signature — then switch on `checkout.session.completed` [isPro true + subId], `customer.subscription.updated` [isPro from active/trialing status], `customer.subscription.deleted` [isPro false, subId null], all via **`prisma.user.updateMany({ where: { stripeCustomerId } })`** so an unmatched customer is a safe no-op; **500** on handler error so Stripe retries; else `{ received: true }`). New **`src/app/api/stripe/{checkout,portal,webhook}/route.ts`** + a lazy `S3Client`-style reuse of the Phase-1 `stripe()`/`isStripeConfigured()`/`priceIdForPlan()` helpers and `getBaseUrl()` (`src/lib/base-url.ts`). **Free-tier gating on both layers (defense-in-depth):** the **`createItem`** action (`src/actions/items.ts`) loads `isPro` via **`getCurrentUser()`** and calls **`canCreateItem`**, returning `"Free plan is limited to 50 items. Upgrade to Pro for unlimited."` over cap before any write, and threads `isPro` into the query; the **`createItem` query** (`src/lib/db/items.ts`) gained an `isPro` param and **re-checks `PRO_ITEM_TYPES` (source of truth)** — a file/image type from a crafted payload returns `null` before any DB work (isPro passed as a param, respecting the db-layer-takes-explicit-args convention rather than reaching for the session); the **`createCollection`** action (`src/actions/collections.ts`) mirrors it with **`canCreateCollection`** (`"Free plan is limited to 3 collections…"`); and **`POST /api/items/upload`** loads the user and returns **403** for non-Pro before touching R2. **Billing UI:** new **`src/components/settings/BillingSection.tsx`** (client) — free users see a Pro-perks list + **Go Pro — Monthly ($8/mo)** / **Yearly ($72/yr)** buttons that `POST /api/stripe/checkout` then `window.location.href = data.url` (per-plan pending spinner, error toast); Pro users see a plan note + **Manage billing** (`POST /api/stripe/portal` → redirect); a mount effect reads `?checkout=success|cancelled` and fires a toast via the **`setTimeout(0)` cold-load-safe pattern** copied from `SignInForm` (a `toastShown` ref, then strips the param with `window.history.replaceState`). Wired into **`src/app/settings/page.tsx`** as a `rounded-xl border bg-card p-6` `<section>` between "Editor preferences" and the danger zone, reading `isPro` from the existing `getProfile()`. **Client UX gate:** `CreateItemDialog` gained an `isPro` prop (threaded through **`TopBar`** from all **5 shell pages** — dashboard, items/[type], collections, collections/[id], favorites — each already resolving `getCurrentUser()`); for free users the **File & Image type chips render disabled** with a "Pro" badge + "Upgrade to Pro to add files & images" title, and `handleTypeChange` no-ops on those types. **No session/JWT change** — `getCurrentUser()`/`getProfile()` re-read `isPro` from the DB each request, so a webhook write shows on the next page load. **Testing:** **+15 Vitest cases** (290 → **305**) — `db/items.test.ts` (createItem query rejects file/image when `!isPro` before any DB work; existing calls updated for the new `isPro` arg), `actions/items.test.ts` (blocks a free user at the 50-item cap without writing; passes `isPro=true` through for a Pro user — with `getCurrentUser`/`canCreateItem` mocked via the `vi.hoisted` + `vi.mock` convention), and `actions/collections.test.ts` (blocks at the 3-collection cap; Pro user passes through). `npm run test` (**305/305**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — route table adds `/api/stripe/{checkout,portal,webhook}` as `ƒ`, `ƒ Proxy (Middleware)` intact. **Verified live** (curl + Playwright MCP, seeded free `test-engineer@a5project.com`) against the running dev server + live Neon **development** branch and **test-mode Stripe**: `/api/stripe/checkout` and `/portal` return **401 JSON** unauthenticated; the webhook returns **400** on a missing signature and **400 "Invalid signature."** on a bad one; an **authenticated** checkout returned a real **`checkout.stripe.com`** URL (200 — proving the auth guard, Customer create/persist, and session creation); the free user's **upload returned 403** before R2; the New Item dialog rendered **Snippet/Prompt/Note/Command/Link enabled and File/Image disabled** with the Pro badge + hint; `/settings` showed the Billing section with the Go Pro buttons; and the **collection cap fired live** — creating a 6th collection (user already has 5 > 3) showed the inline "Free plan is limited to 3 collections…" error with **no write** (count stayed 5). **Deferred per user decision:** the live webhook loop (Stripe CLI is **not installed**, `STRIPE_WEBHOOK_SECRET` empty) — `stripe login` + `stripe listen` + `stripe trigger`/test-card completion to confirm the DB flips `isPro` on/off remains a manual step. **Residue:** verifying the authenticated checkout created a **test-mode Stripe Customer** and persisted `stripeCustomerId` on the seeded user (still `isPro:false`, harmless — future checkouts reuse it); left as-is (clearing it needs an `UPDATE` on the dev branch). Drawer/modal assertions used **`[data-slot=dialog-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches it). **Out of scope and deferred:** proration/plan-switch UI beyond the Portal, session/JWT `isPro`, and AI feature gating.
- **Upgrade Page & Header Button** — Gave free users a clear, self-serve path to Pro. Added a subtle **ghost "Upgrade" button** (lucide `Sparkles`, `variant: "ghost"` / `size: "sm"`) to the dashboard **`TopBar`**, rendered **only when `!isPro`** (Pro users don't see it) and placed first in the right-aligned action cluster; `isPro` was already threaded into `TopBar` from all 5 shell pages by the Stripe Phase 2 work, so no page wiring changed. Instead of jumping straight to Stripe Checkout, it links to a **new protected `/upgrade` page** (`src/app/upgrade/page.tsx`) — a server component in the standalone centered layout used by `/settings` (`await connection()`, `getProfile()` resolved first with a defensive `redirect("/sign-in")`, a "Back to dashboard" link), which additionally **`redirect("/settings")` for Pro users** (nothing to upgrade). Added **`/upgrade/:path*`** to the `src/proxy.ts` matcher. The page renders a client **`UpgradePlans`** component (`src/components/upgrade/UpgradePlans.tsx`): a Pro-perks list plus two **radio-style plan cards** — Monthly (**$8/mo**) and Yearly (**$72/yr**, "$6/month" note + a "Save 25%" badge, pre-selected) — driven by `aria-pressed`, then an **"Upgrade to Pro"** button that POSTs the selected period to the existing **`/api/stripe/checkout`** and `window.location.href`'s to the returned Stripe URL (mirroring `BillingSection`'s checkout call; a pending spinner + error toast via `toastManager`). **Checkout return is unchanged** — the API's `success_url`/`cancel_url` still point at **`/settings?checkout=success|cancelled`**, so whether the user starts from `/upgrade` or `/settings` they land back on `/settings` where `BillingSection` fires the confirmation toast and strips the param (confirmed with the user that success/cancel should return to `/settings`). Deliberately uses **app theme tokens** (`border`/`card`/`primary`/`muted-foreground`), not the marketing `--m-*` palette, for consistency inside the authenticated app. **No unit tests** — components/pages only, which the Vitest scope (`src/actions/*`, `src/lib/*`) deliberately excludes; **no schema/migration**, no new deps. `npm run test` (**305/305**), `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — `/upgrade` is `ƒ`, `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, seeded free `test-engineer@a5project.com`) against the running dev server + live Neon **development** branch + test-mode Stripe — the ghost Upgrade button rendered in the header for the free user (transparent-until-hover, first in the cluster); `/upgrade` showed the features + Monthly/Yearly cards (Yearly pre-selected); clicking Monthly flipped `aria-pressed` correctly; the checkout call returned a real **`checkout.stripe.com`** URL (200); and unauthenticated `/upgrade` **307 → `/sign-in?callbackUrl=%2Fupgrade`**. **Caveat:** the Pro-user `redirect("/settings")` guard is code-only, not runtime-exercised (flipping the seeded user to `isPro` needs an `UPDATE` on the dev branch + a revert); it mirrors the `/settings` guard pattern. Left the pre-existing unrelated `.env.example` reorg (AUTH_URL moved up, Upstash section retitled — present at session start) **out of the feature commit** to keep it focused. **Out of scope and deferred:** an in-app upgrade success page (checkout returns to `/settings`), annual/monthly proration UI, and gating the Upgrade button behind free-tier limits (it always shows for free users).
- **Redirect Free Users from Pro-Only Item Listings** — Closed the gap where free users could still **view** the Pro-only `/items/files` and `/items/images` listing pages even though they're blocked from *creating* file/image items (New Item dialog disables those chips; upload route 403s). Added a **view-time redirect** in `src/app/items/[type]/page.tsx`: after the existing `getCurrentUser()` / `redirect("/sign-in")` guard and **before** the `Promise.all` data fetch, resolve the requested slug via the already-imported, `cache()`d **`getSystemItemType(type)`** and **`redirect("/upgrade")`** when `requestedType?.isPro && !currentUser.isPro`. Reuses the existing Pro source of truth (`PRO_TYPE_NAMES = {file, image}`, surfaced as `isPro` on the resolved type) rather than hardcoding slugs; adds **no extra DB round-trip** (getSystemItemType is `cache()`d and already called inside `getItemsByType`); and a bogus slug resolves to `null`, skips the guard, and still falls through to the existing `notFound()` 404. Gives free users a conversion path, mirroring how `/upgrade` itself redirects Pro users to `/settings`. **No unit tests** (page-level logic, outside the Vitest `src/actions/*`/`src/lib/*` scope; the `isPro` flag on the resolved type is already covered by `db/items.test.ts`), **no schema/migration**, no new deps. Sidebar links and Pro badges left as-is (clicking still routes through this page, which now redirects). `npm run test` (**305/305**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean (route table unchanged, `/items/[type]` still `ƒ`, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded free `test-engineer@a5project.com`) against the live Neon **development** branch — `/items/files` → **`/upgrade`**, `/items/images` → **`/upgrade`** (landing on `/upgrade` proves the user is both authenticated and free, since that page redirects Pro users to `/settings` and unauth to `/sign-in`), `/items/snippets` (non-Pro) still renders (200), and `/items/bogus` still **404**. The Pro-user-still-sees-the-page path is code-only (guard's `!currentUser.isPro`), not runtime-exercised. **Out of scope:** unchanged sidebar/badge behavior and any creation-gating changes.
- **AI Auto-Tagging** — First AI feature, so it establishes the reusable OpenAI foundation plus the tagging UX. A **Pro-only "Suggest Tags" button** (Sparkles, ghost) sits beside the tags input in the New Item dialog and the item-drawer edit form; clicking it sends the item's title + content to OpenAI **`gpt-5-nano`** and returns 3–5 freeform tags as accept (✓) / reject (✗) badges — accepted tags append to the local comma-string tags field (persisted on save/create). Gated **both** in the UI (button hidden for free users) and server-side. **Authoritative spec:** `context/features/ai-auto-tag-spec.md`, which **overrides** the older `docs/ai-integration-plan.md` on the OpenAI call. **Foundation:** installed **`openai`** (one dep); **`src/lib/openai.ts`** — lazy-memoized client + `isOpenAIConfigured()` + `aiModel()` (env `OPENAI_MODEL`, defaults `gpt-5-nano`) mirroring `stripe.ts`; **`canUseAi(isPro)`** in `plan.ts`; an **`ai` rate-limit** entry (**20/hr per userId** — AI costs money, so keyed by user not IP) in `rate-limit.ts`; **`src/lib/ai-tags.ts`** — pure **`parseSuggestedTags`** accepting **both** `{ "tags": [...] }` and bare `[...]`, lowercasing, trim/dedupe, capped at 5, `[]` on any parse failure; **`src/lib/validations/ai.ts`** — `autoTagSchema` (title required/≤300, content optional default `""`, `AUTO_TAG_CONTENT_LIMIT = 2000`). **Server action** `generateAutoTags` (`src/actions/ai.ts`, follows `items.ts` to the letter): `auth()` → `isOpenAIConfigured()` guard → Zod `safeParse` (`z.flattenError`) → `getCurrentUser()` + `canUseAi` Pro gate → `checkRateLimit("ai", userId)` → truncate content to 2000 → **Responses API** `openai().responses.create({ model, instructions, input, text: { format: { type: "json_object" } } })` → read `response.output_text` → `parseSuggestedTags` → `ActionResult<string[]>`; try/catch → generic. **Chose Responses API + `json_object` manual parse over Chat Completions (returns empty content with gpt-5-nano) and `zodTextFormat` structured output (blows length limits) — per the spec's hard-won gotchas.** **UI:** new **`SuggestTags.tsx`** (client) shared by both forms — renders **nothing for free users** (UI gating), a ghost Sparkles button (spinner while pending, disabled when the title is blank), and accepted/rejected suggestion `Badge`s with `Check`/`X` `icon-xs` buttons (styled after `CopyButton`); filters out suggestions already on the item, toasts on error / "no new tags". Wired into `CreateItemDialog` (already had `isPro`) and `ItemDrawerEdit` (gained an `isPro` prop); **`isPro` threaded through a new `ItemDrawerProvider` prop** across all **5 shell pages** (dashboard, favorites, items/[type], collections, collections/[id] — each already resolves `currentUser.isPro`). Accepted tags append to the existing comma-string state (deduped) and ride the existing create/update save path — **no new persistence**. **Confirmed decisions:** skipped the global `AI_FEATURES_ENABLED` flag (gate only on `isPro` + `isOpenAIConfigured()`); threaded `isPro` via the provider rather than the item API payload. **Testing:** **+22 Vitest cases** (305 → **327**) — `ai-tags.test.ts` (both shapes, lowercase, dedupe, cap-5, non-JSON→`[]`, non-string filtering), `validations/ai.test.ts` (title required/trimmed/max, content default, over-max), and `actions/ai.test.ts` (unauth/not-configured/non-Pro/rate-limited all without an OpenAI call, validation issues, parsed+normalized success, **content truncated to 2000 + input contains "json"**, thrown→generic; deps mocked via `vi.hoisted`+`vi.mock`). **No schema/migration.** `npm run test` (**327/327**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — route table unchanged (server action, not a route), `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch + real gpt-5-nano: as a **free** user the Suggest Tags button is **absent** in both the dialog and the drawer edit form; after temporarily flipping the seeded user to **`isPro=true`** (with the user's explicit approval per the no-unprompted-`UPDATE` rule) the button appeared and returned **5 real tags** (`react, hooks, debounce, useeffect, settimeout`, lowercased), **accepting** `react` + `hooks` set the field to `"react, hooks"` and **rejecting** `debounce` removed it (leaving `useeffect, settimeout`). The `isPro` flip was **reverted to `false`** afterward (baseline restored, no throwaway items created). **Bug caught only in the browser, not by the unit tests:** the first live run returned **`400 … input messages must contain the word 'json'`** — OpenAI's `json_object` format requires "json" in the **input** itself, not just `instructions`. Fixed by embedding the word in the `promptInput` and added a regression assertion (`expect(arg.input).toMatch(/json/i)`). Dialog/drawer assertions used **`[data-slot=dialog-content]`** / **`[data-slot=sheet-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches it). **Out of scope and deferred:** the other three AI features (summary, explain-code, prompt-optimizer), server-side persistence of suggestions, a global AI kill-switch, and streaming.
- **Language Dropdown (Above Content)** — Replaced the free-text "Language" `Input` with a curated **dropdown**, moved **above** the Content editor, in both the New Item modal (`CreateItemDialog`) and the drawer edit form (`ItemDrawerEdit`); selecting a language drives Monaco highlighting live (already the behavior — preserved through the input→select swap). **Command items no longer show a language picker at all** — commands are effectively always Shell / Bash, per user feedback. **`src/lib/code-types.ts`** gained the pure/testable pieces: `EDITOR_LANGUAGES` (20 curated options, values are Monaco language ids; "" = Plain text), `normalizeLanguage` (canonicalizes a case-insensitive curated match, e.g. `TypeScript` → `typescript`, so a controlled `<select>` finds its option), `languageOptions` (prepends any current value **not** in the curated list — e.g. a seeded command's `bash` — so the `<select>` reflects it instead of silently resetting), `showsLanguagePicker` (true only for `snippet` — a code type without a **fixed** language), and `effectiveLanguage` (a `FIXED_LANGUAGES` map forces `command` → `shell` regardless of any stored value; other types use their own language). `editorLanguageLabel` now prefers the curated friendly label (`typescript` → "TypeScript", `shell` → "Shell / Bash"). New shared **`LanguageSelect.tsx`** (native `<select>` matching the `EditorPreferencesForm` styling) avoids duplicating the class string across both forms. The **read-only** drawer view (`ItemDrawerDetail`) routes its `CodeEditor` `language`/label through `effectiveLanguage` too, so a command's content box also highlights + labels as Shell / Bash (previously showed the raw stored `bash`). New commands persist **no** language (highlighting is derived, not stored); existing values are left untouched on save. **Testing:** **+12 Vitest cases** (327 → **339**) in `src/lib/code-types.test.ts` covering `EDITOR_LANGUAGES` shape, `normalizeLanguage`, `languageOptions` (prepend path), `showsLanguagePicker`, `effectiveLanguage` (command→shell), and the enhanced `editorLanguageLabel`. **No schema/migration**, no new deps. `npm run test` (**339/339**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean (route table unchanged, `ƒ Proxy (Middleware)` intact). **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`) against the live Neon **development** branch — the New Item dialog and drawer edit form both render the dropdown **above** Content; selecting **TypeScript** flipped the editor header live Snippet → TypeScript; a seeded `typescript` snippet **pre-selected** TypeScript (no duplicate option); a seeded command storing the non-curated `bash` showed `bash` prepended + selected in the (pre-change) picker, and after the command-picker removal shows **no dropdown** with the editor labeled/highlighted **"Shell / Bash"** across the create, edit, and read-only views; **Snippet** still shows the dropdown (default "Plain text"). No data mutated (all edits cancelled). Dialog/drawer assertions used **`[data-slot=dialog-content]`** / **`[data-slot=sheet-content]`**, never `[role=dialog]`. **Out of scope and deferred:** applying the language selector to non-code types, per-language editor overrides, and a searchable/typeahead language picker (native `<select>` is sufficient for the curated list).
- **AI Summary → Description ("Describe" button)** — Second AI feature and the direct sibling of the shipped **AI Auto-Tagging** feature; adds a Pro-only **"Describe"** button (Sparkles icon + label) beside the Description field in both the **New Item dialog** (`CreateItemDialog`) and the **drawer edit form** (`ItemDrawerEdit`) that generates a concise **1–2 sentence** description from the item's current (unsaved) inputs. Reuses the auto-tag foundation wholesale — `src/lib/openai.ts` (lazy client + `isOpenAIConfigured`/`aiModel`), `canUseAi`, the shared **`"ai"` rate-limit bucket** (20/hr/user), and the `ActionResult` action pattern — and mirrors its Pro-gating (hidden in the UI for free users **and** enforced in the action). **Validation** (`src/lib/validations/ai.ts`, extended): `summarySchema` (title required/trimmed/≤300, content optional default `""`) + `SUMMARY_CONTENT_LIMIT = 4000` (the component folds the best-available body into `content`). **Pure helper** (new `src/lib/ai-summary.ts`): `cleanSummary(raw)` — trims, collapses whitespace/newlines to single spaces, strips one pair of wrapping quotes (straight or curly), caps at `SUMMARY_MAX_CHARS = 300`, `""` on empty (never throws). **Key divergence from tags:** the summary uses the Responses API with **plain-text output — no `json_object` format** — so there's no JSON parse and it **sidesteps the "json must appear in the input" gotcha** entirely; `cleanSummary` just normalizes text. **Server action** `generateSummary` (`src/actions/ai.ts`): `auth()` → `isOpenAIConfigured()` → `summarySchema.safeParse` (flatten issues) → `getCurrentUser()` + `canUseAi` Pro gate → `checkRateLimit("ai", userId)` → truncate content to 4000 → `openai().responses.create({ model, instructions, input })` (input is `Title: …\n\nContent:\n…`, or `Title: …` alone when there's no body) → `cleanSummary(output_text)` → `ActionResult<string>` (empty result → friendly error; thrown → generic). **Component** (new `src/components/dashboard/SummarizeDescription.tsx`, modeled on `SuggestTags.tsx`): `if (!isPro) return null`; body = `content?.trim() || url?.trim() || fileName?.trim() || ""` (so **all item types** work — content for text types, url for links, fileName for files/images, else title alone); a ghost **`size="xs"` Sparkles + "Describe"** button absolutely positioned top-right of the `relative` Description wrapper, **disabled while the title is blank**; on success it renders a **preview block with Use / Dismiss** — **Use** calls `onApply(summary)` (fills the field) then clears, **Dismiss** just clears — so **nothing overwrites the Description until the user confirms** (the confirmed UX decision). Wired into both forms by making each Description `<div>` `relative` and passing `title`/`content`/`url`/`fileName`/`onApply={setDescription}` (both forms already receive `isPro`; **no page changes**). **Testing:** **+22 Vitest cases** (339 → **361**) — `src/lib/ai-summary.test.ts` (`cleanSummary`: trim, collapse, strip straight/curly quotes, don't strip inner quotes, cap, empty), extended `src/lib/validations/ai.test.ts` (`summarySchema` title/content rules), and extended `src/actions/ai.test.ts` (`generateSummary`: unauth / not-configured / non-Pro / rate-limited each **without** an OpenAI call, validation issues, cleaned-summary success with **no `text.format`** asserted, empty-result error, content truncation to 4000, **title-only input** when no body, thrown → generic). **No schema/migration**, **no new deps** (reuses `openai`, existing UI primitives). `npm run test` (**361/361**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — **route table unchanged** (server action, not a route), `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`, live Neon **development** branch + real gpt-5-nano): the seeded user was already `isPro=true` (residue), so the **Pro path** was exercised first — the "Describe" button rendered, was **disabled with an empty title**, enabled once a title was typed, and clicking it returned a real 1–2 sentence summary from the **title alone** ("useThrottle hook" → *"React custom hook that throttles a function call by enforcing a minimum delay between invocations. Use it to rate-limit high-frequency events…"*); the **preview rendered without touching the still-empty Description**, and **Use** filled the field and cleared the preview (verified by reading the textarea value + confirming the preview block was gone). Then `isPro` was flipped to **`false`** (which also **restored the correct seed baseline** — the residual `true` predated this feature) and the dialog re-opened to confirm **0 Describe buttons** for a free user. A read-back confirmed the dev DB is clean: `isPro=false`, **0 throwaway items** (Create was never clicked), 18 total items. Assertions used **`[data-slot=dialog-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter). **Caveats:** the shared component was click-exercised only through the **New Item dialog** — the drawer edit form uses the identical component with mirror wiring (typechecks + builds clean) but wasn't separately driven in-browser; **Dismiss** (a symmetric no-apply clear) and Monaco-content-fed summaries (Monaco typing was flaky in the harness, so the title-only path was used) weren't separately exercised to save OpenAI calls. **Post-plan tweak:** the button started icon-only per the "icon button" wording, then the user asked to add a **"Describe"** text label, so it now matches the `SuggestTags` "Suggest tags" style (icon + text, `size="xs"`, no `aria-label` since the visible text is the accessible name). **Out of scope and deferred:** server-side persistence of suggestions, streaming, and the other two AI features (explain-code, prompt-optimizer).
- **AI Explain Code** — Third AI feature (after Auto-Tagging and Summary), and the last of the drawer-facing trio. Adds a **Pro-only "Explain" button** (Sparkles) to the code editor's window-controls header (next to Copy) in the **item drawer read view only**, for **snippet and command** types only (the two code-ish types; prompts/notes/links/files/images are excluded as human-readable or non-code, and use MarkdownEditor / plain boxes, not CodeEditor). Clicking sends the item's title + code + language to OpenAI **`gpt-5-nano`** and returns a concise **~200–300 word Markdown explanation**; the editor header then gains **Code / Explain tabs** to toggle between the Monaco editor and the rendered markdown **in the same container**. Explanations are **not persisted** — regenerated on each click. Reuses the shipped AI foundation wholesale (`src/lib/openai.ts` lazy client + `isOpenAIConfigured`/`aiModel`, `canUseAi`, the shared **`"ai"` rate-limit bucket** 20/hr/user, `ActionResult` pattern, `src/actions/ai.ts`) and mirrors the **Summary** feature's **plain-text Responses API** path — so, like summary and unlike auto-tag, there's **no `json_object` format and no "json must appear in input" gotcha**. **Validation** (`src/lib/validations/ai.ts`): `explainCodeSchema` (title required/≤300; **content required** min-1 — unlike the tag/summary schemas, since you can't explain empty code; optional `language` ≤50) + `EXPLAIN_CONTENT_LIMIT = 4000`. **Pure helper** (new `src/lib/ai-explain.ts`): `cleanExplanation(raw)` — trims and **unwraps a single wrapping fenced code block** (```` ```markdown … ``` ````) the model occasionally puts around the whole answer, but **keeps internal newlines** (Markdown needs them, so unlike `cleanSummary` it does not collapse whitespace); `""` on empty, never throws. **Server action** `explainCode` (`src/actions/ai.ts`, follows `generateSummary` to the letter): `auth()` → `isOpenAIConfigured()` → `explainCodeSchema.safeParse` (flatten issues) → `getCurrentUser()` + `canUseAi` Pro gate → `checkRateLimit("ai", userId)` → truncate content to 4000 → `openai().responses.create({ model, instructions, input })` (input is `Title: …\nLanguage: …\n\nCode:\n…`, language line omitted when blank) → `cleanExplanation(output_text)` → `ActionResult<string>` (empty → friendly error; thrown → generic). **UI** (`src/components/dashboard/CodeEditor.tsx`): a new optional **`explain?: { isPro; title }`** prop enables the feature (omitted by the edit/create forms, so it only appears in the drawer read view). When present it renders the **Explain** button (ghost `size="xs"`, **Loader2 spinner** while generating) for Pro users, or — for **free users** — a **Crown + native `title` tooltip** ("AI features require Pro subscription") on a `<span>` (not a disabled `<button>`, which wouldn't reliably show the title on hover; the native-`title` approach matches the app's existing Pro-gating pattern from the CreateItemDialog file/image chips). After a successful generate, `explanation` state is set and **Code/Explain `TabButton`s** (a small local component mirroring `MarkdownEditor`'s) replace the macOS dots on the header's left; the body swaps between the **Monaco editor (kept mounted but `hidden`** so its fluid height + state survive toggling) and a `.markdown-preview` container rendering the markdown via the existing **`react-markdown` + `remark-gfm`** (same stack as `MarkdownEditor`, so no new dep). Errors surface via `toastManager`. **Wiring:** `ItemDrawerDetail` gained an `isPro` prop and passes `explain={{ isPro, title }}` to its readonly `CodeEditor` (only in the snippet/command `isCodeType` branch); `isPro` is threaded `ItemDrawerProvider` (which already receives it from all 5 shell pages for the earlier AI features) → `ItemDrawerDetail`. **Testing:** **+21 Vitest cases** (361 → **382**) — `src/lib/ai-explain.test.ts` (`cleanExplanation`: trim, preserve internal newlines, unwrap tagged/untagged wrapping fence, **don't** unwrap an inner fence, empty), extended `src/lib/validations/ai.test.ts` (`explainCodeSchema`: trim + language default, keep language, **content required**, blank title, over-max), and extended `src/actions/ai.test.ts` (`explainCode`: unauth / not-configured / non-Pro / rate-limited each **without** an OpenAI call, validation issues on empty content, cleaned-explanation success with **no `text.format`** + `Language:` line asserted, empty-result error, content truncation to 4000, **language line omitted** when none, thrown → generic; deps mocked via the `vi.hoisted` + `vi.mock` convention). **No schema/migration**, **no new deps** (reuses `openai`, `react-markdown`, `remark-gfm`, existing UI primitives). `npm run test` (**382/382**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — **route table unchanged** (server action, not a route), `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`, live Neon **development** branch + real gpt-5-nano): as a **free** user (baseline `isPro=false`), the **snippet** and **command** drawers showed the **Crown span** with `title="AI features require Pro subscription"` (text "Explain") next to Copy — no working button — while a **prompt** drawer (non-code, MarkdownEditor) had **neither** Explain nor Crown, confirming the type gating. After flipping the seeded user to **`isPro=true`** (with the user's explicit approval per the no-unprompted-`UPDATE` rule), the working **Explain button** appeared (no Crown); clicking it made a real gpt-5-nano call that returned a relevant ~250-word Markdown explanation of the `useDebounce` hook, the **Code/Explain tabs** rendered with **Explain auto-selected**, the markdown displayed (`<p>`/`<ul>`/`<li>`/`<code>` via react-markdown), and toggling back to **Code** re-showed the Monaco editor (unhidden) with the explanation view unmounted — **0 console errors** throughout. `isPro` was then **reverted to `false`** (baseline restored; no items persisted, since explanations aren't saved). Drawer assertions used **`[data-slot=sheet-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter). **Out of scope and deferred:** persisting explanations, streaming the response, explaining non-code types, and the last AI feature (prompt optimizer).
- **AI Prompt Optimizer** — Fourth and final planned AI feature (after Auto-Tagging, Summary, Explain Code). Adds a **Pro-only "Optimize" button** (Sparkles) in the item drawer's editor header for **prompt** items only (the spot the "Explain" button sits for snippets/commands) — but prompts render via **`MarkdownEditor`**, not `CodeEditor`, so the button lives in `MarkdownEditor`'s header. Clicking it sends the prompt's title + content to OpenAI **`gpt-5-nano`** and returns a refined version; the drawer then reveals **Prompt / Optimized** tabs (mirroring Explain's Code/Explain reveal) showing the rewrite with a **"Use this prompt" / "Discard"** confirm bar. **Apply behavior (user decision): confirming loads the refined text into the drawer's edit mode** with the Content field pre-filled, so the user reviews and presses **Save** — **reusing the existing `updateItem` path with zero new persistence code** (rather than persisting directly from the read view). Mirrors the shipped **`explainCode`** feature layer-for-layer and reuses its whole foundation (`src/lib/openai.ts` lazy client + `isOpenAIConfigured`/`aiModel`, `canUseAi`, the shared **`"ai"` rate-limit bucket** 20/hr/user, `ActionResult` pattern) — and, like Summary/Explain and unlike Auto-Tag, uses the **plain-text Responses API** (no `json_object` format, so no "json must appear in input" gotcha). **Validation** (`src/lib/validations/ai.ts`): `promptOptimizeSchema` (title required/≤300; **content required** min-1 — you can't optimize an empty prompt) + `PROMPT_OPTIMIZE_CONTENT_LIMIT = 4000`. **Pure helper** (new `src/lib/ai-prompt-optimizer.ts`): `cleanOptimizedPrompt(raw)` — trims and **unwraps a single wrapping fence** but **keeps internal newlines** (a prompt is multi-line), modeled on `ai-explain.ts`; `""` on empty, never throws. **Server action** `optimizePrompt` (`src/actions/ai.ts`, follows `explainCode` exactly): `auth()` → `isOpenAIConfigured()` → `promptOptimizeSchema.safeParse` (flatten issues) → `getCurrentUser()` + `canUseAi` Pro gate → `checkRateLimit("ai", userId)` → truncate content to 4000 → `openai().responses.create({ model, instructions, input })` → `cleanOptimizedPrompt(output_text)` → `ActionResult<string>` (empty → friendly error; thrown → generic). **UI** (`src/components/dashboard/MarkdownEditor.tsx`): a new optional **`optimize?: { isPro; title; onApply }`** prop enables the feature (omitted by edit/create forms, so notes and edit forms are unchanged). When present it renders the **Optimize** button (ghost `size="xs"`, **Loader2** spinner while generating) for Pro users, or — for **free** users — a **Crown + native `title` tooltip** ("AI features require Pro subscription") on a `<span>` (matching the CreateItemDialog/CodeEditor Pro-gating pattern). After a successful generate, `optimized` state is set and **Prompt/Optimized `TabButton`s** replace the Write/Preview tabs; the body swaps to a `.markdown-preview` render of the optimized text (via the existing **`react-markdown` + `remark-gfm`**) with the confirm bar. **Wiring:** `ItemDrawerDetail` gained an `onUseOptimized` prop and passes `optimize` to its readonly `MarkdownEditor` **only when `item.itemType.name === "prompt"`** (notes excluded — the inverse of the same literal gate); `ItemDrawer` holds a new **`editContentOverride`** state that `onUseOptimized` sets before flipping to edit mode (cleared on the normal Edit/cancel/save/open transitions so a plain edit never reuses it), threaded as an **`initialContent`** prop into `ItemDrawerEdit` (which seeds its content `useState(initialContent ?? item.content ?? "")`); `isPro` reaches the read view through the `ItemDrawerProvider` prop the earlier AI features already thread from all 5 shell pages. **Testing:** **+20 Vitest cases** (382 → **402**) — `src/lib/ai-prompt-optimizer.test.ts` (`cleanOptimizedPrompt`: trim, preserve internal newlines, unwrap tagged/untagged wrapping fence, **don't** unwrap an inner fence, empty), extended `src/lib/validations/ai.test.ts` (`promptOptimizeSchema`: trim, **content required**, blank title, over-max), and extended `src/actions/ai.test.ts` (`optimizePrompt`: unauth / not-configured / non-Pro / rate-limited each **without** an OpenAI call, validation issues on empty content, cleaned-prompt success with **no `text.format`** asserted, empty-result error, content truncation to 4000, thrown → generic; deps mocked via the `vi.hoisted` + `vi.mock` convention). **No schema/migration**, **no new deps** (reuses `openai`, `react-markdown`, `remark-gfm`, existing UI primitives). `npm run test` (**402/402**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — **route table unchanged** (server action, not a route), `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`, live Neon **development** branch + real gpt-5-nano): as a **free** user the prompt drawer showed the **Crown** "Optimize" affordance (title "AI features require Pro subscription") with **no** working button. After flipping the seeded user to **`isPro=true`** (with the user's explicit approval per the no-unprompted-`UPDATE` rule), the working **Optimize** button appeared; clicking it returned a real refined prompt in the revealed **Optimized** tab, **"Use this prompt"** switched the drawer to **edit mode with Content pre-filled** (title unchanged), and **Save** persisted via `updateItem` (confirmed by SQL: content updated to the 1681-char optimized prompt), while **"Discard"** cleanly reverted to Preview with nothing saved — **0 console errors**. **Bug caught only in the browser, not by the unit tests** (which mock the model output): the first live run had gpt-5-nano **echo the `Title:`/`Prompt:` input labels into the optimized text** — because, unlike explain/summary, the output *is* a rewrite of the input and mirrors its structure. Fixed by **reframing the input as a plain instruction** (`Improve this prompt (titled "…"): …`, no mirror-able field labels) plus hardening the instructions to output only the prompt body; re-verified in-browser the echo was gone (output started cleanly with "You are a senior software engineer…"). The seeded prompt's **original content + `updatedAt` were restored** and **`isPro` reverted to `false`** afterward (SQL-confirmed baseline). Drawer assertions used **`[data-slot=sheet-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter). **Out of scope and deferred:** persisting optimized suggestions, streaming, optimizing non-prompt types, and applying the feature to the create dialog / edit form (read view only, mirroring Explain).
- **All Items Index Page (`/items`)** — Built the missing **`/items` index page** so the sidebar's existing **"All items"** `NavRow` (`Sidebar.tsx:89` → `/items`, which had 404'd since only the dynamic `/items/[type]` route existed) resolves to a real overview of every item the signed-in user owns. Per the user's confirmed decisions it renders **three sections**: a combined **Items** card grid of the 5 text/url types (snippet, prompt, note, command, link, pinned-first then most-recently-updated) that **lazy-loads more**; a capped **Images** thumbnail gallery preview with a **"View all →"** link to `/items/images`; and a capped **Files** single-column list preview with a **"View all →"** link to `/items/files`. Files & image were pulled into their own sections because they render with the specialized `ImageCardButton` gallery / `FileListRow` list rather than the standard card. **The combined section lazy-loads** (rather than paginating or capping) because there's no single dedicated page to "View all" into for a mixed-type list — the third clarifying answer, chosen by the user over show-all / cap-no-link. **Data layer** (`src/lib/db/items.ts`, reusing the shared `itemSelect`/`toDashboardItem`/`getSystemItemType`/`buildPageInfo`/`ITEMS_PER_PAGE`): **`getCardItemsPage(userId, page = 1)`** → `{ items, pagination }`, owner-scoped `where: { userId, itemType: { name: { notIn: ["file", "image"] } } }` (system type names are singular/lowercase, so this excludes the two special sections), `count` **first** (so `buildPageInfo` clamps out-of-range pages before `skip`), then a `skip`/`take` `findMany` `orderBy: [{ isPinned: "desc" }, { updatedAt: "desc" }]` — the same pinned-first order as `getItemsByType`, so lazy batches stay consistent with page 1; and **`getTypePreview(slug, userId, limit)`** → `{ type, items, total } | null`, resolving via the cached `getSystemItemType` (null on unknown slug) then a parallel `count` + capped `findMany` so the header and "View all" reflect the true total beyond the preview. Added **`ITEMS_INDEX_PREVIEW_LIMIT = 20`** to `src/lib/pagination.ts` (user bumped it from the initial 8). **Server action** `loadMoreCardItems(page)` (`src/actions/items.ts`) → `ActionResult<{ items, hasNext, nextPage }>` — `"use server"`, `auth()` guard (no DB touch when unauthenticated), coerces the page via `parsePageParam` (junk → 1), delegates to `getCardItemsPage`, and returns `nextPage: pagination.page + 1` off the **clamped** page; try/catch → generic. **Client component** `LoadMoreItems.tsx` (`"use client"`) — renders the `ItemCardButton` grid and lazy-loads the next page via an **IntersectionObserver sentinel** (auto-loads when scrolled into view, `rootMargin: "200px"`) **plus a visible "Load more" button** as the accessible fallback / manual trigger (Loader2 spinner while pending, a `loadingRef` guards against overlapping loads); appends on success, advances the page, hides the sentinel/button when `!hasNext`, and toasts on error via `toastManager`. **Page** `src/app/items/page.tsx` (server component) mirrors the `/items/[type]` shell exactly — `await connection()`, `getCurrentUser()` first with a defensive `redirect("/sign-in")`, `SidebarProvider > EditorPreferencesProvider > ItemDrawerProvider(isPro) > (Sidebar + TopBar + main.space-y-8)`, sidebar data + the three item queries + editor prefs in one `Promise.all` — with `metadata.title = "All Items"`, an `<h1>` "All items", **no Pro-redirect guard** (it's a mixed overview for everyone; free users simply own no files/images, so those sections hide), and a global dashed **"No items yet."** empty state when all three totals are 0. Each section renders only when non-empty; the Images/Files "View all →" link appears only when `total > ITEMS_INDEX_PREVIEW_LIMIT`. `/items` was **already protected** by the `/items/:path*` proxy matcher (`:path*` matches zero segments, per the `/collections` precedent), so **no proxy change**; the static `/items/page.tsx` and dynamic `/items/[type]/page.tsx` coexist fine in the App Router. **Testing:** **+9 Vitest cases** (402 → **411**) — `src/lib/db/items.test.ts` (`getCardItemsPage`: owner-scoped `where` with `notIn: ["file","image"]`, pinned-first `orderBy`, count-scoped-like-rows, `skip`/`take`, clamp; `getTypePreview`: owner+type `where`, `take: limit`, total from count, null-on-unknown-slug without an item query) and `src/actions/items.test.ts` (`loadMoreCardItems`: unauth guard without a DB touch, `parsePageParam` coercion, `{ items, hasNext, nextPage }` passthrough, thrown → generic; the `@/lib/db/items` mock gained a `getCardItemsPage` spy). Components/page are out of the Vitest scope. **No schema/migration**, **no new deps**. `npm run test` (**411/411**), `tsc --noEmit`, `npm run lint` (only the pre-existing untouched `prototypes/homepage/script.js` warning), and `npm run build` all clean — route table gains **`/items` as `ƒ`**, `/items/[type]` still `ƒ`, `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, seeded `test-engineer@a5project.com`, live Neon **development** branch): unauthenticated `/items` **307 → `/sign-in?callbackUrl=%2Fitems`**; authenticated, the page rendered `<h1>` "All items" (title "All Items"), the **Items** section showed all **18** seeded card items (4 snippets + 3 prompts + 5 commands + 6 links) with **no** Load more (18 < 20) and Images/Files sections **hidden** (0 owned); a card click opened the **drawer in place** (URL stayed `/items`, Monaco rendered) with **zero** invalid `<button>`-in-`<button>` (the `[role="button"]`-card-wrapping-real-`<button>` copy/favorite pattern is valid); the sidebar **"All items"** link is now `aria-current="page"` and works. **The data-dependent paths were exercised with the user's explicit approval** (per CLAUDE.md's no-unprompted-INSERT/UPDATE rule) by inserting **28 combined / 22 image / 3 file** throwaway rows (`zzverify_` prefix) + flipping `isPro=true` on the Neon **development** branch: on a short viewport the SSR delivered **exactly 20** cards + a **"Load more"** button (proving page-1 caps at `ITEMS_PER_PAGE`, not a full dump), and clicking it appended the remaining **8** (→ 28, button gone, `hasNext` false); the **Images** section capped at **20** thumbnails with a **"View all →"** link that navigated to `/items/images`, while **Files** (3) rendered uncapped with **no** link — confirming both `total > limit` branches. (On a tall viewport the IntersectionObserver auto-loads the next batch immediately — true infinite-scroll — which is why the button only appears when the sentinel is below the fold.) **All throwaway data was reverted** (`DELETE … WHERE id LIKE 'zzverify_%'`, `isPro` back to `false`) and a read-back **confirmed the dev DB at its exact 18-item baseline** (0 leftover rows, `isPro=false`). The 20 console errors during the image test were the placeholder `example.invalid` thumbnail URLs failing to load — expected, not a page bug. Drawer/section assertions used **`[data-slot=sheet-content]`**, never `[role=dialog]` (the always-present mobile sidebar `<aside>` matches the latter). **Out of scope and deferred:** pagination controls on the combined section (lazy-load instead), a combined "all types" dedicated page, per-type sections beyond files/images, and any changes to the existing `/items/[type]` pages.
- **About Page** — Turned the static prototype `prototypes/homepage/about.html` into the real public About page at **`/about`**, replacing the `<h1>About</h1>` placeholder ([src/app/about/page.tsx](src/app/about/page.tsx)) — rebuilt as React **server components** with Tailwind + the existing marketing primitives (reproducing the prototype's look/content, not copying its raw HTML/CSS). The page is a server component wrapped in the existing **`MarketingShell`** (which already provides `MarketingNav`, `Footer`, the `.marketing` token scope, and the ambient glow), **public** (no `auth()` redirect, no `proxy.ts` matcher change — unlike the homepage, signed-in users may view it, so it builds **`○ (Static)`** rather than `ƒ`), with `metadata` title `"About"` + the prototype's `description`. **Two shared, presentational extractions reused by the homepage (the DRY core of the feature):** **`FeatureCard`** (`src/components/marketing/FeatureCard.tsx`) — the tinted card pulled out of the inline markup in `FeatureGrid.tsx`, props `{ title, description, accent, icon? }` with the **icon tile rendered only when `icon` is present** (the About "Who it's for" grid has none); `FeatureGrid` now maps `FEATURES` onto `<Reveal><FeatureCard …/></Reveal>`, keeping the identical hover/glow/top-bar classes. **`CtaBand`** (`src/components/marketing/CtaBand.tsx`) — the closing call-to-action band extracted from the inline block in `src/app/page.tsx`, props `title`/`subtitle`/`ctaLabel`/`ctaHref`, wrapped in `Reveal`; the homepage now renders `<CtaBand …>` with its original copy, and About reuses it ("Ready to Organize Your Knowledge?" / "Everything you know as a developer, in one fast, searchable home." / "Start stashing — free" → `/register`). **Data arrays** added to `src/components/marketing/constants.ts` following the existing `FEATURES` pattern (accents reference `--m-*` tokens, never hardcoded hexes): `ABOUT_STATS` (`7`/`1`/`∞`/`0`), `ABOUT_VALUES` (reuses the existing `MarketingFeature` type, so **with** icons — Fast `Zap`/`--m-snippet`, Searchable `Search`/`--m-command`, AI-enhanced `Sparkles`/`--m-prompt`, Developer-first `Code`/`--m-note`; `Zap` added to the lucide import), and `ABOUT_AUDIENCES` (no icon — everyday `--m-snippet`, AI-first `--m-prompt`, content creator `--m-note`, full-stack `--m-url`). **Sections** (composed in `page.tsx` with one small local `SectionHead` helper): page header (`pill` "Our story" + gradient `<h1>` "Built for developers who / lose things" reusing Hero's pill + gradient-text patterns, in `Reveal`), mission (narrow `max-w-[760px]` prose — `<h2>`, paragraphs with inline `<code>`/`<strong>`, and a brand-accent left-border `<blockquote>`, all styled with per-element Tailwind arbitrary-value utilities rather than a `.prose` parent selector), stats row (4 `stat-card`s with gradient numbers, **4→2 cols** under 720px), "What we optimize for" (section head + 4 `FeatureCard`s **with** icons), "Who it's for" (section head + 4 `FeatureCard`s **without** icons), and the reused `CtaBand`. The prototype's About-only styles were ported to Tailwind **arbitrary-value utilities** — **no new `globals.css` classes** (consistent with the homepage rebuild). Only client dependency is the existing `Reveal`. **No unit tests** (page + presentational components only, which the Vitest scope `src/actions/*`/`src/lib/*` deliberately excludes); **no schema/migration**, no new deps. `npm run lint` clean (only the pre-existing untouched `prototypes/homepage/script.js` warning), `npm run test` **411/411**, and `npm run build` clean — `/about` is `○ (Static)`, homepage `/` still `ƒ`, `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, against the running dev server): `/about` returned 200 with title "About" and **0 console errors**; the a11y snapshot confirmed all six sections with correct content and the CTA linking to `/register`; screenshots confirmed the gradient headline, the `.txt` code chip / bold / accent-bordered blockquote in the mission, the gradient stat numbers (`7 / 1 / ∞ / 0`), the value cards' colored **icon tiles** (blue `Zap`, cyan `Search`, orange `Sparkles`) vs the audience cards' **icon-less** colored top bars, and that `Reveal` fades sections in on scroll; at **390px** there was no horizontal overflow with stats collapsing **4→2** and the feature grids **3→1**. The **homepage was confirmed to render identically** after the refactor via its logged-out SSR output — `CtaBand` (heading + subtitle + CTA) and the `FeatureGrid` head + all six `FeatureCard` titles all present, every CTA → `/register` (the browser session was authenticated, so `/` redirected to `/dashboard`, hence the SSR-output check instead of a browser view). **Out of scope and deferred:** the other interior marketing pages (Blog/Docs/Support), any auth/dashboard changes, and real content beyond the prototype copy.
- **Blog Listing Page** — Turned the static prototype `prototypes/homepage/blog.html` into the real public Blog listing at **`/blogs`**, replacing the `<h1>Blogs</h1>` placeholder ([src/app/blogs/page.tsx](src/app/blogs/page.tsx)) — rebuilt as React **server components** with Tailwind + the existing marketing primitives (reproducing the prototype's look/content, not copying its raw HTML/CSS). Directly mirrors the completed **About Page**: same `MarketingShell`, `--m-*` tokens, `Reveal`, pill + gradient-text patterns, and `constants.ts` data-array approach. The page is **public** (no `auth()` redirect, no `proxy.ts` matcher change — signed-in users may view it), builds **`○ (Static)`**, and sets `metadata` title `"Blogs"` → **`"Blog"`** + the prototype's `<head>` description. **Three new presentational server components** under `src/components/marketing/`: **`PostMeta`** (`PostMeta.tsx`) — the shared byline: a brand-gradient 30px avatar initials chip + an **optional** author name (shown on the featured card and detail stub, omitted on grid cards) + a dot-separated date/read-time, with a tiny local `Dot` helper; **`BlogCard`** (`BlogCard.tsx`) — grid card with an accent media block tinted by the per-card `--c` var (set via inline `style`), a `--c`-colored uppercase category tag, `<h3>` title, flex-1 excerpt, and `PostMeta`, wrapped in a `next/link` → `/blogs/<slug>` with the hover-lift; **`BlogFeatured`** (`BlogFeatured.tsx`) — the large 2-col featured card (`grid lg:grid-cols-[1.1fr_1fr]` → 1 col under `lg`, media on top) with a layered brand/pink radial-gradient media panel (`min-h-[180px]` → `lg:min-h-[260px]`), category tag, `1.7rem` `<h3>`, excerpt, and an **authored** `PostMeta`, `next/link` → `/blogs/<slug>` with `aria-label="Read: <title>"`. **Data** added to `src/components/marketing/constants.ts` following the `FEATURES`/`ABOUT_*` pattern: a `BlogPost` interface (`slug`/`title`/`excerpt`/`category`/`accent`/`author`/`date`/`readTime`/`featured?`), `BLOG_POSTS` (the **7 posts copied verbatim** from the prototype — 1 featured + 6 grid, titles/excerpts/categories/dates/read-times unchanged), a `getBlogPost(slug)` lookup, a `BLOG_AUTHOR` constant ("Romualdo Dasig"), and an `initials(name)` util ("Romualdo Dasig" → "RD"). Accents map 1:1 from the prototype's `--c-*` to `--m-*` (command/prompt/snippet/url/note/image); the page derives `featured = BLOG_POSTS.find(p => p.featured)` and `rest = BLOG_POSTS.filter(p => !p.featured)`. **Blog detail stub** at **`src/app/blogs/[slug]/page.tsx`** (so the listing's links don't dead-end): `MarketingShell`, public, **static via `generateStaticParams`** over `BLOG_POSTS` (all 7 slugs prerendered, so the route builds `●` SSG), resolves the slug with `getBlogPost` and **`notFound()`** on an unknown one, renders a centered article header (category tag, `<h1>`, authored `PostMeta`) + a **"Full article coming soon."** placeholder body, and sets `title`/`description` from the resolved post via **`generateMetadata`** (Next 16 `params: Promise<{ slug }>`, awaited). **Styling** ports the prototype's blog CSS (`styles.css:790–872`) to Tailwind **arbitrary-value utilities** with **no new `globals.css` classes** — matching the About rebuild — except the two layered `color-mix`/radial-gradient media backgrounds, which use inline `style` (referencing the parent's `--c`), consistent with how `MarketingShell` itself renders its layered ambient glow (not cleanly expressible as a single Tailwind utility). The Footer already linked **Blog → `/blogs`**, so no nav/footer wiring changed. **No unit tests** (pages + presentational components only, which the Vitest scope `src/actions/*`/`src/lib/*` deliberately excludes); **no schema/migration**, no new deps. `tsc --noEmit` clean, `npm run lint` clean (only the pre-existing untouched `prototypes/homepage/script.js` warning), `npm run test` **411/411**, and `npm run build` clean — `/blogs` is `○ (Static)`, `/blogs/[slug]` is `●` (7 slugs prerendered), homepage `/` still `ƒ`, `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, against the running dev server on port 3000): `/blogs` rendered with title **"Blog"** and **0 console errors** — the a11y snapshot confirmed the header (pill "The DevStash Blog" + gradient `<h1>` "Stash smarter, ship faster" + subtitle), the featured card (cyan "FEATURED · WORKFLOWS" tag, author byline "RD · Romualdo Dasig · Jul 10, 2026 · 6 min read", `/blogs/stop-re-deriving-that-docker-command` href), and all **6 grid cards** with correct categories/accents/dates/slugs (no author name on grid cards); a full-page screenshot confirmed the brand/pink featured media panel and the accent-tinted grid media (amber AI, blue Snippets, indigo Search) — the bottom row of grid cards sits below the fold at `opacity-0` until scrolled in, the expected `Reveal` behavior. The detail stub at `/blogs/stop-re-deriving-that-docker-command` resolved its document title from the post and rendered the centered category tag + title + authored meta + "Full article coming soon." card; **`/blogs/nope` returned HTTP 404** (standard not-found page). At **390px** the featured card stacked (media on top), the nav collapsed to a hamburger, and there was **no horizontal overflow** (`scrollWidth === clientWidth`). Throwaway verification screenshots were removed from the project root; the dev server (already running on 3000) was left as-is. **Out of scope and deferred:** the full blog-detail article body (prose / author bio / related posts — a separate future feature), the other interior pages (Docs/Support), and real CMS content beyond the prototype copy.
- **Blog Detail Page** — Fleshed out the `/blogs/[slug]` **stub** (previously just a centered header + "Full article coming soon.") into the real Blog Detail page from `prototypes/homepage/blog-post.html` — article header, gradient hero panel, full **Markdown prose body**, an author bio card, and a "Keep reading" related-posts grid — rebuilt as React **server components** with Tailwind + the existing marketing primitives (reproducing the prototype's look/content, not copying its raw HTML/CSS). Directly mirrors the completed **Blog Listing** and **About** features: same `MarketingShell`, `--m-*` tokens, `Reveal`, and `constants.ts` data pattern. The route **stays** `src/app/blogs/[slug]/page.tsx`, **public + static** — kept `generateStaticParams` (over `BLOG_POSTS`), `generateMetadata` (title/description from the resolved post), and `notFound()` on an unknown slug — so it builds **`●` (SSG, one prerendered page per slug)**; no `proxy.ts` change. **Four new presentational server components** under `src/components/marketing/`: **`ArticleHero`** (`ArticleHero.tsx` — the `~h-[320px]` rounded/bordered gradient banner, reusing the same layered brand/pink radial gradient over `--m-surface-2` that `BlogFeatured`'s media uses; `aria-hidden`, height scales `220→280→320px`), **`Prose`** (`Prose.tsx` — renders the post's Markdown `content` via **`react-markdown` + `remark-gfm`**, the same stack as the dashboard `MarkdownEditor`; react-markdown renders no raw HTML by default so the copy is safe; the wrapper carries **all** article typography as Tailwind **arbitrary-value child selectors** — `[&_p]`/`[&_h2]`/`[&_h3]`/`[&_code]`/`[&_pre]`/`[&_blockquote]`/`[&_ul]`/`[&_li]`/`[&_strong]`/`[&_a]` — with **no new `globals.css` classes**, and **returns `null` when `content` is empty** so a future content-less post never crashes), **`AuthorBio`** (`AuthorBio.tsx` — the `.author-bio` card: a larger **54px** brand-gradient avatar chip via the existing `initials()` + `<h4>` name + a dim bio line from `BLOG_AUTHOR`/`BLOG_AUTHOR_BIO`), and **`RelatedPosts`** (`RelatedPosts.tsx` — the "Keep reading" section head + a `grid-cols-1 sm:2 lg:3` of **3** `BlogCard`s = the first 3 of `BLOG_POSTS` excluding the current slug; renders nothing if there are no others). **Reused** `PostMeta` (authored byline) and `BlogCard` (related grid) unchanged. **Composed in `page.tsx`** as a narrow `mx-auto max-w-[760px]` column, each block wrapped in `Reveal`: a small **"← Back to Blog"** `next/link` → article header (category tag + `<h1>` + authored `PostMeta`) → `ArticleHero` → `Prose` → `AuthorBio` → `RelatedPosts`. **Data** (`constants.ts`): added **`content: string`** (Markdown) to the `BlogPost` interface and **every** `BLOG_POSTS` entry — the featured Docker post's body **ported verbatim** from `blog-post.html` (intro → "Stash it once" + the fenced command block → tag advice + the `<ul>` → "Let AI do the tedious part" → close, including the pull-`blockquote`), and the **other 6 authored fresh** to match each post's own title/excerpt/category (same voice, invented copy — the prototype ships only the one article), each with headings, a list, a blockquote, and (where natural) a fenced block — plus **`BLOG_AUTHOR_BIO`** = "Building DevStash — one fast, searchable, AI-enhanced hub for developer knowledge." **Styling** ports the prototype's `.prose`/`.code-block`/`.article__hero`/`.author-bio` to Tailwind **arbitrary values only** (matching the About / Blog Listing rebuilds); **per-token code syntax highlighting is out of scope** — fenced blocks render as a styled monospace block (`#0d1117`) that **scrolls internally** (`overflow-x-auto`). Only client dependency is the existing `Reveal`. **No unit tests** (pages + presentational components only — outside the Vitest `src/actions/*`/`src/lib/*` scope); **no schema/migration**, **no new deps** (`react-markdown`/`remark-gfm` already present). `tsc --noEmit` clean, `npm run lint` clean (only the pre-existing untouched `prototypes/homepage/script.js` warning), `npm run test` **411/411**, and `npm run build` clean — `/blogs/[slug]` is **`●`** (all 7 slugs prerendered), `/blogs` still `○`, `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, against the running dev server): the Docker post at `/blogs/stop-re-deriving-that-docker-command` rendered the document title from the post + **0 console errors**, with the full structure confirmed via DOM query — back link, category, `<h1>`, authored byline (RD · Romualdo Dasig · Jul 10, 2026 · 6 min read), the gradient hero, the prose (h2 "Stash it once" / "Tag for how you'll search…", h3 "Let AI do the tedious part", a `<pre>` docker code block, a `<blockquote>`, **10 inline-code spans**, a 3-item `<ul>`), the author bio (name + bio), the "Keep reading" head, and **3 related `BlogCard`s** linking to the correct `/blogs/<slug>`; **"← Back to Blog"** points to `/blogs`; an unknown slug (`/blogs/nope`) returned **HTTP 404**; at **390px** there was **no horizontal overflow** (`scrollWidth === clientWidth`) with the code block scrolling within its own container; and the `/blogs` **listing still renders** all 7 posts (1 featured + 6 grid) with the shared `BlogCard`/`PostMeta` unchanged. **Out of scope and deferred:** the other interior pages (Docs / Support), a real CMS / MDX pipeline, comments / social sharing, and per-token code syntax highlighting.
- **Documentation Page** — Turned the static prototype `prototypes/homepage/docs.html` into the real Documentation page at **`/docs`**, replacing the `<h1>Documentation</h1>` placeholder ([src/app/docs/page.tsx](src/app/docs/page.tsx)) — rebuilt as React **server components** + one small **client** component for the TOC scroll-spy, using Tailwind + the existing marketing primitives (reproducing the prototype's look/content, not copying its raw HTML/CSS). Directly mirrors the completed **About Page** / **Blog Listing** rebuilds: same `MarketingShell`, `--m-*` tokens, `Reveal`, pill + gradient-text patterns, and `constants.ts` data-array approach. The page is **public** (no `auth()` redirect, no `proxy.ts` matcher change — signed-in users may view it), builds **`○ (Static)`**, and sets `metadata` title `"Documentation"` + the prototype's `<head>` description. **Layout:** a `max-w-[1160px]` container holding a `docs-layout` grid (`grid gap-12 md:grid-cols-[232px_1fr] items-start`) — a sticky TOC sidebar (left) beside a `max-w-[760px]` prose content column (right) of **7 `DocsSection`s** (Getting started, Creating items, Item types, Collections, Search, AI features, Keyboard shortcuts), with an About-style page header (pill + gradient `<h1>` + subtitle) above it in `Reveal`. **Three new components** under `src/components/marketing/`: **`DocsToc`** (`"use client"`) — renders the `DOCS_NAV` groups as in-page anchors and runs an **`IntersectionObserver` scroll-spy** (`rootMargin: "-84px 0px -55% 0px"`) that sets the in-view section's anchor to `aria-current="location"` + active styling (brand text + `--m-surface` bg); sticky beside the content on desktop (`md:sticky md:top-[88px]`), a **static wrapped horizontal bordered row above** the content on mobile; **`DocsSection`** (server) — wraps each section with its anchor `id`, `scroll-mt-[84px]`, the shared prose typography, a docs-specific larger `h2`, and the `[&+&]` divider between consecutive sections; **`Callout`** (server) — brand-tinted leading-icon note (`{ icon: LucideIcon; children }`), used twice (`Info` "New here?" and `Sparkles` "AI actions run only when you click"). **Content** is authored as **inline JSX** in `page.tsx` with all section prose **copied verbatim** from the prototype (paragraphs, `<ul>`s, inline `<code>` ⌘K refs, the search `<pre>` with GitHub-dark accent `<span>`s, and the AI-section **Pro pill** = `bg-[linear-gradient(135deg,var(--m-prompt),#fb923c)]`). **DRY:** extracted the article typography from `Prose.tsx` into a shared exported **`proseClassName`** consumed by both `Prose` (blog Markdown) and `DocsSection` (docs JSX) — the docs `h2` overrides it with `!` since `proseClassName`'s `[&_h2]` descendant selector out-specifies a plain element utility; and added **`DOCS_NAV`** to `constants.ts` (3 groups × links `{ id, label }`) as the **single source of truth** for both the TOC render and the observed scroll-spy ids so they can't drift. **No new `globals.css` classes** (ported the prototype's docs / `.pill--pro` / `.code-block` styles to Tailwind arbitrary values, matching the About/Blog rebuilds). **One deliberate deviation:** the prototype's standalone "Docs" pill in the getting-started section was moved up into the new About-style page header to avoid a double pill (all actual copy stays verbatim). **Only added links:** TOC anchors → in-page `#<section-id>`; the Shortcuts "Support" link → `/support` (via `next/link`; route already exists); inline `⌘K` refs are plain `<code>`. **No unit tests** (page + presentational/client components only, which the Vitest scope `src/actions/*`/`src/lib/*` deliberately excludes); **no schema/migration**, **no new deps** (`react-markdown`/`remark-gfm` already present via `Prose`). `tsc --noEmit` clean, `npm run lint` clean (only the pre-existing untouched `prototypes/homepage/script.js` warning), `npm run test` **411/411**, and `npm run build` clean — `/docs` is **`○ (Static)`**, homepage `/` still `ƒ`, `ƒ Proxy (Middleware)` intact. **Verified end-to-end in a real browser** (Playwright MCP, against the running dev server): `/docs` rendered with title "Documentation" and **0 console errors** — the a11y snapshot confirmed the header, the TOC (3 groups / 8 links), all **7 sections** with verbatim content, both callouts, the code block, the Pro pill, and the Support link → `/support`. **Two scroll-spy bugs were caught in the browser — not by the checks — and fixed:** (1) the `IntersectionObserver` callback only reports *changed* entries, so picking the topmost of a single batch mis-highlighted (scrolling to `search` activated `#ai`); fixed by accumulating the full intersecting set in a `useRef` and choosing the topmost **in DOM order**, after which all of `getting-started`/`creating-items`/`item-types`/`collections`/`search`/`ai` land on their own anchor exactly; (2) the last short section (`shortcuts`) can't scroll high enough to reach the top band so it never highlighted — fixed with a **bottom-of-page fallback** (a passive scroll listener that forces the last section active at `scrollHeight`), verified to activate `#shortcuts` at the bottom. At **390px** the TOC collapses to a **static** (`position: static`) `flex flex-wrap` row with a bottom border **above** the content (`tocRect.bottom <= contentRect.top`), there is **no horizontal overflow** (`scrollWidth === clientWidth`), and the search code block **scrolls within its own `overflow-x: auto` container** rather than the page. **Out of scope and deferred:** the Support page (separate feature), the other interior pages, real docs content beyond the prototype copy, and per-token code syntax highlighting.
